---
phase: 01
plan: 01
title: Repo & org setup (artifact Phase 0)
wave: 1
depends_on: []
must_haves:
  - MIT LICENSE present
  - CODE_OF_CONDUCT.md (Contributor Covenant 2.1)
  - CONTRIBUTING.md with PR/issue conventions and clean-room rule
  - SECURITY.md with responsible disclosure policy
  - Initial README.md with TL;DR + alpha disclaimer
  - .github/ISSUE_TEMPLATE/ with bug, feature, question templates
  - .github/PULL_REQUEST_TEMPLATE.md
  - docs/brand.md (brand voice guide)
  - GitHub repo topics set
  - GitHub repo description set
  - Initial commit with all the above
deferred_to_user:
  - npm package name reservation (`stop-wasting-tokens` and `@swt-labs/cli`)
  - Domain reservation (`stopwastingtokens.dev`)
  - swt-labs org email and avatar
  - org-wide branch protection defaults
  - VBW sunset announcement (pin issue, README banner, v1.0.97-sunset release note) on the user's VBW repo
acceptance_criteria: |
  Repo at https://github.com/swt-labs/stop-wasting-tokens has README,
  MIT LICENSE, CODE_OF_CONDUCT, CONTRIBUTING, SECURITY, .github templates,
  brand.md, topics, and description. First commit lands locally.
  Repo visibility: private (per user choice; artifact recommends public).
---

# Phase 1 Plan: Repo & org setup (artifact Phase 0)

Source: `compass_artifact_wf-e5221c34-3458-4ffc-b1db-feea9e1d8ac2_text_markdown.md` § G, Phase 0.

## Tasks

### T1 — License (MIT)
Author `LICENSE` with MIT text, copyright "Tiago Serôdio (@yidakee) and SWT contributors".

### T2 — Code of Conduct
Author `CODE_OF_CONDUCT.md` using Contributor Covenant 2.1, contact email placeholder for the user to fill.

### T3 — Contributing guide
Author `CONTRIBUTING.md` covering: clean-room rule (no Claude Code leak source), commit conventions, PR template, issue triage, dev setup pointer (Phase 2 will fill), code of conduct reference.

### T4 — Security policy
Author `SECURITY.md`: supported versions (alpha — main only), reporting (placeholder email), 90-day disclosure window.

### T5 — Initial README
Author `README.md`: TL;DR (Codex-first, methodology-first, npm-distributed), "alpha — see ROADMAP" banner, install plan placeholder (Phase 9), "what is SWT" section, link to docs site (Phase 8 placeholder), license, status badges placeholder.

### T6 — Issue templates
Author `.github/ISSUE_TEMPLATE/{bug.md,feature.md,question.md}` plus `config.yml` to disable blank issues.

### T7 — Pull request template
Author `.github/PULL_REQUEST_TEMPLATE.md` with summary / motivation / changes / testing / clean-room confirmation.

### T8 — Brand voice
Author `docs/brand.md`: tone (British/Lisbon-friendly, technical not marketing, "stop wasting tokens" as a literal claim), voice rules, terminology (SWT, vbw-deprecated, methodology layer, etc.), don'ts.

### T9 — Repo metadata
Run `gh repo edit` to set description and topics: `cli`, `codex`, `methodology`, `agents`, `vibe-coding`, `npm`, `typescript`.

### T10 — VBW sunset draft
Author `docs/sunset-vbw.md`: draft text the user can paste into VBW README banner, pin issue, and v1.0.97-sunset release note. NOT pushed anywhere — user merges manually in their VBW repo.

### T11 — First commit
Stage all of the above + planning artifacts (per `planning_tracking=manual`, planning artifacts are NOT auto-committed but the user may opt to include them; for the first commit we stage source/docs only and leave `.vbw-planning/` untracked — same default as `manual`).

Wait — `planning_tracking=manual` means we DO NOT auto-ignore `.vbw-planning/` and we DO NOT auto-commit it. We let the user decide. For Phase 1's first commit we will commit the public-facing project files only and leave `.vbw-planning/` and `CLAUDE.md` for the user to stage on their own.

### T12 — Phase summary
Write `01-01-SUMMARY.md` with status=complete, deviations (private vs public, Agent-tool constraint detour), files_modified, commit_hashes.
