---
phase: 1
plan: 03
title: Documentation + CI Hardening + ADR Acceptance (PR-10 + PR-11)
type: execute
wave: 3
depends_on: ["01-02"]
cross_phase_deps: []
autonomous: false
effort_override: balanced
skills_used: []
files_modified:
  - docs/
  - docs/decisions/ADR-006-cache-control-breakpoint-placement.md
  - docs/decisions/ADR-007-budget-gate-semantics.md
  - docs/decisions/ADR-008-worktree-per-task.md
  - docs/decisions/ADR-009-windows-worktree-path-discipline.md
  - docs/decisions/ADR-010-deterministic-builds.md
  - docs/decisions/ADR-012-six-month-lts-policy.md
  - docs/decisions/ADR-013-docs-site-posture.md
  - docs/decisions/README.md
  - docs/decisions/test-debt-tracking.md
  - docs/operations/migrating-from-v2.md
  - .github/workflows/ci.yml
  - .github/workflows/provider-matrix.yml
  - .github/workflows/regression.yml
  - .github/workflows/chaos.yml
  - scripts/stub-test-regression.mjs
  - scripts/stub-test-chaos.mjs
  - scripts/stub-test-provider-matrix.mjs
  - package.json
  - packages/
  - eslint.config.mjs
  - CONTRIBUTING.md
  - CHANGELOG.md
  - README.md
  - .vbw-planning/v3-tracking.md
forbidden_commands:
  - "git push --force"
  - "rm -rf .github/workflows/"
must_haves:
  truths:
    - "docs/ is reorganized per TDD2§18.1 (methodology/, runtime/, orchestration/, dashboard/, cli/, operations/, decisions/, design/)."
    - "docs/operations/migrating-from-v2.md exists with the seven-section outline per TDD2§18.3."
    - "ADRs 001..005 are written as docs/decisions/ADR-NNN-*.md with Status: Accepted."
    - "ADRs 006..013 are written with Status: Proposed (per TDD2§22.14 — accepted later at implementing-PR merge)."
    - "All ADRs follow the canonical template: Context / Decision / Consequences (per TDD2§18.4)."
    - "ci.yml's Test step is required (no `continue-on-error: true`)."
    - "v2.3.5's 33 pre-existing test failures are EITHER fixed OR marked with `it.skip(...)` and a tracking-issue URL in the comment."
    - "ci.yml includes a `reproducible-build` job that builds twice on push-to-main and asserts byte-identical dist/ (per TDD2§15.2)."
    - "provider-matrix.yml, regression.yml, chaos.yml exist as stubs (callable but skipped pending exercising tests in M2/M3/M5)."
    - "All Codex/Claude-Code/Ollama driver references removed from docs/ and README.md (CHANGELOG retains them as historical record)."
    - "CI matrix green on all 6 OS×Node combos with the now-required Test step."
    - "eslint.config.mjs has the explicit `import/no-restricted-paths` rule matching TDD2 §4.3's From→May-import table verbatim; the rule fails CI on any forbidden import."
    - ".vbw-planning/v3-tracking.md exists as the cross-milestone tracking ledger per TDD2 §13.8 with M1 entries populated."
    - "Root CHANGELOG.md has a v3.0.0-alpha.1 section summarising M1 highlights (drivers deleted, runtime/orchestration/shared scaffold, cassette infra, token meter, first e2e, 13 ADRs)."
  artifacts:
    - {path: "docs/decisions/README.md", provides: "ADR index", contains: "ADR-001"}
    - {path: "docs/decisions/ADR-001-pi-sdk-adoption.md", provides: "ADR-001 Accepted", contains: "Status: Accepted"}
    - {path: "docs/decisions/ADR-005-delete-drivers-wholesale.md", provides: "ADR-005 Accepted", contains: "Status: Accepted"}
    - {path: "docs/operations/migrating-from-v2.md", provides: "v2→v3 migration guide", contains: "swt migrate --to=v3"}
    - {path: ".github/workflows/ci.yml", provides: "hardened CI", contains: "reproducible-build"}
    - {path: ".github/workflows/regression.yml", provides: "regression workflow stub", contains: "pnpm test:regression"}
    - {path: ".github/workflows/chaos.yml", provides: "chaos workflow stub", contains: "pnpm test:chaos"}
    - {path: ".github/workflows/provider-matrix.yml", provides: "provider matrix workflow stub", contains: "workflow_call"}
  key_links:
    - {from: "docs/decisions/ADR-001-pi-sdk-adoption.md", to: "TDD2.md§22.1", via: "supersedes"}
    - {from: "docs/operations/migrating-from-v2.md", to: ".vbw-planning/migration-script-spec", via: "describes"}
    - {from: ".github/workflows/ci.yml#reproducible-build", to: "ADR-010", via: "implements"}
---

<objective>
Close M1 by hardening CI (required tests + reproducible-build job + workflow scaffolds for M2/M3/M5) and producing the documentation artifacts that the M1 exit gate requires (ADRs 001..005 Accepted, ADRs 006..013 Proposed, v2→v3 migration guide stub, docs/ reorganized).

After this plan ships, all 12 M1 PRs are merged. The codebase has: vendor-agnostic methodology + CLI, runtime/orchestration/shared scaffolding, cassette infra, token meter, provider quirks, end-to-end mocked-Pi integration test, hardened CI, complete ADR set, and a migration story. Phase 1 (M1 Foundation) reaches its exit gate per TDD2§13.1.3.

This plan covers the final 2 of M1's 12 PRs. After it ships: M2 Single-agent path can begin.
</objective>

<context>
@.vbw-planning/phases/01-m1-foundation/01-RESEARCH.md
@.vbw-planning/phases/01-m1-foundation/01-01-PLAN.md
@.vbw-planning/phases/01-m1-foundation/01-02-PLAN.md
@TDD2.md  # §18 (docs strategy), §15 (CI/CD), §22 (ADR seeds), §17.4 (reproducible builds)
@.vbw-planning/research/recon.md
</context>

<tasks>

<task type="auto">
  <name>PR-10 Task 1 · Reorganize docs/ per TDD2§18.1</name>
  <files>
    docs/README.md                       (new top-level index)
    docs/getting-started.md              (renamed from v2 quickstart)
    docs/methodology/                    (new dir: phases, roles, must-haves, qa-tiers)
    docs/runtime/                        (new dir: pi-integration, extensions, providers, caching)
    docs/orchestration/                  (new dir: worktrees, claims, dag, crash-recovery)
    docs/dashboard/                      (new dir: panels, permission-gates, cmd-k)
    docs/cli/                            (new dir: reference + per-verb files)
    docs/operations/                     (new dir: observability, budget, failover, migrating-from-v2)
    docs/decisions/                      (new dir: ADRs)
    docs/design/                         (new dir: TDD2 archive landing zone)
    README.md                            (root — update to remove codex/ollama refs, point to docs/)
  </files>
  <action>
1. Pre-audit current `docs/` content (v2.3.5 has 41 files per recon.md F-02):
   ```bash
   find docs -type f | sort
   ```
   Categorize each existing file by the v3 docs/ topical structure (TDD2§18.1).

2. Create the v3 topical structure as empty directories first:
   ```bash
   mkdir -p docs/{methodology,runtime,orchestration,dashboard,cli/verbs,operations,decisions,design,design/benchmark}
   ```

3. **Move** (not copy) existing v2 docs into the new structure. Use `git mv` to preserve history:
   - Quickstart / getting-started → `docs/getting-started.md`
   - Methodology docs (phases, roles, must-haves, qa-tiers) → `docs/methodology/`
   - CLI reference → `docs/cli/reference.md` + per-verb under `docs/cli/verbs/`
   - Existing observability/budget/release docs → `docs/operations/`
   - Architecture / design docs → `docs/design/`
   - Anything codex-specific or ollama-specific → **DELETE** (`git rm`), not moved.

4. Create new v3-specific files (initially as stubs with frontmatter + "Status: stub — populated in M{N}"):
   - `docs/runtime/pi-integration.md` — pointer to TDD2§5 + future expansion
   - `docs/runtime/extensions.md` — pointer to TDD2§5.4 + future expansion
   - `docs/runtime/providers.md` — pointer to TDD2§7 + the quirks.json reference
   - `docs/runtime/caching.md` — pointer to TDD2§8.2 (provider-shim caching)
   - `docs/orchestration/worktrees.md`, `claims.md`, `dag.md`, `crash-recovery.md` — pointers to TDD2§9
   - `docs/dashboard/panels.md`, `permission-gates.md`, `cmd-k.md` — pointers to TDD2§12
   - `docs/operations/observability.md`, `budget.md`, `failover.md` — pointers to TDD2§16, §8.4, §7.4

5. Write the new `docs/README.md` as the docs index. Include:
   - One-line project description (from PROJECT.md)
   - "Authoritative design: TDD2.md at the repo root."
   - Section-by-section links into `docs/methodology/`, `docs/runtime/`, etc.
   - "Decisions:" link to `docs/decisions/`

6. Update root `README.md`:
   - Remove all references to `codex-driver`, `claude-code-driver`, `ollama-driver`.
   - Remove "Choose a backend" / "configure backend" sections (the backend is Pi, full stop).
   - Replace any "supports Codex, Claude Code, Ollama" claims with "vendor-agnostic via Pi (Anthropic, OpenAI, OpenRouter, Bedrock, ...)".
   - Add a "Migrating from v2.x?" section pointing to `docs/operations/migrating-from-v2.md`.
   - Add a "Design" section pointing to TDD2.md.

7. Update `CHANGELOG.md`: do NOT remove historical entries about codex/claude-code/ollama (the CHANGELOG is historical record). Add a new v3.0.0-alpha.1 entry summarizing M1 (will be populated incrementally by changesets).

8. Run Vale (if configured) and fix any new style violations introduced.

9. Run `pnpm format:check` to verify markdown formatting passes.

10. **Write the explicit `import/no-restricted-paths` rule in `eslint.config.mjs`** (REQ-14 + TDD2 §13.1.3 exit gate + §4.3 — the enforcement mechanism for the entire layered architecture). The rule body matches the §4.3 From→May-import table verbatim:
    ```js
    // eslint.config.mjs — append to existing config array
    {
      files: ['packages/**/*.{ts,tsx}'],
      rules: {
        'import/no-restricted-paths': ['error', {
          zones: [
            // shared/ is the leaf — may not import anything except std/zod/typebox/tsc-types
            { target: 'packages/shared', from: 'packages', except: ['packages/shared'] },
            // core/ may only import shared/
            { target: 'packages/core',   from: 'packages', except: ['packages/core', 'packages/shared'] },
            // runtime/ may only import shared/
            { target: 'packages/runtime', from: 'packages', except: ['packages/runtime', 'packages/shared'] },
            // orchestration/ may import core, runtime, shared
            { target: 'packages/orchestration', from: 'packages', except: ['packages/orchestration', 'packages/core', 'packages/runtime', 'packages/shared'] },
            // dashboard/ may import core, orchestration, runtime, shared (NOT cli)
            { target: 'packages/dashboard', from: 'packages', except: ['packages/dashboard', 'packages/core', 'packages/orchestration', 'packages/runtime', 'packages/shared'] },
            // cli/ (Layer 5 public surface) may import dashboard, core, orchestration, runtime, shared
            { target: 'packages/cli', from: 'packages', except: ['packages/cli', 'packages/dashboard', 'packages/core', 'packages/orchestration', 'packages/runtime', 'packages/shared'] },
            // test-utils/ may import any
          ],
        }],
        // Principle 1 enforcement: core may not import any @earendil-works/* package
        'no-restricted-imports': ['error', {
          patterns: [{
            group: ['@earendil-works/*'],
            message: 'Principle 1: only packages/runtime/ may import @earendil-works/*. See TDD2 §4.3.',
          }],
        }],
      },
    },
    // Override the @earendil-works ban inside runtime/ only:
    {
      files: ['packages/runtime/**/*.{ts,tsx}'],
      rules: { 'no-restricted-imports': 'off' },
    },
    ```
    Add a minimal test that asserts a forbidden import fails the lint: create `packages/core/test/eslint-boundary.test.ts` containing one test that writes a temp `packages/core/src/__forbidden__.ts` importing from `@earendil-works/pi-coding-agent`, runs ESLint against it programmatically, and asserts at least one error. Clean up the temp file in afterEach.

11. **Add v3.0.0-alpha.1 section to root `CHANGELOG.md`** (TDD2 §13.1.4 checklist item 9). Section template:
    ```markdown
    ## v3.0.0-alpha.1 — M1 Foundation (in-progress)

    ### Removed
    - `@swt-labs/codex-driver`, `@swt-labs/claude-code-driver`, `@swt-labs/ollama-driver` packages deleted wholesale (ADR-005).
    - `.codex-plugin/` directory deleted.

    ### Added
    - `@swt-labs/runtime` — thin adapter over `@earendil-works/pi-coding-agent` (peerDep, pinned-range dep).
    - `@swt-labs/orchestration` — sequential dispatcher (worktrees land in M3).
    - `@swt-labs/shared` — types + Zod schemas (consolidates v2 `core/types/` + `dashboard-core/schemas/`).
    - `@swt-labs/test-utils` (private) — cassette recorder/replayer + first two cassettes.
    - `runtime/meter/` — token meter with byte-identical cassette-replay assertion.
    - `runtime/providers/quirks.json` + `role-resolver.ts` — vendor-agnostic per-role tier→model resolution.
    - `runtime/extensions/result-protocol.ts` — `swt_report_result` Extension custom tool (closure-captured `pi.appendEntry`).
    - `core/abstractions/SpawnerEnvironment` — new minimal adapter consumed by `vibe.ts` + `doctor.ts`.
    - 7 ADRs Accepted (001..005, 010, 011-Proposed); 6 Proposed (006..009, 012, 013).
    - `reproducible-build` CI job; `regression.yml`, `chaos.yml`, `provider-matrix.yml` workflow stubs.

    ### Changed
    - CI `Test` step is now required (removed `continue-on-error: true`); 33 v2.3.5 pre-existing test failures remediated or tracked.
    - ESLint `import/no-restricted-paths` rule enforces TDD2 §4.3 layered architecture.

    ### Migration
    - v2.x users: see `docs/operations/migrating-from-v2.md`.
    ```
    Do NOT delete or rewrite historical v2.x sections of CHANGELOG.md — they're historical record (per Plan 01-02 PR-05 note).
  </action>
  <verify>
1. `tree docs/ -L 2` matches the structure in TDD2§18.1.
2. `grep -rE "codex-driver|claude-code-driver|ollama-driver" docs/ README.md` returns nothing.
3. `find docs -type f` returns ≥ the v2 file count minus deleted codex/ollama-specific files plus new v3 stubs.
4. `pnpm format:check` passes.
5. Vale (if configured in ci.yml's vale workflow) passes.
6. `git log --follow docs/methodology/phases.md` shows continuity with the v2 file it was moved from (verifies `git mv` preserved history).
7. `grep -nE "import/no-restricted-paths" eslint.config.mjs` returns hits; `pnpm lint` is green on the current tree; `pnpm test --filter @swt-labs/core` is green (eslint-boundary test passes — assertion: forbidden import fails the lint).
8. `grep -nE "^## v3\.0\.0-alpha\.1" CHANGELOG.md` returns one hit.
  </verify>
  <done>
docs/ is in the v3 topical structure; root README no longer mentions deleted drivers; CHANGELOG preserved.
  </done>
</task>

<task type="auto">
  <name>PR-10 Task 2 · Write the v2→v3 migration guide</name>
  <files>
    docs/operations/migrating-from-v2.md
    docs/operations/migration-faq.md     (split: FAQ section as its own file when it grows)
  </files>
  <action>
1. Use TDD2§18.3's outline verbatim. Seven sections:
   1. **What changed at a glance** — table: v2 concept → v3 concept (backend config → tier mapping, codex hooks → Pi events, etc.)
   2. **Pre-migration checklist** — verify `.swt-planning/` committed; verify project on v2.3.5; back up via `cp -r .swt-planning .swt-planning.v2-backup`
   3. **Running the migration script** — exact commands:
      ```bash
      npm install -g stop-wasting-tokens@3
      swt migrate --to=v3
      ```
   4. **What the script does** — step-by-step explanation per artefact, mirroring TDD2§11.3:
      - **`schema_version` policy (REQ-06):** the additive `schema_version: 1` marker lands at **migrate-time** in M6 PR-49, NOT during M1. M1 leaves `.swt-planning/` schemas untouched; v2.x projects continue without a version field; v3.0 projects gain `schema_version: 1` when `swt migrate --to=v3` runs. Document this in the migration guide so users don't expect M1 to retroactively bump their schema.
      - `config.json`: removes `backend:` field; adds `roles[*].tier`; adds `router_strategy: 'tier-routed'`; **adds top-level `schema_version: 1`**.
      - PROJECT/REQUIREMENTS/ROADMAP/STATE: no content change; the schema_version marker on `config.json` covers the whole `.swt-planning/` tree (one marker, not per-file).
      - `phases/NN-slug/plan-NN.md`: gains `claims: []` and `depends_on: []` (initially empty for legacy plans)
      - New directories: `parallel/`, `journal/`, `locks/`
      - New file: `budget-state.json` initialized to zeros
   5. **Verification** — `swt doctor` post-migration confirms Pi peerDep resolved, `swt status` shows correct phase state
   6. **Backing out** — `git revert <migration-commit>` + reinstall v2.3.x; users who did NOT commit before migrating can `mv .swt-planning.v2-backup .swt-planning`
   7. **FAQ** — anticipated questions:
      - "Will my custom skills still work?" → Yes; Pi skill discovery loads from `.pi/skills/` or `.swt-planning/skills/`
      - "What if I used the Claude Code or Ollama backend?" → v3 uses Pi as the only runtime; Pi has native providers for both. Update `.swt-planning/config.json` per the new tier format
      - "Will v2.x still get patches?" → Yes, 6-month LTS per ADR-012
      - "Is my data backed up automatically?" → No — the user is expected to commit or back up before migrating. The migration script does NOT auto-backup (verified decision)
      - "Can I run both v2 and v3 in the same repo?" → No; they're mutually exclusive

2. Each section ≥ 4-6 paragraphs of real prose; this is a user-facing artifact, not an outline.

3. Add a "Known migration issues" section listing edge cases discovered during the M6 PR-49 migration-script test (left as a stub until M6 hits — for M1, just an empty placeholder).

4. Cross-link from `docs/README.md` and root `README.md`.

5. Note in the file's frontmatter: `audience: v2.x users migrating to v3.0`.
  </action>
  <verify>
1. `wc -l docs/operations/migrating-from-v2.md` is ≥ 200 (real prose, not a stub).
2. `grep -c "^## " docs/operations/migrating-from-v2.md` is ≥ 7 (all seven sections present).
3. `grep "swt migrate --to=v3" docs/operations/migrating-from-v2.md` returns at least one hit.
4. Markdown lint clean.
  </verify>
  <done>
v2→v3 migration guide is publishable. Anyone on v2.3.5 can follow it to upgrade to v3.0.
  </done>
</task>

<task type="auto">
  <name>PR-10 Task 3 · Draft ADRs 006..013 + write ADR index</name>
  <files>
    docs/decisions/README.md                                          (ADR index with status column — covers all 13)
    docs/decisions/ADR-006-cache-control-breakpoint-placement.md      (Proposed)
    docs/decisions/ADR-007-budget-gate-semantics.md                   (Proposed)
    docs/decisions/ADR-008-worktree-per-task.md                       (Proposed)
    docs/decisions/ADR-009-windows-worktree-path-discipline.md        (Proposed)
    docs/decisions/ADR-010-deterministic-builds.md                    (Proposed → Accepted upon PR-11 merge)
    docs/decisions/ADR-012-six-month-lts-policy.md                    (Proposed)
    docs/decisions/ADR-013-docs-site-posture.md                       (Proposed, deferred)
  </files>
  <action>
**Scope note:** ADRs land at their **implementing** PR per TDD2 §22.14 (the canonical ADR index — Status promotes to Accepted at the PR that actually realises the decision, not the PR that first mentions it):
- **Plan 01-01:** ADR-001 + ADR-004 Accepted at PR-02; ADR-002 Proposed at PR-02.
- **Plan 01-02:** ADR-005 Accepted at PR-05; ADR-011 Proposed at PR-06 (promotes to Accepted in M5 PR-44, NOT M1); ADR-003 Accepted at PR-08; ADR-002 promotes from Proposed to Accepted at PR-09.
- **This task (01-03 PR-10 Task 3):** drafts the remaining **7 ADRs (006..010, 012, 013)** plus the index (`docs/decisions/README.md`) that ties all 13 together. ADR-010 promotes to Accepted at this plan's PR-11 merge.

1. Copy the ADR skeletons from TDD2§22.6..22.13 verbatim into individual files (skip 001..005 + 011 which other PRs handle). The TDD2 skeletons follow the canonical template (Context / Decision / Consequences) per TDD2§18.4 and are ≤ 500 words each.

2. For ADRs 006..009, 012, 013, set `Status: Proposed` with the implementing-PR identifier (e.g., ADR-006 → M4 PR-32, ADR-008 → M3 PR-22). The status promotes to Accepted at implementing-PR merge per TDD2§22.14 lifecycle note. ADR-013 stays in `Status: Deferred` (deferred until ~1000 users — see TDD2§18.6).

3. **Special case ADR-010** — lands in this same PR-11 (Task 5 adds the reproducible-build CI job). Promote to `Status: Accepted` in this PR's diff with frontmatter note `accepted_in: M1 PR-11`.

4. **Verify ADRs 001..005 + 011 exist** (drafted by prior PRs per the scope note above). If any are missing — meaning a prior PR didn't draft them as expected — STOP and reconcile with Plan 01-01 / Plan 01-02 before continuing. Do NOT re-draft them here (that would create scope creep + ownership confusion).

6. Write `docs/decisions/README.md` as the ADR index. Include:
   - Lifecycle note: "ADRs are Proposed when drafted, Accepted when their implementing PR merges. Vale enforces the Status field on every ADR."
   - Status table: ADR-NNN | Title | Status | Decided | PR
   - The 13 rows with current status

7. Each ADR has YAML frontmatter at the top:
   ```yaml
   ---
   adr: NNN
   title: "..."
   status: "Accepted" | "Proposed" | "Deferred"
   decided: YYYY-MM-DD
   pr: "M1 PR-XX"
   ---
   ```
   This makes the index automatable later (e.g., for Vale's `Verified-Status` rule per TDD2§15.9).

8. Each ADR body uses the canonical template exactly:
   ```markdown
   # ADR-NNN — Title

   **Status:** Accepted

   ## Context
   ...

   ## Decision
   ...

   ## Consequences
   Easier:
   - ...
   Harder:
   - ...
   ```

9. Cross-link: each ADR references TDD2.md (the design doc it implements) and the PR (when known).
  </action>
  <verify>
1. `ls docs/decisions/ADR-*.md | wc -l` returns 13.
2. At M1 close (after PR-11 merges): `grep -l "Status: Accepted" docs/decisions/ADR-*.md | wc -l` returns **6** — ADRs 001, 002 (promoted at 01-02 PR-09), 003, 004, 005, 010 (promoted at this PR-11).
3. `grep -l "Status: Proposed" docs/decisions/ADR-*.md | wc -l` returns **6** — ADRs 006, 007, 008, 009, 011 (promotes in M5 PR-44), 012.
4. `grep -l "Status: Deferred" docs/decisions/ADR-*.md | wc -l` returns **1** — ADR-013 (deferred until ~1000 users per TDD2 §18.6).
4. Each ADR is ≤ 500 words (`wc -w docs/decisions/ADR-*.md | awk '$1 > 500 {print}'` returns nothing).
5. Vale passes on `docs/decisions/`.
6. `docs/decisions/README.md` index matches the file list with correct statuses.
  </verify>
  <done>
All 13 ADRs exist as separate files following the canonical template. Status lifecycle is correctly tracked. ADR-011 already Accepted from Plan 01-02; ADR-010 promotes to Accepted at this PR's merge.
  </done>
</task>

<task type="auto">
  <name>PR-11 Task A · Remediate the 33 test failures; remove `continue-on-error: true`</name>
  <files>
    packages/**/test/                              (selective: fix or skip-with-issue)
    .github/workflows/ci.yml                       (remove `continue-on-error: true` from Test step)
    docs/decisions/test-debt-tracking.md           (new: tracks each skipped test with its tracking-issue URL)
  </files>
  <action>
1. Run the full test suite locally with verbose output to capture every failure:
   ```bash
   pnpm test --reporter=verbose 2>&1 | tee /tmp/test-failures.log
   ```
   Confirm count matches v2.3.5's documented 33 (per recon.md F-06). If the count diverges materially (>5 in either direction), investigate the drift before remediation.

2. Classify each failure:
   - **`fixable`** — clear bug or assertion drift; fix in this PR
   - **`prettier-fixture-drift`** — expected-output fixtures stale vs Prettier 3.x formatting; regenerate fixtures
   - **`obsolete`** — tests for code that was deleted (codex/claude-code/ollama tests not covered by Plan 01-02 PR-05's cascading removal); delete the test file
   - **`skip-with-issue`** — real bug, but out of M1 scope; mark `it.skip(...)` with a tracking-issue URL in the comment

3. For each `fixable`:
   - Make the minimum diff to pass the assertion.
   - Don't refactor surrounding code (out of scope for M1).
   - If the fix would require ≥ 50 lines of change, demote to `skip-with-issue`.

4. For each `prettier-fixture-drift`:
   - Regenerate the fixture: `prettier --write <fixture-path>`.
   - Verify the test now passes.

5. For each `obsolete`:
   - `git rm packages/<pkg>/test/<file>.test.ts`.
   - Common candidates: any test mentioning `CodexAgentSpawner`, `detectCodexVersion`, `OllamaAgentSpawner`, codex hooks.

6. For each `skip-with-issue`:
   - **Prerequisite:** `gh auth status` must report an authenticated user with `repo` scope on the swt-labs/stop-wasting-tokens repo. If not, run `gh auth login` first.
   - Create the tracking issue with `gh` so the URL is captured deterministically (no manual web-UI step):
     ```bash
     ISSUE_URL=$(gh issue create \
       --repo swt-labs/stop-wasting-tokens \
       --title "[v3-debt] <test name> — <brief reason>" \
       --label "v3-debt,test-skip" \
       --body "Auto-generated during M1 PR-11 test-debt remediation. Test marked it.skip pending fix. Scope: ..." \
       | tail -1)
     ```
   - Edit the test: `it('...', () => {})` becomes `it.skip('... — see ${ISSUE_URL}', () => {})`.
   - Add a comment line above: `// TODO(v3-debt): tracking ${ISSUE_URL}`.
   - Append a row to `docs/decisions/test-debt-tracking.md` referencing the issue.

7. Update the new `docs/decisions/test-debt-tracking.md` (a project artifact, not an ADR) with a table:
   | Test | Status | Tracking issue | Disposition |
   For each of the 33 (and any new failures introduced) record its disposition.

8. Edit `.github/workflows/ci.yml`:
   - Remove `continue-on-error: true` from the `Test` step.
   - The step name can stay `Unit + integration tests` (per Plan 01-02 PR-06's ci.yml update, if applied; otherwise just `Test`).

9. Run the full pipeline locally to confirm green:
   ```bash
   pnpm install --frozen-lockfile
   pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build
   ```
   All must pass. If anything red after remediation, return to step 2 and reclassify.

10. Run on Windows (via CI matrix) to catch any POSIX-only assumptions in remediated tests.
  </action>
  <verify>
1. `grep -n "continue-on-error" .github/workflows/ci.yml` shows no occurrence on the Test step (may still be present elsewhere if appropriate, but not on Test).
2. `pnpm test` exits 0 locally on Linux + macOS + Windows.
3. `grep -rE "it\.skip\(" packages/ --include='*.test.ts' | wc -l` ≤ the count from step 6 (cap on accepted debt).
4. Every `it.skip` has an `ISSUE-URL` in its description string.
5. `docs/decisions/test-debt-tracking.md` exists and has a row for every skipped test.
6. CI matrix green on all 6 OS×Node combos with the Test step now required.
  </verify>
  <done>
v2.3.5's test-debt is discharged or explicitly tracked. CI's Test step is required gating; new failures will fail CI immediately.
  </done>
</task>

<task type="auto">
  <name>PR-11 Task B · Add v3 CI workflow scaffolds (reproducible-build, regression, chaos, provider-matrix)</name>
  <files>
    .github/workflows/ci.yml                       (add reproducible-build job to existing workflow)
    .github/workflows/regression.yml               (NEW)
    .github/workflows/chaos.yml                    (NEW)
    .github/workflows/provider-matrix.yml          (NEW)
  </files>
  <action>
1. Edit `.github/workflows/ci.yml` — add the `reproducible-build` job after the existing `build` matrix per TDD2§15.2:
   ```yaml
   reproducible-build:
     name: Reproducibility check
     runs-on: ubuntu-latest
     needs: build
     if: github.event_name == 'push'              # main + release branches only
     steps:
       - uses: actions/checkout@v4
       - uses: pnpm/action-setup@v4
       - uses: actions/setup-node@v4
         with: { node-version: 22, cache: pnpm }
       - run: pnpm install --frozen-lockfile
       - name: Build twice and diff
         run: |
           pnpm build
           mv dist dist-first
           pnpm build
           diff -r dist-first dist
       - name: Upload first build on diff failure
         if: failure()
         uses: actions/upload-artifact@v4
         with: { name: dist-first, path: dist-first }
   ```
   This satisfies ADR-010 (Accepted at this PR's merge).

2. Create `.github/workflows/regression.yml` per TDD2§15.4 — STUB for now; the regression suite doesn't exist yet (lands in M2 PR-18):
   ```yaml
   name: Regression
   on:
     pull_request:
       paths:
         - 'packages/**'
         - 'test/regression/**'
         - 'packages/test-utils/golden/**'
         - 'packages/test-utils/cassettes/**'
   jobs:
     regression:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: pnpm/action-setup@v4
         - uses: actions/setup-node@v4
           with: { node-version: 22, cache: pnpm }
         - run: pnpm install --frozen-lockfile
         - name: Run regression suite
           run: pnpm test:regression || echo "Regression suite not yet implemented (lands in M2 PR-18)"
         - name: Upload diff report
           if: failure()
           uses: actions/upload-artifact@v4
           with:
             name: regression-diff
             path: test/regression/output/diff-report.html
           continue-on-error: true                  # report may not exist yet
   ```
   Note: `pnpm test:regression` script needs to be added to root package.json — for M1 it routes to `scripts/stub-test-regression.mjs` (see step 5 below for the canonical cross-platform stub pattern). Real implementation in M2 PR-18.

3. Create `.github/workflows/chaos.yml` per TDD2§15.5 — STUB; chaos tests land in M3 PR-28:
   ```yaml
   name: Chaos
   on:
     pull_request:
       types: [labeled, synchronize]
     schedule:
       - cron: '0 7 * * *'        # nightly 07:00 UTC
   jobs:
     chaos:
       if: github.event_name == 'schedule' || contains(github.event.pull_request.labels.*.name, 'run:chaos')
       runs-on: ${{ matrix.os }}
       strategy:
         matrix:
           os: [ubuntu-latest, macos-latest, windows-latest]
       steps:
         - uses: actions/checkout@v4
         - uses: pnpm/action-setup@v4
         - uses: actions/setup-node@v4
           with: { node-version: 22, cache: pnpm }
         - run: pnpm install --frozen-lockfile
         - run: pnpm test:chaos || echo "Chaos suite not yet implemented (lands in M3 PR-28)"
   ```
   Add `pnpm test:chaos` stub to root package.json similarly.

4. Create `.github/workflows/provider-matrix.yml` per TDD2§15.3 — STUB; provider-matrix tests land in M5 PR-44 (the simulated-failover test) but cassette-based stubs for the test infrastructure could happen earlier:
   ```yaml
   name: Provider Matrix
   on:
     workflow_call: {}
     schedule:
       - cron: '0 5 * * *'        # nightly 05:00 UTC
   jobs:
     matrix:
       name: ${{ matrix.provider }}
       runs-on: ubuntu-latest
       strategy:
         fail-fast: false
         matrix:
           provider:
             - anthropic
             - openai
             - openrouter-deepseek
             - openrouter-kimi
             - google
             - bedrock
       steps:
         - uses: actions/checkout@v4
         - uses: pnpm/action-setup@v4
         - uses: actions/setup-node@v4
           with: { node-version: 22, cache: pnpm }
         - run: pnpm install --frozen-lockfile
         - run: pnpm test:provider-matrix --filter ${{ matrix.provider }} || echo "Provider matrix tests land in M5 PR-44"
           env:
             SWT_MOCK_PROVIDER: ${{ matrix.provider }}
   ```
   Add `pnpm test:provider-matrix` stub to root package.json.

5. Add the three pnpm scripts to root `package.json` as stubs. Use a tiny `.mjs` file per stub rather than inline `node -e` (Windows PowerShell quoting differs from bash; inline strings break on Windows CI):
   ```jsonc
   "scripts": {
     ...,
     "test:regression":      "node scripts/stub-test-regression.mjs",
     "test:chaos":           "node scripts/stub-test-chaos.mjs",
     "test:provider-matrix": "node scripts/stub-test-provider-matrix.mjs"
   }
   ```
   And create each stub script (`scripts/stub-test-regression.mjs` etc.):
   ```js
   // scripts/stub-test-regression.mjs
   console.log('Regression suite lands in M2 PR-18; this stub exits 0 for CI to pass.');
   process.exit(0);
   ```
   Three nearly-identical files; each names the milestone+PR where the real test runner lands.
   These stubs are replaced with real test runners as each milestone lands them.

6. Update branch protection rules documentation in `CONTRIBUTING.md` (the existing root-level file in v2.3.5 — verified present). Add a new `## Branch Protection (v3)` section with:
   - Required status checks: `build (ubuntu-latest / Node 22)`, `build (macos-latest / Node 22)`, `build (windows-latest / Node 22)`, `reproducible-build`, `CodeQL`, `Vale`.
   - Required reviews: 1; for changes under `packages/runtime/`, 2.
   - Linear history required; no force-push to main; no branch deletion of main.

7. Verify ADR-010 (deterministic builds) promotes to `Status: Accepted` upon this PR's merge. Update `docs/decisions/ADR-010-deterministic-builds.md` accordingly.

8. **Create `.vbw-planning/v3-tracking.md`** (TDD2 §13.8 — the cross-milestone tracking ledger that surfaces in the dashboard's Milestones panel). Skeleton:
   ```markdown
   # v3 Cross-milestone tracking

   Live ledger surfacing PRs merged, ADRs decided, TPAC measurements, cache hit ratios, and exit-gate signoffs across M1..M6. Updated in the same PR that lands each tracked item — not retroactively. Read by the dashboard's Milestones panel.

   ## M1 — Foundation
   | PR | Subject | Merged | ADRs touched | Notes |
   |---|---|---|---|---|
   | PR-01a | methodology→codex-driver edge break | TBD | — | Entry gate |
   | PR-01b | cli→{3-drivers} edge break + SpawnerEnvironment | TBD | — | Entry gate |
   | PR-02 | packages/runtime/ skeleton + Pi peerDep | TBD | 001, 002 (P), 004 | |
   | PR-03 | packages/orchestration/ skeleton | TBD | — | |
   | PR-04 | packages/shared/ — types + schemas | TBD | — | |
   | PR-05 | Delete 3 drivers + .codex-plugin/ | TBD | 005 | |
   | PR-06 | Cassette infrastructure | TBD | 011 (P) | |
   | PR-07 | Token meter + byte-identical replay assertion | TBD | — | |
   | PR-08 | Provider quirks + role-resolver | TBD | 003 | |
   | PR-09 | First end-to-end mocked-Pi e2e | TBD | 002 (A) | |
   | PR-10 | Docs reorg + migration guide + ADRs 006..013 | TBD | 006..013 | |
   | PR-11 | CI Test required + reproducible-build + v3-tracking.md | TBD | 010 (A) | M1 exit |

   ## M2..M6 — sections added when each milestone enters.

   ## Metrics (filled in over time)
   | Milestone | TPAC | Cache hit ratio | Cost / criterion | Notes |
   |---|---|---|---|---|
   | M2 baseline | TBD | n/a (M4) | TBD | Established at M2 close |
   | M4 target  | −40% vs M2 | ≥70% | −50% | Hard merge gate per §13.4.2 |
   ```
   Initial M1 PR rows can be backfilled by the executor when each PR merges — this task just creates the skeleton with the M1 table pre-populated (Merged column = "TBD" until the row's PR actually merges; updated in that PR's diff).

9. **Update TDD2 §19 risk register with M1-discovered risks** (TDD2 §13.1.4 checklist item 6 — the exit-interview asks "what new risks surfaced?"). The format follows the existing §19 table. For M1, **at least scan** these candidate sources:
   - Plan 01-01 risks R-01..R-05 — already in TDD2 §13.1.6; check if any escalated during execution.
   - 33 test-failure remediation (Task A above) — note any failure clusters that suggest a deeper bug than carry-forward.
   - Cassette recording cost overruns (Plan 01-02 PR-06 / PR-09).
   - Pi version drift between recon (May 2026) and PR-02 merge date.
   - Any new dashboard → cli edge issues surfaced (recon §1.3 noted this v2 edge but M1 doesn't break it — confirm whether M2 PR-17 still owns the rewrite).
   Update `TDD2.md` §19 in this PR's diff. If no new risks surfaced, add an explicit "M1 closed — no new risks" line to the section's preamble — silence is too easy to misread as "nobody looked."
  </action>
  <verify>
1. `ls .github/workflows/ | sort` shows 8 files: ci.yml, codeql.yml, install-smoke.yml, release.yml, vale.yml, provider-matrix.yml, regression.yml, chaos.yml (matches TDD2§15.1).
2. `grep -n "reproducible-build" .github/workflows/ci.yml` returns hits.
3. `jq -r '.scripts | keys[]' package.json | grep -E "^test:(regression|chaos|provider-matrix)$" | wc -l` returns 3.
4. CI matrix green: pushing to a branch triggers ci.yml; pushing to main also triggers reproducible-build; the new stub workflows exist but don't break CI.
5. `docs/decisions/ADR-010-deterministic-builds.md` has `Status: Accepted` after this PR's merge.
6. `test -f .vbw-planning/v3-tracking.md` is true; `grep -c "^| PR-" .vbw-planning/v3-tracking.md` returns ≥12 (the M1 PR table is populated).
7. `git diff TDD2.md` in this PR shows §19 risk register touched (either new rows or the "M1 closed — no new risks" line).
  </verify>
  <done>
v3 CI infrastructure is in place. Future milestones (M2 regression, M3 chaos, M5 provider-matrix) drop in their tests and the workflows pick them up automatically. ADR-010 promotes to Accepted.
  </done>
</task>

</tasks>

<verification>
1. **M1 exit gate (TDD2§13.1.3) — final check:**
   - All unit tests pass for `core/`, `runtime/`, `orchestration/`, `shared/` in isolation (with mocked Pi). ✓ from Plan 01-02 PR-07/PR-09
   - Integration test dispatches a no-op Scout task and returns parsed TaskResult. ✓ from Plan 01-02 PR-09
   - `grep -r "codex exec\|@swt-labs/{codex,claude-code,ollama}-driver" packages/` returns nothing. ✓ from Plan 01-02 PR-05
   - Token meter delta = 0 tokens on cassette replay. ✓ from Plan 01-02 PR-07
   - `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build` all green on M1 branch. ✓ this plan PR-11
   - CI matrix green on all 6 combos. ✓ this plan PR-11
   - **ESLint `import/no-restricted-paths` rule enforced with the explicit From→May-import table from §4.3** — written as a first-class deliverable in PR-10 Task 1 step 10, including a regression test that asserts forbidden imports fail the lint. ✓ this plan PR-10
   - ADRs Accepted at M1 close: **6** — ADR-001 + ADR-004 (Plan 01-01 PR-02), ADR-005 (Plan 01-02 PR-05), ADR-003 (Plan 01-02 PR-08), ADR-002 (Plan 01-02 PR-09), ADR-010 (this plan PR-11).
   - ADRs Proposed at M1 close: **6** — ADRs 006..009 + 011 (promotes in M5 PR-44) + 012 (promotes in M6 PR-53).
   - ADR Deferred: **1** — ADR-013 (deferred until ~1000 users per TDD2 §18.6).

2. **Documentation coherence:**
   - docs/ reorganized per TDD2§18.1.
   - Root README has no driver references.
   - v2→v3 migration guide is publishable.
   - 13 ADRs exist with correct status lifecycle.

3. **CI hardening:**
   - Test step required (no `continue-on-error: true`).
   - reproducible-build job runs on push-to-main.
   - regression / chaos / provider-matrix workflows exist as stubs ready for M2/M3/M5 content.
   - Branch protection rules documented (configured manually in GitHub UI by the repo admin).

4. **Test-debt accountability:**
   - Every skipped test has a tracking-issue URL.
   - `test-debt-tracking.md` is the source of truth for outstanding debt.
   - No `continue-on-error` masks failures.
</verification>

<success_criteria>
After PR-10 and PR-11 merge:
- M1 exits cleanly per TDD2§13.1.3.
- Documentation is in v3 shape (TDD2§18.1).
- **13 ADRs exist; 6 Accepted (001, 002, 003, 004, 005, 010), 6 Proposed (006..009, 011, 012), 1 Deferred (013).** Matches §22.14 verbatim.
- CI matrix is fully gating with Test step required.
- Four v3 CI workflows exist (3 as stubs ready for M2/M3/M5; reproducible-build active).
- v2→v3 migration guide is user-ready (including the `schema_version: 1` marker policy: bump at migrate-time, not retroactively).
- Test debt is fully accounted for in `docs/decisions/test-debt-tracking.md`.
- **ESLint `import/no-restricted-paths` enforces TDD2 §4.3** with a regression test in `packages/core/test/eslint-boundary.test.ts`.
- **`.vbw-planning/v3-tracking.md`** ledger initialised with the M1 PR table populated.
- **Root `CHANGELOG.md`** has a `v3.0.0-alpha.1` section summarising M1 (alongside the unchanged historical v2.x entries).
- **TDD2 §19 risk register** updated with any M1-discovered risks (or an explicit "no new risks" line).
- **Telemetry event registry** has M1 entries (`swt.m1.meter.updated`, `swt.m1.cassette.replay_*`, `swt.m1.task_result.parsed` — from Plan 01-02 PR-07).

**M2 entry conditions met:**
- Phase 1 (M1 Foundation) marked Complete in `.swt-planning/STATE.md` (VBW's PostToolUse hook updates phase status when each SUMMARY.md lands; once all 3 plans report `status: complete`, the hook flips Phase 1 to `Complete`). ROADMAP.md's progress table updates via a manual edit or the `update-phase-total.sh` script — STATE.md is the live source, ROADMAP.md is the stable roadmap document.
- Phase 2 (M2 Single-agent path) can begin: per TDD2§13.2, PR-12 (Lead role through dispatcher) is the next PR.
- Next user action: invoke `/vbw:vibe` from the SWT working repo (or here for further planning) to start M2.

**The TDD2.md sign-off checklist (end of document) can now be filled in for M1's scope.** Roles to check:
- Tech lead — M1 architectural debt discharged
- Methodology owner — methodology preserved verbatim
- Runtime owner — runtime adapter exists with peerDep
- Orchestration owner — orchestration scaffold exists
- Dashboard owner — no dashboard changes in M1 (M2 territory)
- CLI owner — CLI rewired through abstractions
- Test/QA owner — cassette infra + token-meter assertion in place
- Release engineering — reproducible-build job live
</success_criteria>

<output>
01-03-SUMMARY.md
</output>
