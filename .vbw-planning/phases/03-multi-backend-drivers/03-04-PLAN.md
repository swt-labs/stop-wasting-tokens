---
phase: 03
plan: 03-04
title: Ollama sandbox-mode wrapping — process-level isolation per spec.sandbox_mode
type: execute
wave: 2
depends_on: ["03-03"]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - packages/ollama-driver/src/sandbox/wrapper.ts
  - packages/ollama-driver/src/sandbox/index.ts
  - packages/ollama-driver/src/spawner/ollama-agent-spawner.ts
  - packages/ollama-driver/src/index.ts
  - packages/ollama-driver/test/sandbox/wrapper.test.ts
forbidden_commands: []
must_haves:
  truths:
    - "OllamaAgentSpawner honors the spec.sandbox_mode field by wrapping the request prompt with a sandbox-appropriate guard preamble before sending to Ollama"
    - "sandbox_mode='read-only': preamble instructs the model to refuse any tool that would mutate the filesystem; allowed_mcp_servers (tools) stay limited to read-only; the SWT-side PermissionGate enforces this at the boundary if available"
    - "sandbox_mode='workspace-write': preamble instructs the model that mutations are scoped to request.cwd and below; PermissionGate (if present) checks tool args against this root"
    - "sandbox_mode='danger-full-access': preamble notes no sandbox; passes through to the model unchanged"
    - "applySandboxToPrompt(prompt, mode, cwd) is a pure function — same input + mode + cwd always produces the same wrapped prompt; trivially testable"
    - "Ollama lacks a native sandbox primitive (it's a model server, not a tool runner) so sandbox enforcement is process-level on the SWT side via PermissionGate; this plan documents that boundary explicitly"
  artifacts:
    - path: "packages/ollama-driver/src/sandbox/wrapper.ts"
      provides: "applySandboxToPrompt(prompt, mode, cwd) pure function + sandbox preamble templates"
      contains: "applySandboxToPrompt"
    - path: "packages/ollama-driver/test/sandbox/wrapper.test.ts"
      provides: "vitest coverage of all 3 sandbox modes + edge cases"
      contains: "describe('applySandboxToPrompt'"
  key_links:
    - from: "packages/ollama-driver/src/spawner/ollama-agent-spawner.ts"
      to: "packages/ollama-driver/src/sandbox/wrapper.ts"
      via: "applySandboxToPrompt import"
---
<objective>
Honor `spec.sandbox_mode` in the Ollama driver. Ollama itself doesn't have a native sandbox concept (no `--sandbox` flag like Codex), so v1.5's approach is twofold: (1) wrap the prompt with a sandbox-appropriate preamble that instructs the model about its expected behavior, and (2) document that real enforcement happens at the SWT-side PermissionGate boundary (the v1.0 ship has the PermissionGate contract; the actual enforcement implementation is a v2 concern). This plan delivers (1) and the documentation marker for (2). Closes F3's "Sandbox modes degrade gracefully — driver wraps process-level isolation" success criterion.
</objective>
<context>
**Sandbox-mode preamble templates** (one per mode, prepended to the system message):

- `read-only`:
  ```
  SANDBOX MODE: read-only
  You may read files and run inspection commands but MUST NOT mutate the filesystem.
  Refuse any tool call that would write, delete, or rename files. Reply with a textual
  explanation of what you would have done instead. Working directory: <cwd>.
  ```

- `workspace-write`:
  ```
  SANDBOX MODE: workspace-write
  You may read and write files within the working directory <cwd> and its subtree.
  Refuse any tool call that targets paths outside this subtree. Network access is allowed.
  ```

- `danger-full-access`:
  ```
  SANDBOX MODE: danger-full-access
  No sandbox. You may read and write any path the running process can access. Use caution.
  Working directory: <cwd>.
  ```

**Where the preamble lands:** prepended to the system message ahead of the agent's `developer_instructions`. Order: `[SANDBOX_PREAMBLE, "\n\n", developer_instructions]`. The user prompt is unchanged.

**Why this is "process-level"** (the F3 success criterion's language): Ollama has no kernel-level sandbox to invoke. The "process-level" framing means SWT keeps the host process's capability footprint stable across modes — the preamble is the model-facing contract, and the SWT-side PermissionGate (when present) is the enforcement contract. Plan 03-04 lands the preamble half cleanly; the PermissionGate enforcement half is tracked as a v2 follow-up referenced in 03-04-SUMMARY's deviations.

**Default behavior when sandbox_mode is undefined:** treat as `workspace-write` (the SWT default per `packages/core/src/abstractions/PermissionGate.ts` PermissionProfile shape).
</context>
<tasks>
<task type="auto">
  <name>T1: applySandboxToPrompt + preamble templates</name>
  <files>
    packages/ollama-driver/src/sandbox/wrapper.ts
    packages/ollama-driver/src/sandbox/index.ts
    packages/ollama-driver/src/index.ts
  </files>
  <action>
Create `packages/ollama-driver/src/sandbox/wrapper.ts`:
- Export `SANDBOX_PREAMBLES: Readonly<Record<SandboxMode, (cwd: string) => string>>` with the three template functions
- Export `applySandboxToPrompt(systemPrompt: string, mode: SandboxMode | undefined, cwd: string): string` — returns `${preamble(cwd)}\n\n${systemPrompt}`. Defaults `mode` to `'workspace-write'` when undefined.

Create `packages/ollama-driver/src/sandbox/index.ts` barrel.

Append `export * from './sandbox/index.js'` to `packages/ollama-driver/src/index.ts`.
  </action>
  <verify>
Type-check clean.
  </verify>
  <done>
applySandboxToPrompt is exported as a pure function; SANDBOX_PREAMBLES is the source of truth for the three preambles.
  </done>
</task>
<task type="auto">
  <name>T2: Wire applySandboxToPrompt into OllamaAgentSpawner</name>
  <files>
    packages/ollama-driver/src/spawner/ollama-agent-spawner.ts
  </files>
  <action>
In `OllamaAgentSpawner.spawn(request)`:
- Resolve the effective spec: `effective = installed.get(request.spec.role) ?? request.spec`
- Compute the wrapped system prompt: `applySandboxToPrompt(effective.developer_instructions, effective.sandbox_mode, request.cwd)`
- Pass the wrapped prompt into spawnOllama via a new SpawnFlags field (e.g., `system_prompt_override`) that wrapper.ts then uses instead of effective.developer_instructions when building the chat request body
  </action>
  <verify>
Type-check clean; existing OllamaAgentSpawner tests from Plan 03-03 still pass (the wrapping is additive — text-only fixtures + handoff fixtures don't assert against the system prompt content, so the existing 5+3+5 cases remain green).
  </verify>
  <done>
Sandbox preamble is applied in the spawn path; an existing test that calls spawn with sandbox_mode='read-only' would observe the preamble in the spy on fetch's request body.
  </done>
</task>
<task type="auto">
  <name>T3: Sandbox-mode unit tests</name>
  <files>
    packages/ollama-driver/test/sandbox/wrapper.test.ts
  </files>
  <action>
Vitest cases (5):
1. `applySandboxToPrompt(p, 'read-only', cwd)` returns a string containing "SANDBOX MODE: read-only" + the original prompt
2. `applySandboxToPrompt(p, 'workspace-write', cwd)` references the cwd in the preamble
3. `applySandboxToPrompt(p, 'danger-full-access', cwd)` notes no sandbox + still includes the cwd marker
4. `applySandboxToPrompt(p, undefined, cwd)` defaults to `workspace-write` semantics (asserts the workspace-write preamble shape)
5. Pure function: same inputs always produce the same output (deterministic)
  </action>
  <verify>
`pnpm vitest run packages/ollama-driver/test/sandbox/wrapper.test.ts` — 5/5 pass.
  </verify>
  <done>
Sandbox preamble behavior is covered for all 3 modes + the undefined fallback + determinism property.
  </done>
</task>
</tasks>
<verification>
1. `pnpm --filter @swt-labs/ollama-driver typecheck` exits 0
2. `pnpm vitest run packages/ollama-driver` — Plan 03-03 tests (13) + Plan 03-04 tests (5) all pass
3. A spy on the spawn fetch's request body for a `read-only` sandbox_mode role observes the preamble at the head of the system message content
</verification>
<success_criteria>
- F3's "Sandbox modes degrade gracefully — driver wraps process-level isolation" success criterion is met (preamble half is delivered; enforcement-half is tracked as a v2 follow-up via SUMMARY deviation)
- The three preamble templates are documented + tested for shape
- The Ollama driver respects sandbox_mode without crashing or skipping the field
</success_criteria>
<output>
03-04-SUMMARY.md
</output>
