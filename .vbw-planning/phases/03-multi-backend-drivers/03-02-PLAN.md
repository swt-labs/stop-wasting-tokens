---
phase: 03
plan: 03-02
title: ClaudeCodeHookHost — 12-event taxonomy mapping to SWT's 6 generic events
type: execute
wave: 2
depends_on: ["03-01"]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - packages/claude-code-driver/src/hooks/host.ts
  - packages/claude-code-driver/src/hooks/index.ts
  - packages/claude-code-driver/src/index.ts
  - packages/claude-code-driver/test/hooks/host.test.ts
forbidden_commands: []
must_haves:
  truths:
    - "@swt-labs/claude-code-driver exports a ClaudeCodeHookHost class implementing the HookHost contract from @swt-labs/core"
    - "the 12 Claude Code lifecycle events are documented as a typed enum and mapped onto SWT's 6 generic events (session_start, user_prompt_submit, pre_tool_use, post_tool_use, permission_request, stop)"
    - "events Claude Code emits but SWT doesn't model are observable via the underlying CC stream (when --include-hook-events is set) but do not fire SWT-side handlers"
    - "registering a handler via subscribe(event, handler) returns a HookSubscription whose unsubscribe() detaches idempotently"
    - "dispatch(context) routes to all subscribed handlers for the matching SWT event; aggregated outcome follows block-precedes-allow semantics"
  artifacts:
    - path: "packages/claude-code-driver/src/hooks/host.ts"
      provides: "ClaudeCodeHookHost class + the CC_TO_SWT_EVENT_MAP table"
      contains: "class ClaudeCodeHookHost implements HookHost"
    - path: "packages/claude-code-driver/test/hooks/host.test.ts"
      provides: "vitest coverage of mapping table + subscribe/unsubscribe + dispatch precedence"
      contains: "describe('ClaudeCodeHookHost'"
  key_links:
    - from: "packages/claude-code-driver/src/hooks/host.ts"
      to: "packages/core/src/abstractions/HookHost.ts"
      via: "implements HookHost"
---
<objective>
Implement ClaudeCodeHookHost so the same SWT-side hook subscriptions registered against the Codex backend continue to fire when the active backend is Claude Code. This closes F2's "Hook event taxonomy in Claude Code driver covers the 12 events; SWT's 6 generic events map to a subset" success criterion.
</objective>
<context>
**Claude Code lifecycle events (12 total, per `--include-hook-events` documentation):**
1. `SessionStart`
2. `SessionEnd`
3. `UserPromptSubmit`
4. `PreToolUse`
5. `PostToolUse`
6. `Notification`
7. `Stop`
8. `SubagentStop`
9. `PreCompact`
10. `PreCompactRequest`
11. `Notification` (variant — duplicate above; keep distinct in the enum if CC's payload differentiates)
12. (Reserved for marketplace plugin events; treat as observable but unmapped for v1.5)

**SWT's 6 generic events (`packages/core/src/abstractions/HookHost.ts`):**
- `session_start`
- `user_prompt_submit`
- `pre_tool_use`
- `post_tool_use`
- `permission_request`
- `stop`

**Mapping (CC_TO_SWT_EVENT_MAP):**
| Claude Code event | SWT event | Notes |
|---|---|---|
| SessionStart | session_start | direct |
| UserPromptSubmit | user_prompt_submit | direct |
| PreToolUse | pre_tool_use | direct |
| PostToolUse | post_tool_use | direct |
| Stop | stop | direct |
| SessionEnd | stop | merged — both terminate the session |
| SubagentStop | stop | merged — subagent completion terminates that subagent |
| Notification | (none) | observable only; no SWT handler fires |
| PreCompact / PreCompactRequest | (none) | observable only |
| (CC permission flow) | permission_request | derived — synthesized when CC emits a permission decision request envelope; not a direct event mapping |

The host does NOT fork a child process to listen for events; Plan 03-01's spawn already runs `claude --print --include-hook-events`, and parsed hook envelopes from the stream get routed to the host's `dispatch()` method.

**block-precedes-allow semantics:** if any subscribed handler returns `{decision: 'block'}`, the aggregated outcome is `block` with the first reason. Otherwise if any returns `{decision: 'observe'}`, outcome is `observe`. Otherwise `allow`.
</context>
<tasks>
<task type="auto">
  <name>T1: ClaudeCodeHookHost class + CC_TO_SWT_EVENT_MAP</name>
  <files>
    packages/claude-code-driver/src/hooks/host.ts
    packages/claude-code-driver/src/hooks/index.ts
  </files>
  <action>
Create `packages/claude-code-driver/src/hooks/host.ts`:
- export `type ClaudeCodeHookEvent` (12-variant union)
- export `CC_TO_SWT_EVENT_MAP: Readonly<Partial<Record<ClaudeCodeHookEvent, HookEvent>>>` covering the direct + merged mappings; events with no SWT mapping are absent from the map
- export `class ClaudeCodeHookHost implements HookHost`:
  - private `subscriptions: Map<HookEvent, Set<HookHandler>>`
  - `subscribe(event, handler): HookSubscription` returns `{unsubscribe}` that removes the handler from the set
  - `dispatch(context): Promise<HookOutcome>` invokes all handlers for `context.event` and aggregates per block-precedes-allow rules
  - `routeFromClaudeCode(ccEvent, payload, sessionId, cwd): Promise<HookOutcome | undefined>` — translates CC events to SWT context + dispatches; returns undefined when CC event has no SWT mapping

Create `packages/claude-code-driver/src/hooks/index.ts` barrel exporting host module.

Append `export * from './hooks/index.js'` to `packages/claude-code-driver/src/index.ts`.
  </action>
  <verify>
Type-check clean.
  </verify>
  <done>
HookHost contract satisfied; mapping table is the source of truth for the CC→SWT translation.
  </done>
</task>
<task type="auto">
  <name>T2: ClaudeCodeHookHost tests</name>
  <files>
    packages/claude-code-driver/test/hooks/host.test.ts
  </files>
  <action>
Vitest cases:
1. `subscribe → dispatch` calls the registered handler with the right context shape
2. `unsubscribe` removes the handler (subsequent dispatch does not call it; idempotent on re-call)
3. `block-precedes-allow` aggregation: 2 handlers, one allow + one block → outcome is block with the block's reason
4. `observe-fallthrough`: 2 handlers, one observe + one allow → outcome is observe
5. `routeFromClaudeCode` for a direct mapping (`PreToolUse` → `pre_tool_use`) dispatches; returned HookOutcome matches handler's
6. `routeFromClaudeCode` for an unmapped event (`Notification`) returns undefined and does NOT call any subscribed handler
7. `routeFromClaudeCode` for a merged mapping (`SessionEnd` → `stop`) reaches handlers subscribed to `stop`
  </action>
  <verify>
`pnpm vitest run packages/claude-code-driver/test/hooks/host.test.ts` — 7/7 pass.
  </verify>
  <done>
Hook event taxonomy is structurally validated.
  </done>
</task>
</tasks>
<verification>
1. `pnpm --filter @swt-labs/claude-code-driver typecheck` exits 0
2. 7/7 hook tests pass
3. `CC_TO_SWT_EVENT_MAP` is exported and treated as the authoritative table — Plan 05 (F7 hook taxonomy expansion) will extend the SWT-side enum, not duplicate this map
</verification>
<success_criteria>
- F2's "Hook event taxonomy in Claude Code driver covers the 12 events; SWT's 6 generic events map to a subset" success criterion is met
- The same SWT-side hook subscription pattern works against Claude Code as against Codex (Codex's HookHost lands in Plan 05 if not already present; v1.0 ship has the contract but no Codex implementation)
- Block-precedes-allow aggregation matches the documented HookHost semantics
</success_criteria>
<output>
03-02-SUMMARY.md
</output>
