---
phase: 2
plan: 02
title: Dashboard SSE + Cassette Regression + TPAC Baseline + `swt rpc` + `swt bench` (PR-17 → PR-21)
type: execute
wave: 2
depends_on: ['02-01']
cross_phase_deps: ['01-01', '01-02', '01-03']
autonomous: false
effort_override: balanced
skills_used: []
files_modified:
  - packages/dashboard/src/server/event-bus.ts
  - packages/dashboard/src/server/snapshot/
  - packages/dashboard/src/server/routes/sse.ts
  - packages/dashboard/src/server/markdown/
  - packages/dashboard/src/server/lib/
  - packages/dashboard/src/server/lib/tail-file.ts
  - packages/dashboard/src/server/snapshot/events-tailer.ts
  - packages/dashboard/src/server/snapshot/snapshotter.ts
  - packages/dashboard/test/snapshot-reducer.test.ts
  - packages/dashboard/src/client/services/sse.ts
  - packages/dashboard/src/client/components/LogPanel.tsx
  - packages/dashboard/src/client/components/AgentTimeline.tsx
  - packages/dashboard/src/client/components/CostPanel.tsx
  - packages/dashboard/test/sse-snapshot-changed.test.ts
  - packages/dashboard/test/sse-reconnect.test.ts
  - packages/dashboard/test/events-tailer.test.ts
  - packages/dashboard/test/log-rate-limit.test.ts
  - packages/dashboard/test/server.test.ts
  - packages/dashboard/test/artifact-route.test.ts
  - packages/dashboard/test/snapshot-reducer.test.ts
  - packages/test-utils/golden/                          (NEW dir — ref-fastapi v2-baseline)
  - packages/test-utils/golden/ref-fastapi/spec/
  - packages/test-utils/golden/ref-fastapi/v2-baseline/
  - packages/test-utils/golden/ref-fastapi/cassettes/
  - packages/test-utils/src/diff-artefacts.ts            (NEW)
  - packages/test-utils/src/run-milestone.ts             (NEW)
  - packages/test-utils/src/index.ts                     (export run-milestone + diff-artefacts)
  - test/regression/ref-fastapi.regression.test.ts      (NEW per TDD2 §14.6)
  - test/regression/diff-artefacts.test.ts              (NEW — unit-test the allowed-drift comparator)
  - scripts/stub-test-regression.mjs                    (replace stub with real runner once cassettes recorded)
  - packages/cli/src/commands/rpc.ts                    (NEW — swt rpc verb)
  - packages/cli/src/commands/bench.ts                  (NEW — swt bench verb)
  - packages/cli/src/router.ts                          (route rpc + bench verbs)
  - packages/cli/src/commands/index.ts                  (export new verbs)
  - packages/cli/test/commands/rpc.test.ts              (NEW)
  - packages/cli/test/commands/bench.test.ts            (NEW)
  - packages/runtime/src/rpc-runner.ts                  (NEW — delegate to Pi runRpcMode)
  - packages/runtime/src/index.ts                       (export rpc-runner)
  - packages/orchestration/src/tpac-meter.ts            (NEW — aggregator on top of runtime meter, milestone-scoped)
  - packages/orchestration/test/tpac-meter.test.ts      (NEW)
  - packages/shared/src/schemas/tpac-report.ts          (NEW)
  - packages/shared/src/schemas/index.ts                (export tpac-report)
  - docs/decisions/test-debt-tracking.md
  - docs/operations/cassette-recording.md
  - docs/cli/README.md
  - docs/cli/verbs/rpc.md                               (NEW per-verb doc)
  - docs/cli/verbs/bench.md                             (NEW per-verb doc)
  - docs/operations/observability.md                    (expand TPAC + cache-hit-ratio sections)
  - CHANGELOG.md
  - .vbw-planning/v3-tracking.md
  - .github/workflows/regression.yml                    (replace stub with real `pnpm test:regression`)
  - README.md
  - CHANGELOG.md
forbidden_commands:
  - 'git push --force'
  - 'rm -rf packages/test-utils/golden/'
must_haves:
  truths:
    - "Dashboard SSE consumes Pi events through `@swt-labs/runtime`'s `mapPiEvent` + `TASK_TOKEN_USAGE`; the v2 codex-driver event path is gone."
    - 'All 10 v2.3.5-carry-forward dashboard test failures (LogPanel TS2322 + 9 others) are unskipped and pass (umbrella issue #32 dashboard cluster closes).'
    - '`packages/test-utils/golden/ref-fastapi/` contains the v2.3.5 baseline `.swt-planning/` + the recorded cassettes; the regression suite at `test/regression/ref-fastapi.regression.test.ts` replays end-to-end on v3 and passes the diff against the v2 golden (modulo allowed drift).'
    - "First TPAC measurement against the `ref-fastapi-empty` reference scenario is recorded in `.vbw-planning/v3-tracking.md`'s Metrics table as the **fixed M2 baseline** for M4's −40% target."
    - "`swt rpc` verb delegates to Pi's `runRpcMode` without protocol modification (per TDD2 §3.2 + §5)."
    - '`swt bench --fixture=ref-fastapi-empty` runs the M2 reference scenario, harvests the TPAC measurement, and prints the report; the output is identical to what `test/regression/ref-fastapi.regression.test.ts` consumes.'
    - '`.github/workflows/regression.yml` Test step calls `pnpm test:regression` (no longer the stub) and the workflow runs on PR + push-to-{main,v3-foundation}.'
    - '`scripts/stub-test-regression.mjs` is replaced by a real runner that invokes vitest against the `test/regression/` directory; the stub-message pointer (to M2 PR-18) is removed.'
    - "TPAC report Zod schema (`@swt-labs/shared/schemas/tpac-report.ts`) ships and is validated at both `swt bench`'s emit boundary and `test/regression`'s consume boundary."
    - '`packages/runtime/src/extensions/cassette-replay.int.test.ts` (deferred from Plan 01-02 PR-07) activates if the user-driven `scout-read-readme.jsonl` recording session has landed; otherwise stays `skipIf(!HAS_CASSETTE)` (the activation is orthogonal to this plan).'
  artifacts:
    - {
        path: 'packages/test-utils/golden/ref-fastapi/v2-baseline/',
        provides: 'v2.3.5 golden run',
        contains: '.swt-planning',
      }
    - {
        path: 'packages/test-utils/golden/ref-fastapi/cassettes/',
        provides: 'recorded session cassettes for replay',
        contains: 'scout-noop.jsonl',
      }
    - {
        path: 'test/regression/ref-fastapi.regression.test.ts',
        provides: 'M2 regression baseline test',
        contains: 'runMilestone',
      }
    - {
        path: 'packages/test-utils/src/run-milestone.ts',
        provides: 'test harness for milestone-level replay',
        contains: 'export function runMilestone',
      }
    - {
        path: 'packages/test-utils/src/diff-artefacts.ts',
        provides: 'allowed-drift diff',
        contains: 'diffArtefacts',
      }
    - { path: 'packages/cli/src/commands/rpc.ts', provides: 'swt rpc verb', contains: 'runRpcMode' }
    - { path: 'packages/cli/src/commands/bench.ts', provides: 'swt bench verb', contains: 'TPAC' }
    - {
        path: 'packages/orchestration/src/tpac-meter.ts',
        provides: 'milestone-scoped TPAC aggregator',
        contains: 'computeTpac',
      }
    - {
        path: 'packages/shared/src/schemas/tpac-report.ts',
        provides: 'TPAC report schema',
        contains: 'TpacReportSchema',
      }
  key_links:
    - {
        from: 'test/regression/ref-fastapi.regression.test.ts',
        to: 'packages/test-utils/golden/ref-fastapi/',
        via: 'runMilestone fixture',
      }
    - {
        from: 'packages/cli/src/commands/bench.ts',
        to: 'packages/orchestration/src/tpac-meter.ts',
        via: 'computeTpac call',
      }
    - {
        from: 'packages/cli/src/commands/rpc.ts',
        to: 'packages/runtime/src/rpc-runner.ts',
        via: 'delegate to Pi runRpcMode',
      }
    - {
        from: '.github/workflows/regression.yml',
        to: 'pnpm test:regression',
        via: 'real runner replaces stub',
      }
---

<objective>
M2's second plan finishes the milestone: dashboard SSE rewires to consume Pi events (closing 10 dashboard test-debt items from umbrella #32), the v2.3.5 golden-run regression suite goes live, the M2 TPAC baseline is measured against the `ref-fastapi-empty` reference scenario per TDD2 §13.2.3, and two new CLI verbs (`swt rpc` + `swt bench`) ship per TDD2 §3.2.

The TPAC baseline is the **single hard merge gate** for M2's exit per TDD2 §13.2.3 — once recorded, M4's −40% target has a fixed point to compare against. The regression suite is the safety net for the methodology-preservation claim (§11) — if v3 silently drifts from v2 behaviour, regression fails.

This plan covers the final 5 of M2's 10 PRs (PR-17..PR-21). After it ships: M2 exit gate is reached per TDD2 §13.2.3. M3 Worktree dispatcher is ready to begin.
</objective>

<context>
@.vbw-planning/phases/02-m2-single-agent-path/02-01-PLAN.md
@TDD2.md  # §8 (token meter + TPAC), §12 (dashboard panels), §13.2 (M2 deliverables), §14.6 (regression baseline), §14.7 (cassette infra), §14.9 (TPAC measurement protocol)
@.vbw-planning/v3-tracking.md
@docs/decisions/test-debt-tracking.md  # umbrella #32 — dashboard cluster details

## State at plan start (post-Plan-02-01 baseline)

What's on disk when this plan begins:

- **Methodology layer is rewired through orchestration** (Plan 02-01 PR-12..PR-15). `swt vibe` runs end-to-end on Anthropic via Pi. Plan/Architect/Dev/QA all dispatch through `@swt-labs/orchestration`'s `createDispatcher`.
- **6 role profiles ship at `packages/methodology/src/profiles/<role>.{ts,prompt.md}`** with per-role tier + tool-subset + thinking-level + session-mode declarations per TDD2 §10.
- **`UiPermissionGate` lands** (Plan 02-01 PR-16) as a sibling to `DashboardPermissionGate`; UI-button POSTs route through it. Dashboard SSE rewire (this plan's PR-17) closes the broader 10-test dashboard cluster from umbrella #32.
- **`@swt-labs/runtime`'s token meter is wired into the methodology layer** (via Plan 01-02 PR-07's `routeUsageToMeter`). The aggregator in this plan's `orchestration/src/tpac-meter.ts` sums the per-task records into milestone totals for the TPAC report.
- **Cassette infrastructure is fully scaffolded** (Plan 01-02 PR-06's `@swt-labs/test-utils`); the regression suite in PR-18 will consume freshly-recorded cassettes for the `ref-fastapi-empty` scenario. The 2 cassette-deferred tests from Plan 01-02 (PR-07 byte-identical + PR-09 end-to-end) remain orthogonal to this plan — they activate when the cassettes are recorded; this plan records the ref-fastapi cassettes via the same recorder.
- **`stub-test-regression.mjs` exits 0 with a pointer to "M2 PR-18"** (Plan 01-03 PR-11 Task B). This plan replaces the stub with the real runner.
- **TPAC has no measurement yet.** The `.vbw-planning/v3-tracking.md` Metrics table has the M2-baseline row marked `TBD`. This plan fills it in.

What's known-deferred forward (so this plan doesn't re-litigate the decisions):

- M4 PR-32..PR-36 will introduce the cache-control breakpoint placement (ADR-006) and the Budget Gate (ADR-007). The TPAC measurement this plan records is the **without-cache-control** baseline — explicit per TDD2 §13.2.3.
- M3 worktree-per-task isolation (ADR-008) and parallel batching are M3 territory. M2 measures TPAC in the sequential-no-worktree mode.
- The 2 Plan 01-02 cassette-deferred tests stay orthogonal. They activate when the user records `scout-read-readme.jsonl` + `scout-search-codebase.jsonl` — independent of this plan's `ref-fastapi-empty` recording (different scenarios, different cassette files).
- ADR-011 (provider matrix via cassettes only) was Proposed at Plan 01-02 PR-06; it promotes to Accepted at M5 PR-44 — NOT this plan. This plan's regression suite uses the Anthropic provider only; the multi-provider matrix lives in `provider-matrix.yml` (stubbed at Plan 01-03 PR-11 Task B).
  </context>

<tasks>

<task type="auto">
  <name>PR-17 · Dashboard SSE rewire — consume Pi events through runtime + close 10 dashboard test-debt items</name>
  <files>
    packages/dashboard/src/server/event-bus.ts
    packages/dashboard/src/server/snapshot/
    packages/dashboard/src/server/routes/sse.ts
    packages/dashboard/src/server/markdown/
    packages/dashboard/src/client/services/sse.ts
    packages/dashboard/src/client/components/LogPanel.tsx
    packages/dashboard/src/client/components/AgentTimeline.tsx
    packages/dashboard/src/client/components/CostPanel.tsx
    packages/dashboard/test/sse-snapshot-changed.test.ts
    packages/dashboard/test/sse-reconnect.test.ts
    packages/dashboard/test/events-tailer.test.ts
    packages/dashboard/test/log-rate-limit.test.ts
    packages/dashboard/test/server.test.ts
    packages/dashboard/test/artifact-route.test.ts
    packages/dashboard/test/snapshot-reducer.test.ts
  </files>
  <action>
1. The dashboard's SSE event bus currently consumes a v2-shaped event format. v3 needs to consume `@swt-labs/runtime`'s `SwtEvent` discriminated union (`AGENT_START`, `AGENT_END`, `MESSAGE_DELTA`, `TOOL_CALL`, `TOOL_RESULT`, `TASK_TOKEN_USAGE`) per Plan 01-02 PR-07.

2. Rewire `packages/dashboard/src/server/event-bus.ts`:
   - Subscribe to the runtime's session event stream.
   - Map `SwtEvent` to dashboard SSE messages per TDD2 §12 panels (`AgentTimeline` consumes `AGENT_START/END` + `TOOL_CALL/RESULT`; `LogPanel` consumes `MESSAGE_DELTA`; `CostPanel` consumes `TASK_TOKEN_USAGE`).
   - Preserve the existing snapshot-reducer shape so the dashboard React/Solid components don't need full rewires (just the event mapping changes).

3. Fix `packages/dashboard/src/client/components/LogPanel.tsx(78,9)` TS2322 — the pre-existing v2.3.5 carry-forward (Type 'void' is not assignable to type 'string | Element'). The fix is structural: ensure the JSX expression at line 78 always returns a renderable value (return `<></>` or a string in the void path).

4. Unskip all 10 dashboard test files under umbrella issue #32:
   - `sse-snapshot-changed.test.ts` (2 tests)
   - `sse-reconnect.test.ts` (1 test — requires reinstating the `@vitest-environment jsdom` directive; install jsdom as a devDep for `@swt-labs/dashboard` if needed)
   - `events-tailer.test.ts` (4 tests)
   - `log-rate-limit.test.ts` (2 tests)
   - `server.test.ts` (1 test — `refuses 0.0.0.0 without allowPublic`)
   - `artifact-route.test.ts` (1 test — `GET /api/artifact rendered HTML+frontmatter when render=html`)
   - `snapshot-reducer.test.ts`
     Remove `describe.skip(` → `describe(` and the 3-line `// TODO(v3-debt): tracking #32` header from each file. Adjust assertions where they reference the v2-shaped events.

5. Reinstate the `@vitest-environment jsdom` directive in `sse-reconnect.test.ts` (the directive was removed at Plan 01-03 PR-11 Task A to unblock the stub-script CI gate). Install jsdom into `packages/dashboard/package.json` devDependencies.

6. Update `docs/decisions/test-debt-tracking.md`:
   - Mark the dashboard cluster row as resolved with this commit's SHA.
   - The cluster of 10 dashboard tests closes; umbrella issue #32 now carries only the methodology + verification clusters (the latter already closed by Plan 02-01 PR-14).
     </action>
     <verify>
7. `pnpm test --filter @swt-labs/dashboard` green; all 10 previously-skipped tests pass.
8. `pnpm -r typecheck` clean (the LogPanel.tsx fix removes the per-package TS2322).
9. `pnpm typecheck` (root) clean.
10. `grep -rE "describe.skip" packages/dashboard/test/` returns nothing.
11. The dashboard renders correctly against a real `swt vibe` session (manual smoke test — not a CI gate, but spot-checked).
    </verify>
    <done>
    Dashboard SSE consumes Pi events via runtime; LogPanel TS2322 fixed; 10 dashboard test-debt items cleared. Umbrella issue #32 progress: methodology + verification + dashboard clusters all closed; only the CLI publishConfig + config-doc-drift clusters remain (those resolve at M6 release prep).
    </done>
    </task>

<task type="auto">
  <name>PR-18 · Cassette regression suite — v2 golden run replays byte-identical (modulo allowed drift)</name>
  <files>
    packages/test-utils/golden/ref-fastapi/spec/        (frozen spec — TDD2 §13.2.2)
    packages/test-utils/golden/ref-fastapi/v2-baseline/  (v2.3.5 recorded .swt-planning/)
    packages/test-utils/golden/ref-fastapi/cassettes/    (recorded Anthropic interactions)
    packages/test-utils/src/run-milestone.ts             (NEW — test harness)
    packages/test-utils/src/diff-artefacts.ts            (NEW — allowed-drift diff)
    packages/test-utils/src/index.ts                     (exports)
    test/regression/ref-fastapi.regression.test.ts       (NEW per TDD2 §14.6)
    test/regression/diff-artefacts.test.ts               (NEW — unit-test the comparator)
    scripts/stub-test-regression.mjs                     (REPLACE — real runner invokes vitest)
    .github/workflows/regression.yml                     (Test step calls `pnpm test:regression` not stub)
    docs/operations/cassette-recording.md                (extend with ref-fastapi recording workflow)
  </files>
  <action>
1. **Freeze the `ref-fastapi-empty` reference fixture** at `packages/test-utils/golden/ref-fastapi/spec/`:
   - Spec per TDD2 §13.2.2: 1 health endpoint, 1 echo endpoint, pytest tests, Dockerfile.
   - The fixture spec is the input — `PROJECT.md` + `REQUIREMENTS.md` + an empty `phases/` directory.
   - Any change to the spec breaks the regression chain and requires an ADR. Document this in `packages/test-utils/golden/ref-fastapi/README.md`.

2. **Record the v2.3.5 golden run** at `packages/test-utils/golden/ref-fastapi/v2-baseline/`:
   - Run `stop-wasting-tokens@2.3.5` end-to-end against the frozen spec on a developer-local machine.
   - Commit the resulting `.swt-planning/` tree (after a full milestone) as the v2 baseline.
   - This is a **one-time** recording step. Subsequent v2.x patches don't refresh it (the comparison is fixed to v2.3.5).

3. **Record the Anthropic cassettes** at `packages/test-utils/golden/ref-fastapi/cassettes/`:
   - Use the recorder from Plan 01-02 PR-06.
   - One cassette per role dispatched during the milestone (Scout → Architect → Dev × N → QA).
   - User-driven session (requires Anthropic API key); developer-local; not in CI.
   - This cassette set is **separate** from the 2 Plan 01-02 cassette-deferred tests — they record `scout-read-readme.jsonl` + `scout-search-codebase.jsonl` (different scenarios).

4. Implement `packages/test-utils/src/run-milestone.ts`:

   ```ts
   export async function runMilestone(opts: {
     fixture: string;
     cassettes: string;
   }): Promise<RunResult> {
     // 1. Copy `packages/test-utils/golden/<fixture>/spec/` to a tmpdir
     // 2. installReplay(cassettes/*.jsonl) so all Pi HTTP calls are deterministic
     // 3. Invoke `swt vibe` (or its underlying methodology entry point) against the tmpdir
     // 4. Return the resulting artefactsPath + the meter snapshot
   }
   ```

5. Implement `packages/test-utils/src/diff-artefacts.ts` per TDD2 §14.6's allowed-drift spec:
   - `STATE.md`: timestamps in activity log allowed to differ; phase summary text fingerprint must match within Levenshtein distance ≤ 100 chars.
   - `phases/NN-slug/plan-NN.md`: task-ID prefixes can differ; task content fingerprint must match.
   - `phases/NN-slug/qa-NN.md`: timestamps allowed; counts must match exactly.
   - Generated artefacts (`scout-briefs/`, `debug-reports/`): semantic fingerprint match.
   - Returns `{ violations: string[] }` — empty = clean diff.

6. Implement `test/regression/ref-fastapi.regression.test.ts`:
   - The exact test in TDD2 §14.6 verbatim.
   - Uses `runMilestone` + `diffArtefacts` from `@swt-labs/test-utils`.
   - Should pass deterministically on every CI run.

7. Implement `test/regression/diff-artefacts.test.ts`:
   - Unit-test the comparator: feed it synthetic fixtures with known-bad drift (timestamp + Levenshtein-too-large summary) and assert violations.
   - Feed it identical fixtures and assert no violations.

8. Replace `scripts/stub-test-regression.mjs`:
   - Currently: `console.log('Regression suite stub. Real implementation lands in M2 PR-18.'); process.exit(0);`
   - New: invoke vitest against the `test/regression/` directory.
   - The script can stay an `.mjs` wrapper that calls `vitest run test/regression/` or be replaced with a direct script entry in `package.json#scripts`.

9. Update `.github/workflows/regression.yml`:
   - Remove the `|| echo "Regression suite not yet implemented (lands in M2 PR-18)"` fallback.
   - The `pnpm test:regression` step now fails the workflow on any regression diff.

10. Document the recording workflow in `docs/operations/cassette-recording.md` — add a section "Recording the ref-fastapi-empty cassettes for the M2 regression baseline" with the exact commands.
    </action>
    <verify>
11. `pnpm test:regression` runs `test/regression/ref-fastapi.regression.test.ts` + `diff-artefacts.test.ts`.
12. Both regression tests pass; the diff returns no violations against the v2.3.5 golden.
13. `cat scripts/stub-test-regression.mjs` no longer contains the "stub" string.
14. `grep -n "Regression suite not yet implemented" .github/workflows/regression.yml` returns nothing.
15. `ls packages/test-utils/golden/ref-fastapi/` shows `spec/`, `v2-baseline/`, `cassettes/`, `README.md`.
16. The regression-workflow CI run goes green on push.
    </verify>
    <done>
    v2.3.5 golden run replays byte-identical (modulo allowed drift) on v3. Regression suite is the safety net for the methodology-preservation claim per TDD2 §11. CI workflow `regression.yml` is no longer a stub.
    </done>
    </task>

<task type="auto">
  <name>PR-19 · First TPAC measurement on `ref-fastapi-empty` — M2 baseline recorded</name>
  <files>
    packages/orchestration/src/tpac-meter.ts             (NEW — milestone-scoped aggregator)
    packages/orchestration/test/tpac-meter.test.ts       (NEW)
    packages/shared/src/schemas/tpac-report.ts           (NEW — TpacReportSchema Zod)
    packages/shared/src/schemas/index.ts                 (export)
    packages/orchestration/src/index.ts                  (export tpac-meter)
    .vbw-planning/v3-tracking.md                         (record M2 TPAC baseline in metrics table)
    docs/operations/observability.md                     (expand TPAC + cache-hit-ratio sections)
  </files>
  <action>
1. Implement `packages/orchestration/src/tpac-meter.ts`:
   ```ts
   import type { MeterSnapshot } from '@swt-labs/shared';

export interface TpacReport {
readonly schema_version: 1;
readonly milestone: string;
readonly fixture: string;
readonly provider: string;
readonly model: string;
readonly tpac_input: number;
readonly tpac_output: number;
readonly tpac_total: number;
readonly criteria_satisfied: number; // count of must_haves passed
readonly tokens_per_criterion: number; // tpac_total / criteria_satisfied
readonly recorded_at: string;
}

export function computeTpac(snapshot: MeterSnapshot, opts: ComputeTpacOptions): TpacReport {
// Aggregate the meter records by milestone; divide by criteria_satisfied to get TPAC
// per TDD2 §8.1's TPAC formula.
}

````

2. Implement `packages/shared/src/schemas/tpac-report.ts`:
- Zod schema matching `TpacReport`.
- Validates at both `swt bench`'s emit boundary (PR-21) and `test/regression`'s consume boundary.

3. Run the M2 reference scenario end-to-end:
- Use `packages/test-utils/src/run-milestone.ts` from PR-18.
- Fixture: `ref-fastapi-empty` (the frozen spec from PR-18).
- Cassettes: `packages/test-utils/golden/ref-fastapi/cassettes/`.
- Provider: Anthropic.
- Run produces a `MeterSnapshot` (from the runtime's token meter) + a list of satisfied must_haves from QA.

4. Compute the TPAC:
- `tpac_total = sum of input + output tokens across the milestone`
- `criteria_satisfied = count of must_haves with verdict 'passed'`
- `tokens_per_criterion = tpac_total / criteria_satisfied`

5. Record the M2 baseline in `.vbw-planning/v3-tracking.md`'s Metrics table:
- Replace the `M2 baseline | TBD` row with the actual numbers.
- Include the full TpacReport JSON in a `<details>` block in the same file (for forensics later).

6. Update `docs/operations/observability.md`:
- Expand the "M4 cache-hit-ratio" section since the M2 baseline is the comparison point.
- Expand the "TPAC measurement" section with the M2 baseline numbers + the calculation methodology.

7. Tests:
- `packages/orchestration/test/tpac-meter.test.ts`: feed `computeTpac` synthetic MeterSnapshots; assert the math.
- No new test for the M2 baseline run itself — that's a one-time measurement, not a recurring CI gate (the recurring gate is the regression suite from PR-18).
</action>
<verify>
1. `pnpm test --filter @swt-labs/orchestration` green; tpac-meter.test.ts passes.
2. `.vbw-planning/v3-tracking.md`'s Metrics table row for M2 has real numbers (not `TBD`).
3. The TpacReport JSON in the v3-tracking file passes Zod validation (`pnpm exec node -e "..."` to validate).
4. `grep "M2 baseline" .vbw-planning/v3-tracking.md` shows a concrete tpac_total + tokens_per_criterion.
5. `docs/operations/observability.md` references the M2 baseline numbers in the TPAC section.
</verify>
<done>
M2 TPAC baseline recorded. The single hard merge gate for M2 exit per TDD2 §13.2.3 is met. M4's −40% target now has a fixed comparison point.
</done>
</task>

<task type="auto">
<name>PR-20 · `swt rpc` verb — delegate to Pi `runRpcMode` without protocol modification</name>
<files>
 packages/cli/src/commands/rpc.ts                     (NEW)
 packages/runtime/src/rpc-runner.ts                   (NEW — delegate to Pi)
 packages/runtime/src/index.ts                        (export rpc-runner)
 packages/cli/src/router.ts                           (route rpc verb)
 packages/cli/src/commands/index.ts                   (export rpc)
 packages/cli/test/commands/rpc.test.ts               (NEW)
 docs/cli/verbs/rpc.md                                (NEW per-verb doc)
 docs/cli/README.md                                   (update M2 verbs delta table)
</files>
<action>
1. Implement `packages/runtime/src/rpc-runner.ts`:
```ts
import { runRpcMode, type AgentSessionRuntime } from '@earendil-works/pi-coding-agent';

export async function runRpc(opts: RpcOpts): Promise<void> {
  // Build an AgentSessionRuntime from Pi's `createAgentSession` shape
  // Delegate to `runRpcMode(runtime)` per TDD2 §3.2 + §5
  // Pi's RPC protocol is preserved; we expose it under the `swt` binary name
  const runtime = await buildRpcRuntime(opts);
  await runRpcMode(runtime);
}
````

- This is the only direct Pi value-level import outside `runtime/src/session.ts` + the extensions. Per Principle 1 + ADR-001, runtime is the only Pi importer.

2. Implement `packages/cli/src/commands/rpc.ts`:
   - Parses args per Pi's RPC mode CLI conventions (`--mode rpc` is implicit when invoked via `swt rpc`).
   - Stdout reserved for the RPC log stream by Pi's convention (per TDD2 §3.2.4 — "Output: stderr by default; stdout for `swt rpc` mode").
   - Delegates to `runtime.runRpc(opts)`.

3. Wire `swt rpc` into the router (`packages/cli/src/router.ts`) + dispatch table (`packages/cli/src/commands/index.ts`).

4. Tests:
   - `packages/cli/test/commands/rpc.test.ts`: mocks `runRpcMode`; asserts the verb delegates without modification (no extra args, no transformation, just passthrough).
   - Asserts stdout vs stderr separation per the Pi convention.

5. Documentation:
   - `docs/cli/verbs/rpc.md` (NEW): brief verb reference. Inputs, outputs, exit codes, invocation examples.
   - `docs/cli/README.md`: update the M2 verbs delta table to mark `swt rpc` as ✓ shipped.
     </action>
     <verify>
6. `pnpm test --filter @swt-labs/cli` green; rpc.test.ts passes.
7. `swt rpc --help` (or `swt rpc --mode rpc`) returns the expected usage.
8. `grep -nE "runRpcMode" packages/runtime/src/rpc-runner.ts` returns hits.
9. `grep -rE "from '@earendil-works/" packages/cli` returns nothing (Principle 1 still holds — only runtime imports Pi).
10. `pnpm typecheck` + `pnpm lint` + `pnpm format:check` all green.
    </verify>
    <done>
    `swt rpc` delegates to Pi's `runRpcMode` unchanged. Pi's RPC surface is now reachable under the `swt` binary name without the user installing two CLIs.
    </done>
    </task>

<task type="auto">
  <name>PR-21 · `swt bench` verb — TPAC reference benchmark + CLI invocation</name>
  <files>
    packages/cli/src/commands/bench.ts                   (NEW)
    packages/cli/src/router.ts                           (route bench verb)
    packages/cli/src/commands/index.ts                   (export bench)
    packages/cli/test/commands/bench.test.ts             (NEW)
    docs/cli/verbs/bench.md                              (NEW per-verb doc)
    docs/cli/README.md                                   (M2 verbs delta table)
    .vbw-planning/v3-tracking.md                         (note `swt bench` ships in PR-21 + 1st TPAC baseline recorded in PR-19)
  </files>
  <action>
1. Implement `packages/cli/src/commands/bench.ts`:
   - Args: `--fixture=<name>` (default `ref-fastapi-empty`); `--provider=<name>` (default `anthropic`); `--cassettes=<path>` (default `packages/test-utils/golden/<fixture>/cassettes/`); `--output=<file>` (writes TpacReport JSON; default stdout).
   - Invokes `runMilestone` from `@swt-labs/test-utils` (PR-18) + `computeTpac` from `@swt-labs/orchestration` (PR-19).
   - Emits the TpacReport JSON validated by `TpacReportSchema` from `@swt-labs/shared` (PR-19).

2. The output of `swt bench --fixture=ref-fastapi-empty` is identical to what `test/regression/ref-fastapi.regression.test.ts` consumes — same TpacReport shape, same milestone, same fixture. This is the "user-facing" wrapper on the regression test's machinery.

3. Wire `swt bench` into the router + dispatch table.

4. Tests:
   - `packages/cli/test/commands/bench.test.ts`: mocks `runMilestone` + `computeTpac`; asserts the verb invokes them with the correct args, validates the output against `TpacReportSchema`, and writes to the specified file when `--output` is set.
   - Stdout output is JSON when `--output` is unset (machine-readable by default).

5. Documentation:
   - `docs/cli/verbs/bench.md` (NEW): brief verb reference + invocation examples + reference to TDD2 §14.9 (the TPAC measurement protocol).
   - `docs/cli/README.md`: M2 verbs delta table marks `swt bench` as ✓ shipped.

6. Final `.vbw-planning/v3-tracking.md` update:
   - The M2 PR table gains rows for PR-17..PR-21 (matches the M1 pattern).
   - The exit-gate signoff table: M2 row marked complete with the date + the TpacReport summary.
   - The Metrics table: M2 baseline already populated by PR-19; this PR adds the `swt bench` invocation snippet for reproducibility.
     </action>
     <verify>
7. `pnpm test --filter @swt-labs/cli` green; bench.test.ts passes.
8. `swt bench --fixture=ref-fastapi-empty --provider=anthropic` runs end-to-end against the cassettes, emits a valid TpacReport JSON.
9. The JSON output passes Zod validation (cli internally validates before emitting).
10. `swt bench --help` returns the expected usage.
11. `pnpm typecheck` + `pnpm lint` + `pnpm format:check` + `pnpm test` + `pnpm build` all green.
12. CI matrix green on push.
    </verify>
    <done>
    `swt bench` runs the TPAC reference scenario and emits a validated TpacReport. M2 exit gate per TDD2 §13.2.3 is reached:

- Reference greenfield project runs a full milestone end-to-end on Anthropic ✓ (PR-18 regression suite)
- Regression suite passes ✓ (PR-18)
- TPAC measured + recorded as the fixed M2 baseline ✓ (PR-19)
- Dashboard's existing panels work against the new event stream ✓ (PR-17)
- `swt bench` ships ✓ (this PR)
  </done>
  </task>

</tasks>

<verification>
1. After PR-17: dashboard SSE consumes Pi events; LogPanel TS2322 fixed; 10 dashboard test-debts cleared.
2. After PR-18: v2.3.5 golden-run regression test passes against v3; methodology-preservation safety net live.
3. After PR-19: M2 TPAC baseline recorded in v3-tracking.md; the fixed point for M4's −40% target exists.
4. After PR-20: `swt rpc` delegates to Pi's `runRpcMode` unchanged.
5. After PR-21: `swt bench` ships; M2 exit gate per TDD2 §13.2.3 is reached.

After all 5 PRs: M2 exit gate criteria all met. M3 Worktree dispatcher is ready to begin via `/vbw:vibe --plan 3`.
</verification>

<success_criteria>
After PR-17..PR-21 ship as 5 atomic commits, M2 exits cleanly per TDD2 §13.2.3:

- Dashboard SSE consumes Pi events through `@swt-labs/runtime`'s `SwtEvent` union; 10 dashboard test-debt items closed.
- `packages/test-utils/golden/ref-fastapi/` ships the frozen spec + v2.3.5 baseline + recorded cassettes.
- `test/regression/ref-fastapi.regression.test.ts` runs in CI and passes against the v2 golden (modulo allowed drift).
- `.vbw-planning/v3-tracking.md` Metrics table has the M2 TPAC baseline as concrete numbers (the fixed M4 −40% comparison point).
- `swt rpc` verb delegates to Pi's `runRpcMode` unchanged.
- `swt bench` verb runs the reference scenario and emits validated TpacReport.
- `regression.yml` workflow runs `pnpm test:regression` (no longer stub).
- Umbrella issue [#32](https://github.com/swt-labs/stop-wasting-tokens/issues/32) closes out the dashboard + verification + methodology clusters (only the CLI publishConfig + config-doc-drift clusters remain; those resolve at M6 release prep).
- ADR matrix unchanged: 6 Accepted, 6 Proposed, 1 Deferred.

**M2 EXIT GATE conditions met:**

- Reference greenfield project runs a full milestone end-to-end on Anthropic, byte-identical to v2.x golden (modulo allowed drift).
- Regression suite passes.
- TPAC measured + recorded as the fixed baseline.
- Dashboard existing panels work against the new event stream.

**M3 entry conditions met.** Plan 03-01 (worktree-manager + claim-registry + DAG-resolver + lock-files per TDD2 §13.3.1's PR-22..PR-29 table) is ready to begin.
</success_criteria>
