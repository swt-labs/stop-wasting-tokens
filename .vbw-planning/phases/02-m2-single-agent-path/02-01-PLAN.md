---
phase: 2
plan: 01
title: Methodology Rewire + Single-Agent Path (PR-12 → PR-16)
type: execute
wave: 1
depends_on: ["01-03"]
cross_phase_deps: ["01-01", "01-02"]
autonomous: false
effort_override: balanced
skills_used: []
files_modified:
  - packages/methodology/src/vibe/handlers/plan.ts
  - packages/methodology/src/vibe/handlers/execute.ts
  - packages/methodology/src/vibe/handlers/qa.ts
  - packages/methodology/src/vibe/handlers/bootstrap.ts
  - packages/methodology/src/vibe/handlers/verify.ts
  - packages/methodology/src/vibe/handlers/re-verify.ts
  - packages/methodology/src/vibe/handlers/plan-and-execute.ts
  - packages/methodology/src/vibe/handlers/all-done.ts
  - packages/methodology/src/vibe/handlers/archive.ts
  - packages/methodology/src/vibe/handlers/milestone-uat-recovery.ts
  - packages/methodology/src/vibe/handlers/scope.ts
  - packages/methodology/src/vibe/handlers/stubs.ts
  - packages/methodology/src/vibe/orchestration/agent-spec-resolver.ts
  - packages/methodology/src/vibe/orchestration/dev-runner.ts
  - packages/methodology/src/vibe/orchestration/lazy-install-spawner.ts
  - packages/methodology/src/vibe/orchestration/plan-input.ts
  - packages/methodology/src/vibe/orchestration/summary-writer.ts
  - packages/methodology/src/vibe/orchestration/waves.ts
  - packages/methodology/src/vibe/orchestration/index.ts
  - packages/methodology/src/profiles/scout.ts
  - packages/methodology/src/profiles/architect.ts
  - packages/methodology/src/profiles/lead.ts
  - packages/methodology/src/profiles/dev.ts
  - packages/methodology/src/profiles/qa.ts
  - packages/methodology/src/profiles/debugger.ts
  - packages/methodology/src/profiles/index.ts
  - packages/methodology/test/vibe/handlers/bootstrap.test.ts
  - packages/methodology/test/vibe/handlers/plan.test.ts
  - packages/methodology/test/vibe/handlers/execute.test.ts
  - packages/methodology/test/vibe/handlers/qa.test.ts
  - packages/methodology/test/vibe/dispatch.test.ts
  - packages/orchestration/src/dispatcher.ts
  - packages/orchestration/src/role-router.ts
  - packages/orchestration/src/prompt-builder.ts
  - packages/orchestration/test/role-router.test.ts
  - packages/orchestration/test/prompt-builder.test.ts
  - packages/core/src/abstractions/AgentSpawner.ts
  - packages/core/src/types/codex-reasoning-effort.ts
  - packages/core/src/types/agent-spec.ts
  - packages/core/test/handoff.test.ts
  - packages/cli/src/commands/vibe.ts
  - packages/cli/src/main.ts
  - packages/cli/src/router.ts
  - packages/cli/test/commands/vibe.test.ts
  - packages/cli/test/commands/doctor.test.ts
  - packages/dashboard/src/server/vibe/permission-gate.ts
  - packages/dashboard/src/server/vibe/ui-permission-gate.ts
  - packages/dashboard/src/server/vibe/composite-permission-gate.ts
  - packages/dashboard/src/server/routes/permissions.ts
  - packages/dashboard/src/server/routes/update.ts
  - packages/dashboard/test/permission-gate.test.ts
  - packages/dashboard/test/ui-permission-gate.test.ts
  - packages/verification/src/runner.ts
  - packages/verification/src/checks/index.ts
  - packages/verification/src/guards/bash-guard.ts
  - packages/verification/test/guards.test.ts
  - packages/verification/test/runner.test.ts
  - packages/shared/src/types/agent-spec.ts
  - packages/shared/src/types/index.ts
  - docs/decisions/ADR-002-extension-result-protocol.md
  - docs/decisions/test-debt-tracking.md
  - .vbw-planning/STATE.md
  - .vbw-planning/ROADMAP.md
  - .vbw-planning/v3-tracking.md
forbidden_commands:
  - "git push --force"
  - "git rebase -i origin/main"
  - "rm -rf packages/methodology"
must_haves:
  truths:
    - "Lead role dispatches Architect (scout/plan) and Dev tasks through `@swt-labs/orchestration`'s dispatcher; methodology no longer calls Pi directly (Principle 2)."
    - "Dev role runs one task at a time end-to-end through the dispatcher; per-task `swt-task-result` Pi Extension custom entries are emitted and harvested per ADR-002."
    - "QA role runs the static-check ladder (typecheck → lint → format → tests) before escalating to LLM; LLM-tier checks fire only when at least one static check fails."
    - "`swt vibe` end-to-end works against a Pi session for a single Anthropic provider (the M2 reference scenario); no driver-package code is invoked."
    - "`UiPermissionGate` lands as a sibling to `DashboardPermissionGate`; UI-button-originated POSTs (no `session_id`) route through it; vibe-session POSTs continue through `DashboardPermissionGate`."
    - "`CodexReasoningEffort` → `ThinkingLevel` cascade rename complete: `AgentSpec.reasoning_effort: CodexReasoningEffort` → `AgentSpec.thinking_level: ThinkingLevel`; `packages/core/src/types/codex-reasoning-effort.ts` deleted; methodology's `agent-spec-resolver.ts` reads `thinking_level` from project TOML."
    - "9 v2.3.5 carry-forward methodology test failures (4 bootstrap.test.ts ZodError + 2 dispatch.test.ts NotImplementedError shape + 3 plan/qa/execute driver fallout) are unskipped (`describe.skip` → `describe`) and pass."
    - "3 v2.3.5 carry-forward `packages/verification/test/guards.test.ts` failures (`checkBashCommand` denylist regression) are FIXED in this plan — security regression flagged HIGH-priority in `docs/decisions/test-debt-tracking.md`. The test file is unskipped + passes."
    - "Role profiles live at `packages/methodology/src/profiles/<role>.{ts,prompt.md}` per TDD2 §10.1 (default tier, tool subset, prompt strategy, session mode, thinking level — all 6 roles)."
    - "Dispatcher accepts a `role: SDLCRole` parameter and routes the role through the appropriate tool subset (read-only for scout/architect/qa, coding for lead/dev/debugger) per TDD2 §10.4."
    - "No source file outside `packages/runtime/` imports from `@earendil-works/*` (Principle 1 ESLint rule passes at error severity)."
  artifacts:
    - {path: "packages/methodology/src/profiles/scout.ts", provides: "Scout role profile", contains: "defaultTier"}
    - {path: "packages/methodology/src/profiles/architect.ts", provides: "Architect role profile", contains: "defaultTier"}
    - {path: "packages/methodology/src/profiles/lead.ts", provides: "Lead role profile", contains: "defaultTier"}
    - {path: "packages/methodology/src/profiles/dev.ts", provides: "Dev role profile", contains: "defaultTier"}
    - {path: "packages/methodology/src/profiles/qa.ts", provides: "QA role profile", contains: "defaultTier"}
    - {path: "packages/methodology/src/profiles/debugger.ts", provides: "Debugger role profile", contains: "defaultTier"}
    - {path: "packages/orchestration/src/role-router.ts", provides: "role → tool subset router", contains: "toolsForRole"}
    - {path: "packages/orchestration/src/prompt-builder.ts", provides: "deterministic prompt construction", contains: "buildPrompt"}
    - {path: "packages/dashboard/src/server/vibe/ui-permission-gate.ts", provides: "UI-button permission gate", contains: "class UiPermissionGate"}
    - {path: "packages/dashboard/src/server/vibe/composite-permission-gate.ts", provides: "session-keyed router", contains: "CompositePermissionGate"}
  key_links:
    - {from: "methodology/src/vibe/handlers/plan.ts", to: "orchestration/src/dispatcher.ts", via: "createDispatcher import"}
    - {from: "methodology/src/profiles/<role>.ts", to: "orchestration/src/role-router.ts", via: "toolsForRole consumption"}
    - {from: "dashboard/src/server/vibe/composite-permission-gate.ts", to: "ui-permission-gate.ts", via: "session-keyed routing"}
    - {from: "cli/src/commands/vibe.ts", to: "orchestration/createDispatcher", via: "end-to-end glue"}
---

<objective>
M2's first plan rewires the methodology layer (preserved verbatim from v2.x) so it dispatches through `@swt-labs/orchestration`'s `createDispatcher` (M1 PR-09's `HarvestStrategy` surface) instead of the v2 driver-package paths. Each of the six SDLC roles gets a real profile at `packages/methodology/src/profiles/<role>.{ts,prompt.md}` per TDD2 §10.1 (default tier, tool subset, thinking level, session mode). The dispatcher gains a `role-router.ts` that maps roles to the correct tool subsets (read-only vs coding) per §10.4. `swt vibe` end-to-end works against a Pi session for one Anthropic provider; this is the M2 reference scenario.

Two M1-deferred items close in this plan: (a) the `CodexReasoningEffort → ThinkingLevel` cascade rename (deferred from Plan 01-01 PR-04), and (b) the HIGH-priority `packages/verification/test/guards.test.ts` bash-guard security regression (flagged at Plan 01-03 PR-11 Task A as next-hotfix priority). 9 methodology test failures + 3 verification failures from the v2.3.5 test-debt umbrella (#32) unskip and pass.

`UiPermissionGate` lands as a sibling to v2.x's `DashboardPermissionGate` per TDD2 §12 — UI-button-originated POSTs route through it; vibe-session POSTs (with `session_id`) continue through the existing gate. A `CompositePermissionGate` routes by request shape.

This plan covers 5 of M2's 10 PRs (PR-12..PR-16). Plan 02-02 (PR-17..PR-21 — dashboard SSE rewire, cassette regression, TPAC baseline, swt rpc, swt bench) follows.
</objective>

<context>
@.vbw-planning/phases/01-m1-foundation/01-01-SUMMARY.md
@.vbw-planning/phases/01-m1-foundation/01-02-SUMMARY.md
@.vbw-planning/phases/01-m1-foundation/01-03-SUMMARY.md
@TDD2.md  # §10 (six roles reframed for Pi), §11 (methodology layer), §13.2 (M2 deliverables), §12 (UiPermissionGate placement)
@.vbw-planning/v3-tracking.md  # M1 PR table + ADR matrix
@docs/decisions/test-debt-tracking.md  # umbrella #32 cluster inventory

## State at plan start (post-M1 baseline)

What's on disk when this plan begins:

- **`@swt-labs/methodology` is the v2.3.5 import surface** — `packages/methodology/src/vibe/handlers/*.ts` (10 handlers: bootstrap, plan, plan-and-execute, execute, qa, verify, re-verify, archive, all-done, milestone-uat-recovery, scope, stubs). `vibe/orchestration/*.ts` (agent-spec-resolver, dev-runner, lazy-install-spawner, plan-input, summary-writer, waves, index). Currently uses `AgentSpec` from `@swt-labs/core` carrying `reasoning_effort: CodexReasoningEffort` — the v2 vocabulary that needs the M1-deferred rename.
- **`@swt-labs/orchestration` ships dispatcher + harvest** (Plan 01-02 PR-09, commit `df9cc78`). `createDispatcher(opts)` accepts `HarvestStrategy` (`'stub' | 'entries' | 'file'`); harvest returns parsed `TaskResult` validated by `TaskResultSchema`. M2 PR-12 extends this with `role: SDLCRole` routing + a real prompt-builder. The `'stub'` HarvestStrategy default still works for callers that haven't migrated.
- **`@swt-labs/runtime` ships meter + extractors + provider quirks + result-protocol Extension** (Plans 01-01 + 01-02). `swt_report_result` registered via closure-captured `pi.appendEntry`. `runtime/src/providers/role-resolver.ts` already has `resolveTierForRole` + `resolveThinkingLevelForRole` per TDD2 §10.5; this plan wires those into the methodology layer at the role-profile boundary.
- **Profiles directory does NOT exist yet** — `packages/methodology/src/profiles/` is created in PR-12. The v2 codebase had implicit per-role configuration scattered across `agent-spec-resolver.ts` + the codex-driver TOMLs; v3 consolidates into the canonical per-role profile files per TDD2 §10.1.
- **`UiPermissionGate` does NOT exist yet** — `packages/dashboard/src/server/vibe/permission-gate.ts` ships the v2 `DashboardPermissionGate` (session-keyed). PR-16 adds `ui-permission-gate.ts` + `composite-permission-gate.ts` siblings.
- **`packages/verification/src/guards/bash-guard.ts` has the HIGH-priority denylist regression** — `checkBashCommand` no longer blocks `rm -rf /`, `curl ... | sh`, fork bombs. Flagged at Plan 01-03 PR-11 Task A in `docs/decisions/test-debt-tracking.md` for next-hotfix priority. This plan fixes it in PR-14 (the QA role plan, where bash-guard naturally sits in the QA static-check ladder).
- **9 v2.3.5 methodology test failures are skipped at describe-level** under umbrella #32: 4 bootstrap.test.ts ZodError (`writeRoadmap` "too_small" min 1); 2 dispatch.test.ts NotImplementedError shape; 3 plan/qa/execute driver-fallout (`/Real Codex AgentSpawner/` assertion). PR-12..PR-15 work resolves them as a side-effect; un-skipping is recorded in each PR's deviations.
- **5 ADRs Accepted at this plan's start** (001/002/003/004/005/010); 6 Proposed; 1 Deferred. This plan doesn't add or promote ADRs by itself — the M2 methodology rewire is implementation of decisions already documented.

What's known-deferred forward (so this plan doesn't re-litigate the decisions):

- TPAC baseline measurement on `ref-fastapi` lands in Plan 02-02 PR-19. This plan ships the methodology that produces the artefacts the baseline measures; the measurement itself happens after PR-17's dashboard SSE rewire.
- Worktree-per-task isolation (ADR-008) is M3 territory. M2 runs Dev tasks sequentially without worktrees — the dispatcher's HarvestStrategy is mode-agnostic so M3 swaps in worktree-keyed harvest without reshaping the methodology layer.
- The two cassette-driven test activations (PR-07 byte-identical + PR-09 end-to-end) stay deferred to the user-driven recording session. M2 PR-18's regression suite uses different cassettes (the v2 golden-bundle replay), so the two activations are independent.
- ADR-002 promotion to "Accepted" already happened at Plan 01-02 PR-09; M2 PR-13's `swt_report_result` consumption in the Dev role is the first real end-to-end exercise but doesn't change the ADR's status.
</context>

<tasks>

<task type="auto">
  <name>PR-12 · Lead role + Architect role through orchestration dispatcher</name>
  <files>
    packages/methodology/src/profiles/                   (NEW dir: scout.ts, architect.ts, lead.ts, dev.ts, qa.ts, debugger.ts, index.ts + corresponding *.prompt.md)
    packages/orchestration/src/role-router.ts            (NEW — toolsForRole per TDD2 §10.4)
    packages/orchestration/src/prompt-builder.ts         (NEW — deterministic prompt construction per role)
    packages/orchestration/test/role-router.test.ts      (NEW)
    packages/orchestration/test/prompt-builder.test.ts   (NEW)
    packages/methodology/src/vibe/handlers/plan.ts       (replace v2 codex-driver path with orchestration.createDispatcher)
    packages/methodology/src/vibe/orchestration/agent-spec-resolver.ts  (read from `packages/methodology/src/profiles/` instead of TOML)
    packages/shared/src/types/agent-spec.ts              (NEW — moved from core; carries thinking_level not reasoning_effort)
    packages/core/src/types/agent-spec.ts                (delete or re-export shim)
    packages/core/src/types/codex-reasoning-effort.ts    (DELETE — M2 carry-forward from Plan 01-01 PR-04)
  </files>
  <action>
1. Create `packages/methodology/src/profiles/` with one TypeScript module + one Markdown prompt file per role per TDD2 §10.1:
   - `scout.ts` — `defaultTier: 'cheap-fast'`, tool subset `read|grep|find|ls`, prompt at `scout.prompt.md` (excerpt from TDD2 §10.3 — "You are a focused code reader...").
   - `architect.ts` — `defaultTier: 'quality'`, read-only tools, prompt at `architect.prompt.md`.
   - `lead.ts` — `defaultTier: 'balanced'`, coding tools, prompt at `lead.prompt.md`.
   - `dev.ts` — `defaultTier: 'balanced'`, coding tools, prompt at `dev.prompt.md`.
   - `qa.ts` — `defaultTier: 'balanced'`, `read + bash` tools, prompt at `qa.prompt.md` (instructs static-check-first ladder).
   - `debugger.ts` — `defaultTier: 'reasoning'`, all tools, prompt at `debugger.prompt.md`, thinking level `xhigh`.
   - `index.ts` — barrel that exports the 6 profile objects + `PROFILES: Record<SDLCRole, RoleProfile>`.

2. Each profile object shape:
   ```ts
   export interface RoleProfile {
     readonly role: SDLCRole;
     readonly defaultTier: Tier;
     readonly toolSubset: 'readonly' | 'bash' | 'coding';
     readonly sessionMode: 'ephemeral' | 'persistent';
     readonly defaultThinkingLevel: ThinkingLevel;
     readonly promptPath: string; // resolved from import.meta.url to the sibling .prompt.md
   }
   ```

3. Implement `packages/orchestration/src/role-router.ts` per TDD2 §10.4:
   ```ts
   export function toolsForRole(role: SDLCRole, cwd: string): AgentTool[] {
     // matches the §10.4 sketch verbatim
     switch (role) {
       case 'scout':
       case 'architect':
         return createReadOnlyTools(cwd);
       case 'lead':
       case 'dev':
       case 'debugger':
         return createCodingTools(cwd);
       case 'qa':
         return [...createReadOnlyTools(cwd), createBashTool(cwd)];
     }
   }
   ```
   Plus role-specific session options (ephemeral vs persistent per §10.5).

4. Implement `packages/orchestration/src/prompt-builder.ts` — emits the fixed-order prompt blocks per TDD2 §8.3 (role system prompt → PROJECT → REQUIREMENTS → STATE → phase context → cache breakpoint → task brief → must-haves). Reads `RoleProfile.promptPath` for the role system prompt.

5. Rewire `packages/methodology/src/vibe/handlers/plan.ts`:
   - Plan handler currently calls codex-driver-style spawn paths (the imports broke at Plan 01-01 PR-01a).
   - Replace with `createDispatcher({ harvestStrategy: { kind: 'entries', getEntries: () => session.entries } })` from `@swt-labs/orchestration`.
   - Architect dispatch: `await dispatcher.dispatch({ taskId, role: 'architect', cwd, claims: [] })` — the dispatcher resolves tier + thinking via the role profile + project-config override.
   - The plan handler writes the resulting `TaskResult.summary` to `phases/{NN}-{slug}/{NN}-RESEARCH.md` (Scout output) and `{NN}-{MM}-PLAN.md` (Architect output) per TDD2 §10.6.

6. Rewire `packages/methodology/src/vibe/orchestration/agent-spec-resolver.ts`:
   - Currently reads `.swt-planning/config.json#roles.<role>` + a TOML for the codex-driver.
   - New behaviour: read `RoleProfile` from `packages/methodology/src/profiles/<role>.ts`; apply project-config overrides per the 4-level precedence (TDD2 §10.2: built-in → user config → project config → task override). Return an `AgentSpec` with `thinking_level: ThinkingLevel` (the M2 rename).

7. **CodexReasoningEffort → ThinkingLevel rename** (the M2 carry-forward from Plan 01-01 PR-04):
   - Move `AgentSpec` from `@swt-labs/core/src/types/agent-spec.ts` to `@swt-labs/shared/src/types/agent-spec.ts`; replace `reasoning_effort: CodexReasoningEffort` with `thinking_level: ThinkingLevel`.
   - `@swt-labs/core/src/types/codex-reasoning-effort.ts` — `git rm`. The vocabulary lives at `@swt-labs/shared/src/types/thinking-level.ts` (already in place from Plan 01-01 PR-04).
   - Update every `AgentSpec` instantiation in `packages/methodology/`, `packages/core/`, `packages/cli/`.
   - Update tests that assert against `reasoning_effort` — rename the field, keep the assertions.

8. Unskip + fix the 9 v2.3.5 methodology test failures + 4 from bootstrap.test.ts:
   - `packages/methodology/test/vibe/handlers/bootstrap.test.ts` — 4 ZodError "too_small min 1" — `writeRoadmap` requires at least one phase; tests pass empty arrays. Update fixtures to include a minimal phase entry.
   - `packages/methodology/test/vibe/handlers/plan.test.ts` + `qa.test.ts` + `execute.test.ts` — driver-fallout assertions (`/Real Codex AgentSpawner/`). Update assertions to match the new orchestration-dispatcher path; verify they now exercise real behaviour.
   - `packages/methodology/test/vibe/dispatch.test.ts` — 2 NotImplementedError shape regressions. Inspect `dispatch.ts`'s actual error class shape; update the test's `instanceof` check.
   - Remove the `describe.skip(` → `describe(` + delete the 3-line `// TODO(v3-debt): tracking #32` header from each restored file.

9. Update `docs/decisions/test-debt-tracking.md` rows for the methodology cluster — mark resolved with this commit's SHA; note in the umbrella issue (close the methodology sub-section if all 9 pass).
  </action>
  <verify>
1. `pnpm typecheck` clean.
2. `pnpm test --filter @swt-labs/methodology` green; the 9 previously-skipped tests pass.
3. `pnpm test --filter @swt-labs/orchestration` green; `role-router.test.ts` + `prompt-builder.test.ts` are new tests covering the toolsForRole switch + the fixed-order prompt blocks.
4. `grep -rE "reasoning_effort|CodexReasoningEffort" packages/` returns nothing (rename complete).
5. `ls packages/core/src/types/codex-reasoning-effort.ts` returns no such file.
6. `pnpm lint` 0 errors.
7. `grep -rE "from '@earendil-works/" packages/methodology packages/orchestration` returns nothing (Principle 1 still holds).
  </verify>
  <done>
Lead + Architect roles dispatch through `@swt-labs/orchestration`'s `createDispatcher`. Role profiles consolidated at `packages/methodology/src/profiles/`. CodexReasoningEffort rename complete. 9 methodology test debts cleared.
  </done>
</task>

<task type="auto">
  <name>PR-13 · Dev role through dispatcher; one task at a time + swt_report_result end-to-end</name>
  <files>
    packages/methodology/src/vibe/handlers/execute.ts    (rewire to use orchestration.createDispatcher per-task)
    packages/methodology/src/vibe/orchestration/dev-runner.ts  (sequential dispatch loop; reads plan, dispatches Dev per task, harvests TaskResult)
    packages/orchestration/src/dispatcher.ts             (extend HarvestStrategy with role-aware dispatching)
    packages/orchestration/test/dispatcher-dev.int.test.ts  (NEW — sequential Dev dispatch through stubbed Pi session)
    packages/methodology/test/vibe/handlers/execute.test.ts  (unskip + assert against new dispatcher path)
    packages/methodology/test/vibe/orchestration/dev-runner.test.ts  (NEW — covers the sequential-loop semantics)
  </files>
  <action>
1. Rewire `packages/methodology/src/vibe/handlers/execute.ts` to delegate to `vibe/orchestration/dev-runner.ts`. The handler becomes thin (parses args, loads plan, calls `runDevTasks(plan, cwd)`).

2. Implement `vibe/orchestration/dev-runner.ts`:
   ```ts
   import { createDispatcher } from '@swt-labs/orchestration';

   export async function runDevTasks(plan: Plan, cwd: string, opts?: DevRunnerOptions): Promise<RunSummary> {
     const dispatcher = createDispatcher({
       harvestStrategy: opts?.harvestStrategy ?? { kind: 'entries', getEntries: () => currentSession.entries },
     });
     const results: TaskResult[] = [];
     for (const task of plan.tasks) {
       // Sequential by design at M2; M3 parallelises with worktree isolation
       const result = await dispatcher.dispatch({
         taskId: task.id,
         role: 'dev',
         cwd,
         claims: task.claims ?? [],
         promptContext: buildTaskContext(task, plan),
       });
       results.push(result);
       if (result.status === 'failed' || result.status === 'blocked') {
         break; // halt on failure; QA will route through remediation
       }
     }
     return summariseRun(results);
   }
   ```

3. Each Dev dispatch:
   - Pi session created with `role: 'dev'` → coding tool subset (per PR-12's role-router).
   - Plan's `task.claims` propagated as `TaskBrief.claims` (no-op at M2 since no worktree; M3 wires the claim registry).
   - Prompt built via `prompt-builder.ts` (PR-12) with PROJECT/REQUIREMENTS/STATE/phase-context as cache prefix + task brief + must-haves as variable suffix (ready for M4's cache-control breakpoint).

4. Dev agents call `swt_report_result` (Plan 01-02 PR-09's Extension custom tool — already registered via `runtime/src/extensions/result-protocol.ts`); the dispatcher's `'entries'` HarvestStrategy reads the `swt-task-result` custom entry from the session and validates against `TaskResultSchema`.

5. The `'stub'` HarvestStrategy default stays in place for callers without a real Pi session; M2 production code uses `'entries'`. The sequential-loop test in `dispatcher-dev.int.test.ts` exercises both: stub for empty-session edge cases; entries for the real path with synthetic `swt-task-result` data.

6. Defensive `agent_end` placeholder behaviour from PR-09 stays in place. When a Dev task ends without calling `swt_report_result`, the harvester reads the placeholder with `status: 'failed'` + `blockers: ['protocol-violation: swt_report_result not called']`. The Dev runner halts on this.

7. Unskip + fix `packages/methodology/test/vibe/handlers/execute.test.ts`:
   - Remove `describe.skip(` → `describe(` + delete the `// TODO(v3-debt)` header.
   - Update assertions: the previous test expected `/Real Codex AgentSpawner/` error message; the new path dispatches successfully and asserts against the `TaskResult.status` and the recorded `swt-task-result` envelope.

8. New `packages/methodology/test/vibe/orchestration/dev-runner.test.ts`:
   - Synthetic plan with 3 tasks; mocked HarvestStrategy emits success/success/failed.
   - Asserts the runner halts after the failed task (4th task NOT dispatched).
   - Asserts the `RunSummary` carries all 3 results + the halt reason.
  </action>
  <verify>
1. `pnpm typecheck` + `pnpm lint` + `pnpm format:check` + `pnpm test` all green.
2. `dev-runner.test.ts` exercises the halt-on-failed semantics.
3. `dispatcher-dev.int.test.ts` exercises sequential dispatch against the synthetic `'entries'` HarvestStrategy.
4. `execute.test.ts` unskipped and passing.
5. `grep -rE "describe.skip.*v3-debt.*tracking.*32" packages/methodology/test/vibe/handlers/execute.test.ts` returns nothing.
6. The 2 `dispatch.test.ts` NotImplementedError shape regressions resolved (PR-12 unskipped that file; PR-13 confirms it still passes with the new dispatcher path).
  </verify>
  <done>
Dev role dispatches through orchestration sequentially. `swt_report_result` end-to-end (Pi Extension custom tool → custom session entry → harvest → validated TaskResult) works for the first time on a real Dev workflow.
  </done>
</task>

<task type="auto">
  <name>PR-14 · QA role with static-check ladder + LLM escalation + verification/bash-guard security fix</name>
  <files>
    packages/methodology/src/vibe/handlers/qa.ts          (rewire static-check ladder)
    packages/methodology/src/profiles/qa.ts               (default tier, prompt path)
    packages/methodology/src/profiles/qa.prompt.md        (system prompt — read + bash; static-check-first)
    packages/verification/src/runner.ts                   (entry point: static checks then LLM escalation)
    packages/verification/src/checks/                     (typecheck, lint, format, tests — pure shell wrappers)
    packages/verification/src/guards/bash-guard.ts        (FIX denylist regression — HIGH-priority security)
    packages/verification/test/guards.test.ts             (unskip + assert denylist works again)
    packages/verification/test/runner.test.ts             (NEW — covers the escalation ladder)
    packages/methodology/test/vibe/handlers/qa.test.ts    (unskip + reshape)
  </files>
  <action>
1. **Static-check ladder per TDD2 §11.2:** QA runs (in order) typecheck → lint → format → unit tests. If all pass, QA is done (no LLM call). If any check fails, QA escalates to an LLM-tier role profile (`qa.ts` profile, `balanced` tier).

2. `packages/verification/src/runner.ts`:
   ```ts
   export interface VerificationResult {
     readonly status: 'passed' | 'failed' | 'escalated';
     readonly failedChecks: ReadonlyArray<CheckResult>;
     readonly llmReport?: LlmVerificationReport;
   }

   export async function runVerification(opts: VerificationOptions): Promise<VerificationResult> {
     for (const check of STATIC_CHECKS) {
       const result = await check.run(opts.cwd);
       if (result.status === 'failed') {
         // Escalate to LLM
         const llmReport = await runLlmVerification(opts, result);
         return { status: 'escalated', failedChecks: [result], llmReport };
       }
     }
     return { status: 'passed', failedChecks: [] };
   }
   ```

3. `packages/verification/src/checks/`:
   - `typecheck.ts` — wraps `pnpm typecheck` (already in root scripts).
   - `lint.ts` — wraps `pnpm lint`.
   - `format-check.ts` — wraps `pnpm format:check`.
   - `unit-tests.ts` — wraps `pnpm test`.
   - Each module exports `{ name, run(cwd): Promise<CheckResult> }`.

4. **HIGH-priority security fix — `packages/verification/src/guards/bash-guard.ts`:**
   - The `checkBashCommand` function no longer blocks `rm -rf /`, `curl ... | sh`, fork bomb `:(){ :|: & };:`. Inspect the function's current denylist regex set; restore the patterns. Likely the regression came from a refactor that escaped special chars incorrectly or replaced `\s` with literal spaces.
   - Add a comment block above the denylist explaining each pattern (so future refactors don't drop them silently).
   - Add a regression test fixture: every pattern in the denylist must round-trip through a test case asserting `decision === 'block'`.

5. Unskip `packages/verification/test/guards.test.ts`:
   - Remove `describe.skip(` → `describe(` + delete the 3-line `// TODO(v3-debt)` header.
   - The 3 previously-failing tests (`rm -rf /`, `curl ... | sh`, fork bomb) now pass after the guard fix.

6. New `packages/verification/test/runner.test.ts`:
   - Cover the escalation ladder: all-static-checks-pass → `status: 'passed'`; one check fails → `status: 'escalated'` with `llmReport` defined.
   - Mock the LLM-tier escalation to avoid network calls.

7. Unskip `packages/methodology/test/vibe/handlers/qa.test.ts`:
   - Update assertions to match the new ladder shape (QA returns `VerificationResult` instead of a driver-flavoured object).

8. The QA role profile (`packages/methodology/src/profiles/qa.ts`, created in PR-12) gets its prompt populated:
   - `qa.prompt.md` content per TDD2 §10.3 — instructs the LLM to use `read + bash` only, run the static checks the runner already failed on (for forensics), and propose a fix as `TaskResult.notes`. Calls `swt_report_result` with `status: 'failed'` + the must_haves verdict array.

9. Update `docs/decisions/test-debt-tracking.md` — close out the verification + qa.test rows from umbrella #32.
  </action>
  <verify>
1. `pnpm test --filter @swt-labs/verification` green; `guards.test.ts` and `runner.test.ts` pass.
2. `pnpm test --filter @swt-labs/methodology` green; `qa.test.ts` is unskipped and passes.
3. `grep -rE "checkBashCommand.*decision.*block" packages/verification/test/guards.test.ts` shows the 3 denylist tests are now active assertions.
4. `pnpm typecheck` + `pnpm lint` + `pnpm format:check` all green.
5. `bash -c 'echo "rm -rf /" | node -e "import(\"./packages/verification/src/guards/bash-guard.js\").then(m => console.log(m.checkBashCommand(\"rm -rf /\").decision))"'` returns `block` (or however the cli surface invokes it — adapt to the actual module shape).
  </verify>
  <done>
QA static-check ladder works; LLM escalation only fires on real failures (saves tokens). Bash-guard security regression FIXED — `checkBashCommand` blocks the documented denylist patterns again. Umbrella issue #32's verification + qa.test clusters can close.
  </done>
</task>

<task type="auto">
  <name>PR-15 · `swt vibe` end-to-end with Pi backend for one provider (Anthropic)</name>
  <files>
    packages/cli/src/commands/vibe.ts                    (full wire-up — dispatches through orchestration via Pi)
    packages/cli/src/main.ts                             (CommandIO `spawnerEnv` resolved from runtime probe; M2 makes Pi the only real path)
    packages/cli/src/router.ts                           (route options + autonomy flags)
    packages/methodology/src/vibe/handlers/plan-and-execute.ts  (wires plan handler → dev-runner sequentially)
    packages/methodology/src/vibe/handlers/all-done.ts   (handles end-of-milestone routing per FSM)
    packages/cli/test/commands/vibe.test.ts              (unskip + rebuild the end-to-end test for one provider)
    packages/cli/test/commands/doctor.test.ts            (Pi peer-dep version surfaced in doctor output)
    packages/methodology/src/vibe/handlers/verify.ts     (QA → VERIFICATION.md write per §11.2)
    packages/methodology/src/vibe/handlers/re-verify.ts  (remediation round)
    packages/methodology/src/vibe/handlers/all-done.ts   (advance phase per FSM §11.1)
    packages/methodology/src/vibe/handlers/index.ts      (dispatch table updated)
  </files>
  <action>
1. The plan handler (PR-12), Dev runner (PR-13), and QA static-check ladder (PR-14) are now real. PR-15 wires them together in `swt vibe` so a real Anthropic-backed milestone runs end-to-end.

2. `packages/cli/src/commands/vibe.ts`:
   - Parse `--phase` / `--effort` / `--autonomy` flags as M1 did.
   - Construct `dispatcher` from `@swt-labs/orchestration` with `harvestStrategy: { kind: 'entries', getEntries: () => session.entries }`.
   - Route through the appropriate handler (`plan`, `plan-and-execute`, `execute`, `verify`, `re-verify`, `all-done`) per the methodology FSM (TDD2 §11.1).

3. `packages/cli/src/main.ts`:
   - Make Pi the only real spawner path: remove the `Pr01bStubSpawnerEnvironment` fallback (the v2-era driver-detection stub).
   - Default to `PiSpawnerEnvironment` (already in place from Plan 01-01 PR-03). `probePiAvailable()` is the doctor-surface check.

4. `packages/methodology/src/vibe/handlers/plan-and-execute.ts`:
   - Sequential: plan-handler runs (writes `{NN}-{MM}-PLAN.md`); if successful, dev-runner runs every task in the plan; if successful, qa-handler runs the static-check ladder.

5. `packages/methodology/src/vibe/handlers/verify.ts`:
   - QA's output gets written to `{NN}-VERIFICATION.md` with the `verification_passed` Zod-validated shape (per TDD2 §11.2's MustHaveSchema).
   - The VBW `phase-detect.sh` consumes this file to flip `qa_status` from `pending` to `passed` for the phase.

6. `packages/methodology/src/vibe/handlers/re-verify.ts`:
   - Remediation round: if QA returns `escalated`, the methodology FSM transitions to `REMEDIATION` (TDD2 §11.1); a Debugger dispatch is queued with the QA report as input.

7. `packages/methodology/src/vibe/handlers/all-done.ts`:
   - When the last phase in the milestone hits `COMPLETE`, write the milestone-level signoff entries.

8. Update `packages/cli/test/commands/vibe.test.ts`:
   - The 1 remaining init-redirect test from Plan 01-01 PR-04 stays. Add a new test for the end-to-end vibe-on-Pi path with mocked Pi entries (uses the orchestration dispatcher's `'entries'` strategy + synthetic `swt-task-result` data per the Plan 01-02 PR-09 pattern).

9. Update `packages/cli/test/commands/doctor.test.ts`:
   - `swt doctor` now surfaces the Pi peer-dep version via `probePiAvailable()`. Add an assertion that the report includes `pi.version` when present.

10. Documentation: ensure `docs/cli/README.md` reflects the M2 changes for `vibe`/`doctor` (per the M2-deltas section landed at Plan 01-03 PR-10 Task 1).
  </action>
  <verify>
1. `pnpm test --filter @swt-labs/cli` green; the new end-to-end vibe test passes (mocked Pi).
2. `swt vibe --plan 1` against a fixture project completes (writes a plan), provided a configured Anthropic API key + Pi runtime.
3. `pnpm typecheck` + `pnpm lint` + `pnpm format:check` all green.
4. The methodology FSM transitions are exercised by the integration tests (PR-12..PR-14's tests + this PR's new end-to-end test).
5. `swt doctor` includes `pi.version` in its report.
  </verify>
  <done>
`swt vibe` runs an end-to-end milestone on a Pi-backed session. The methodology layer dispatches Lead/Architect/Dev/QA through orchestration; the runtime layer talks to Pi; the dispatcher harvests `swt_report_result` Extension entries. M2's headline single-agent-path criterion is satisfied.
  </done>
</task>

<task type="auto">
  <name>PR-16 · UiPermissionGate + composite gate routing for dashboard UI mutations</name>
  <files>
    packages/dashboard/src/server/vibe/ui-permission-gate.ts        (NEW — UiPermissionGate class)
    packages/dashboard/src/server/vibe/composite-permission-gate.ts (NEW — session-keyed routing)
    packages/dashboard/src/server/vibe/permission-gate.ts           (existing DashboardPermissionGate preserved)
    packages/dashboard/src/server/routes/permissions.ts             (routes wired through composite)
    packages/dashboard/src/server/routes/update.ts                  (UI button-originated mutations route through UI gate)
    packages/dashboard/test/permission-gate.test.ts                 (existing tests — keep passing)
    packages/dashboard/test/ui-permission-gate.test.ts              (NEW — covers UI-gate behaviour + composite routing)
    docs/decisions/test-debt-tracking.md                            (close out the dashboard cluster after M2 PR-17)
  </files>
  <action>
1. Implement `packages/dashboard/src/server/vibe/ui-permission-gate.ts` per TDD2 §12 (`UiPermissionGate` class extending the `PermissionGate` interface):
   ```ts
   import type { PermissionGate, PermissionResponse, PermissionRequest } from '@swt-labs/core';

   export class UiPermissionGate implements PermissionGate {
     async ask(request: PermissionRequest): Promise<PermissionResponse> {
       // UI-button-originated mutations: surface to the dashboard's permission UI
       // (the same React/Solid component that v2 already has) but route by a
       // separate registry from session-keyed prompts. Approval / rejection
       // returns through the SSE channel.
       return this.surfaceToUi(request);
     }
     // ... implementation
   }
   ```

2. Implement `packages/dashboard/src/server/vibe/composite-permission-gate.ts`:
   - Inspects the request: if it carries a `session_id` (vibe-session POSTs), route to `DashboardPermissionGate`. Otherwise (UI-button POSTs), route to `UiPermissionGate`.
   - Per TDD2 §12: "POSTs originating from UI button clicks (vs vibe sessions with session_id) route through UiPermissionGate."

3. Update `packages/dashboard/src/server/routes/permissions.ts`:
   - Replace the direct `DashboardPermissionGate` injection with `CompositePermissionGate` so the router picks the correct gate per request.

4. Update `packages/dashboard/src/server/routes/update.ts`:
   - The routes listed in TDD2 §12.6 ("Routes that route through UiPermissionGate" — mostly mutations originating from button clicks: `/api/phase/regenerate`, `/api/plan/save`, etc.) wire through the composite gate.

5. Tests:
   - `packages/dashboard/test/ui-permission-gate.test.ts` (NEW): mock `UiPermissionGate.surfaceToUi`; assert that direct UI mutations route through it (not through the v2 `DashboardPermissionGate`).
   - `packages/dashboard/test/permission-gate.test.ts` (existing): keep passing (the v2 session-keyed path is preserved).
   - Add a `composite-permission-gate.test.ts` that exercises the routing decision: with `session_id` → DashboardPermissionGate; without → UiPermissionGate.

6. Update `docs/decisions/test-debt-tracking.md`:
   - The dashboard test cluster (10 v2.3.5 carry-forward failures) doesn't close here — it closes when M2 PR-17's dashboard SSE rewire ships. PR-16 only adds the permission-gate piece. Record the partial progress in test-debt-tracking.md.

7. `CONTRIBUTING.md` Branch Protection (v3) section: no update needed; the existing rules cover dashboard changes.
  </action>
  <verify>
1. `pnpm test --filter @swt-labs/dashboard` — the new `ui-permission-gate.test.ts` + `composite-permission-gate.test.ts` pass; existing `permission-gate.test.ts` still passes.
2. `pnpm typecheck` + `pnpm lint` + `pnpm format:check` all green.
3. `grep -nE "class UiPermissionGate" packages/dashboard/src/server/vibe/ui-permission-gate.ts` returns hits.
4. `grep -nE "CompositePermissionGate" packages/dashboard/src/server/routes/permissions.ts` returns hits.
5. The 10 dashboard test cluster (LogPanel TS2322 + 9 carry-forward failures) stays skipped; close-out is PR-17 territory.
  </verify>
  <done>
`UiPermissionGate` lands as a sibling to `DashboardPermissionGate`; UI-button-originated POSTs route through it via a session-keyed `CompositePermissionGate`. v2.x's deferred work flagged at v2.3 lands per TDD2 §12.
  </done>
</task>

</tasks>

<verification>
1. After PR-12: 6 role profiles + role-router + prompt-builder all in place; `CodexReasoningEffort` deleted; 9 methodology test debts cleared.
2. After PR-13: Dev role dispatches sequentially through orchestration; `swt_report_result` end-to-end is the first real-world exercise of Plan 01-02 PR-09's Extension surface.
3. After PR-14: QA static-check ladder + LLM escalation working; HIGH-priority bash-guard security regression FIXED; verification + qa.test clusters of umbrella #32 closed.
4. After PR-15: `swt vibe` runs an end-to-end milestone on a Pi-backed Anthropic session; the methodology FSM transitions work; `swt doctor` surfaces Pi peer-dep version.
5. After PR-16: `UiPermissionGate` + `CompositePermissionGate` route UI-button mutations through the new gate; v2 session-keyed path preserved.
</verification>

<success_criteria>
After PR-12..PR-16 ship as 5 atomic commits:

- All 6 SDLC role profiles live at `packages/methodology/src/profiles/` per TDD2 §10.1.
- Methodology layer dispatches via `@swt-labs/orchestration`'s `createDispatcher`; no direct Pi imports (Principle 1).
- `CodexReasoningEffort` → `ThinkingLevel` cascade rename complete; `core/src/types/codex-reasoning-effort.ts` deleted.
- `packages/verification/test/guards.test.ts` HIGH-priority denylist regression FIXED.
- 12 of the umbrella #32 test-debt skips close (9 methodology + 3 verification).
- `swt vibe` runs end-to-end on Anthropic via Pi for one provider.
- `UiPermissionGate` lands per TDD2 §12.

Plan 02-02 (PR-17..PR-21) is ready to start. The TPAC baseline measurement (PR-19) depends on PR-15's `swt vibe` end-to-end working, which is satisfied at this plan's close.
</success_criteria>
