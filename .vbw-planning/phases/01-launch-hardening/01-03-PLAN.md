---
phase: 01
plan: 01-03
title: Documentation + scripts cleanup
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - README.md
  - scripts/verify-install.sh
  - .github/workflows/install-smoke.yml
  - docs/roadmap/v1.5.md
forbidden_commands: []
must_haves:
  truths:
    - "the public README's status block links to a real, post-archive roadmap (docs/roadmap/v1.5.md or similar) — never to the gitignored .vbw-planning/ROADMAP.md"
    - "the post-install smoke test fails loudly if `swt init` produces a `.vbw-planning/` directory — there is no silent fallback masking the AGENTS.md fix"
    - "M7's state-drift verifier improvement is captured as documented design intent (deferred to v1.5 if v1.0 verifier scripts aren't shipped product code)"
  artifacts:
    - path: "README.md"
      provides: "public-facing project README with current roadmap link"
      contains: "docs/roadmap/v1.5.md"
    - path: "scripts/verify-install.sh"
      provides: "post-install smoke test that asserts .swt-planning/ output"
      contains: ".swt-planning/PROJECT.md"
  key_links: []
---
<objective>
Close audit M1, M3, and document M7. M1 and M3 are small one-line fixes. M7 is technically a `verify-state-consistency.sh` improvement — that script lives inside the VBW plugin (developer-side), not SWT product code, so the SWT-side action is to document the deferral cleanly. After this plan ships, the README points at a current roadmap, the install smoke test is strict, and M7 has a clear v1.5 disposition.
</objective>
<context>
M1: `README.md:3` reads `See [ROADMAP](.vbw-planning/ROADMAP.md)`. The path is gitignored (per `.gitignore`'s `planning_tracking=manual` policy) and post-archive moved to `.vbw-planning/milestones/01-.../ROADMAP.md` anyway. The fix: link to `docs/roadmap/v1.5.md` (canonical v1.5 engineering roadmap) or remove the link.

M3: `scripts/verify-install.sh` contains:
```bash
if [ ! -f .swt-planning/PROJECT.md ] && [ ! -f .vbw-planning/PROJECT.md ]; then
```
The `.vbw-planning/` fallback masks Plan 01-01's bootstrap fix — if `swt init` accidentally still emits to `.vbw-planning/`, this smoke test would not catch it. Strict fix: only `.swt-planning/PROJECT.md`.

M7: the state-drift warning during the audit was traced to `verify-state-consistency.sh --mode archive` flagging `missing_roadmap_md` as a hard FAIL on a clean post-archive state (no active milestone scoped). This script is part of the **VBW Claude Code plugin's helper scripts** under `/tmp/.vbw-plugin-root-link-*/scripts/`, not SWT product code shipped to npm. The SWT-side resolution is to acknowledge the issue in the v1.5 roadmap so future SWT product (which will ship its own state-consistency verifier in v1.5 F6/F7) handles the post-archive state cleanly. Action for this plan: capture as a v1.5 backlog note in `docs/roadmap/v1.5.md` Methodology section.
</context>
<tasks>
<task type="auto">
  <name>T1: README.md ROADMAP link → docs/roadmap/v1.5.md</name>
  <files>
    README.md
  </files>
  <action>
Update line 3: replace `See [ROADMAP](.vbw-planning/ROADMAP.md)` with `See [v1.5 ROADMAP](docs/roadmap/v1.5.md)`. Verify the docs path exists and is the canonical engineering roadmap. If preferable phrasing emerges, alternative: `See the [v1.5 roadmap](docs/roadmap/v1.5.md) for what's coming next.`
  </action>
  <verify>
`grep -n '\.vbw-planning' README.md` returns no matches. The new link path resolves to a file that exists.
  </verify>
  <done>
README.md status block links to a current, public, post-archive roadmap.
  </done>
</task>
<task type="auto">
  <name>T2: verify-install.sh strict .swt-planning check</name>
  <files>
    scripts/verify-install.sh
  </files>
  <action>
Remove the `.vbw-planning/PROJECT.md` fallback from the post-install smoke test. The condition becomes:
```bash
if [ ! -f .swt-planning/PROJECT.md ]; then
  echo "✗ swt init did not scaffold .swt-planning/PROJECT.md" >&2
  popd >/dev/null
  exit 1
fi
```
The point of this smoke test is to catch the C1/C2 regression — bootstrap must produce SWT-named output, not VBW-legacy paths. Hiding it behind a fallback defeats the test.
  </action>
  <verify>
`grep -n '\.vbw-planning' scripts/verify-install.sh` returns no matches. Read the script end-to-end to confirm the smoke logic is still coherent (check for `.swt-planning/` only, fail with a clear error otherwise).
  </verify>
  <done>
The smoke test will fail loudly if `swt init` ever emits to `.vbw-planning/` again.
  </done>
</task>
<task type="auto">
  <name>T3: install-smoke.yml regression check</name>
  <files>
    .github/workflows/install-smoke.yml
  </files>
  <action>
Read `.github/workflows/install-smoke.yml`. Confirm it invokes `scripts/verify-install.sh` with the expected version arg. No edits needed unless the workflow has its own embedded check that duplicates the `.vbw-planning/` fallback — if so, remove that too. This task is a "verify the workflow doesn't have its own fallback" sanity check.
  </action>
  <verify>
`grep -n '\.vbw-planning' .github/workflows/install-smoke.yml` returns no matches.
  </verify>
  <done>
The CI install smoke workflow inherits the strict check from T2 with no in-workflow override.
  </done>
</task>
<task type="auto">
  <name>T4: Document M7 in v1.5 roadmap</name>
  <files>
    docs/roadmap/v1.5.md
  </files>
  <action>
Under the Methodology section (where F7 — Hook event taxonomy expansion lives), append a small note about the state-consistency verifier handling clean post-archive state: when no active milestone is scoped (post-ship, pre-next-scope), the verifier should treat `missing_roadmap_md` as INFO/SKIP rather than FAIL. This is a v1.5 cleanup item tied to F6 / F7 (methodology infrastructure). Frame as a follow-up annotation, not a new Fn feature — it's an existing-script polish.

Suggested phrasing: `**Follow-up (M7 from v1.0 audit):** `verify-state-consistency.sh --mode archive` should treat clean post-archive state (no active milestone) as a pass condition, not flag `missing_roadmap_md` as FAIL. Fold into F6 / F7 work.`
  </action>
  <verify>
`grep -n 'M7\|missing_roadmap_md\|state-consistency' docs/roadmap/v1.5.md` returns the new note.
  </verify>
  <done>
M7 has an explicit v1.5 disposition; the deferral is documented and traceable.
  </done>
</task>
</tasks>
<verification>
1. `grep -RIn '\.vbw-planning/' README.md scripts/ .github/workflows/` returns no matches
2. `bash -n scripts/verify-install.sh` exits 0 (script syntax valid)
3. `pnpm test` exits 0 (no test regressions)
4. Manual: open `docs/roadmap/v1.5.md`, confirm M7 disposition is captured under Methodology
</verification>
<success_criteria>
- M1 closed: README.md status block links to a current roadmap
- M3 closed: install smoke test is strict — no `.vbw-planning/` fallback
- M7 captured: documented as a v1.5 follow-up in `docs/roadmap/v1.5.md`
- No CI workflow regressions
</success_criteria>
<output>
01-03-SUMMARY.md
</output>
