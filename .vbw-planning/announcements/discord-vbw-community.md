hey VBW folks 👋

We've shipped v0.1.0-alpha of stop-wasting-tokens (SWT) — token-disciplined SDLC, but as a portable npm CLI for Codex (and Claude Code / Ollama in v1.5).

Same methodology as VBW. Same lifecycle states. Same artifact shapes. Frontmatter is wire-compatible, so migration is `mv .vbw-planning .swt-planning` plus a `npm install -g @swt-labs/cli`. Full guide: <https://docs.stopwastingtokens.dev/migration/from-vbw>

If you want to try it on a real project, the closed beta is open this week. Drop a 👍 here and I'll DM the install instructions.

Looking for ~10 testers across:
- Greenfield (new project)
- Brownfield (codebase mapping + assumptions mode)
- VBW migration (in-place rename)

Repo: <https://github.com/swt-labs/stop-wasting-tokens>
Docs: <https://docs.stopwastingtokens.dev>
