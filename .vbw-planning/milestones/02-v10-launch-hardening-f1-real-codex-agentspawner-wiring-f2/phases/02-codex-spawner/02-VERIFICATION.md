---
phase: 02
tier: standard
result: PARTIAL
passed: 27
failed: 11
total: 38
date: 2026-05-07
verified_at_commit: acb12a7ab5f1e9384a3ef7ab28db6a178d8f3317
writer: write-verification.sh
plans_verified:
  - 02-01
  - 02-02
  - 02-03
---

## Other Checks

| # | ID | Check | Status | Evidence |
|---|-----|-------|--------|----------|
| 1 | MH-1A | @swt-labs/codex-driver exports a CodexAgentSpawner class implementing the AgentSpawner contract from @swt-labs/core | PASS | packages/codex-driver/src/spawner/codex-agent-spawner.ts:38 declares `class CodexAgentSpawner implements AgentSpawner`; AgentSpawner imported from @swt-labs/core via line 6-12; barrel re-exports through packages/codex-driver/src/spawner/index.ts and packages/codex-driver/src/index.ts |
| 2 | MH-1B | installAgent(spec) writes a deterministic TOML profile to ${CODEX_HOME:-~/.codex}/agents/${role}.toml using emitAgentToml; idempotent on re-install | PASS | codex-agent-spawner.ts:50-57 — mkdir -p agentsDir, writeFile(tmp), rename(tmp, target). Atomic tmp+rename pattern. Uses emitAgentToml from ../toml/agents.js (line 14). |
| 3 | MH-1C | spawn(request) delegates to the existing spawnCodex low-level function with no semantic changes | PASS | codex-agent-spawner.ts:59-64 — `return spawnCodex(request, {bin: this.#bin, ...env})`. Pure delegation, no transformation of the request. |
| 4 | MH-1D | removeAgent(role) unlinks the TOML profile file; idempotent (no-op if missing) | PASS | codex-agent-spawner.ts:66-79 — try unlink; catch ENOENT and swallow (no-op); other errors rethrown. |
| 5 | MH-1E | the class is constructable without arguments and uses sensible defaults for codex_home + bin path | PASS | codex-agent-spawner.ts:43-48 — constructor(opts: CodexAgentSpawnerOptions = {}); codex_home defaults to process.env.CODEX_HOME ?? join(homedir(), '.codex'); bin defaults to 'codex'. |
| 6 | ART-1A | packages/codex-driver/src/spawner/codex-agent-spawner.ts contains class CodexAgentSpawner implements AgentSpawner | PASS | File exists; line 38: `export class CodexAgentSpawner implements AgentSpawner`. |
| 7 | ART-1B | packages/codex-driver/test/spawner/codex-agent-spawner.test.ts contains describe('CodexAgentSpawner' | PASS | File exists; vitest run reports 5/5 cases passing for CodexAgentSpawner suite. |
| 8 | KL-1A | codex-agent-spawner.ts → core/abstractions/AgentSpawner.ts via implements AgentSpawner | PASS | Line 6-12 imports AgentSpawner from @swt-labs/core; line 38 implements clause. |
| 9 | KL-1B | codex-agent-spawner.ts → spawn/wrapper.ts via spawnCodex import | PASS | Line 15: `import { spawnCodex } from '../spawn/wrapper.js'`. |
| 10 | KL-1C | codex-agent-spawner.ts → toml/agents.ts via emitAgentToml import | PASS | Line 14: `import { emitAgentToml } from '../toml/agents.js'`. |
| 11 | MH-2A | parseLine recognises Codex `usage` chunks with input_tokens / output_tokens fields and surfaces them as a typed ParsedLine.usage | PASS | packages/codex-driver/src/spawn/parser.ts adds UsageChunkSchema (Zod) and a usage-recognition branch between handoff and text. Tests `parses a usage chunk`, `rejects a usage chunk with non-numeric token counts`, `rejects a usage chunk with negative token counts` all pass. |
| 12 | MH-2B | parseStream accumulates usage across lines (last-write-wins for the final tally) | PASS | wrapper.test.ts case `aggregates usage last-write-wins when multiple chunks appear` constructs a stream with two usage chunks (input_tokens 100→250) and asserts result.usage matches the second (final) chunk. |
| 13 | MH-2C | spawnCodex's SpawnResult.usage is populated when Codex emits at least one usage chunk; absent otherwise | PASS | wrapper.test.ts cases `populates SpawnResult.usage when the stream contains a usage chunk` and `omits SpawnResult.usage when no usage chunk is emitted` cover both branches. |
| 14 | MH-2D | existing text + handoff parsing stays unchanged — the usage field is purely additive | PASS | parser.test.ts original 4 cases (text chunk, handoff envelope, malformed JSON, empty line) still pass without modification. Net new test failures introduced by Plan 02-02: 0. |
| 15 | ART-2A | packages/codex-driver/src/spawn/parser.ts contains input_tokens | PASS | grep `input_tokens` parser.ts returns the UsageChunkSchema definition + ParsedLine.usage field declaration. |
| 16 | ART-2B | packages/codex-driver/src/spawn/wrapper.ts contains usage | PASS | grep `usage` wrapper.ts returns the UsageChunk import, `let usage: UsageChunk &#124; undefined` aggregation, and conditional inclusion in SpawnResult. |
| 17 | ART-2C | packages/codex-driver/test/fixtures/codex-stream-with-usage.ndjson contains input_tokens | PASS | Fixture file exists; line 4 declares `{type:"usage", usage:{input_tokens:4218, output_tokens:312}}`. |
| 18 | KL-2A | wrapper.ts → core/abstractions/AgentSpawner.ts via SpawnResult.usage type | PASS | wrapper.ts:3 imports SpawnResult from @swt-labs/core; SpawnResult.usage was added to the type definition in packages/core/src/abstractions/AgentSpawner.ts:31-34. |
| 19 | MH-3A | the swt CLI's vibe execute command constructs a CodexAgentSpawner, resolves a per-role AgentSpec from agents-templates + config.json model_overrides, and injects both into executeHandler | PASS | packages/cli/src/commands/vibe.ts now reads config via loadSwtConfig, resolves devSpec eagerly via resolveAgentSpec({role: 'dev', config, templates_dir: getBundledAgentTemplatesDir()}), constructs CodexAgentSpawner wrapped in LazyInstallSpawner, and passes both into executeHandler({spawner, devSpec}). |
| 20 | MH-3B | executeHandler no longer throws NotImplementedError when invoked through the CLI; the live path runs | PASS | packages/cli/test/commands/vibe.test.ts execute case asserts `err.text() not toContain 'Not yet implemented'` and `out.text() toContain 'Route: execute'`. The handler runs to completion writing 01-01-SUMMARY.md from the parsed Dev handoff. |
| 21 | MH-3C | model resolution honors precedence: model_overrides[role] > agents-templates/{role}.toml model field > backend default | PASS | agent-spec-resolver.test.ts cases `resolves model from agents-templates when no override` (TOML wins), `resolves model from config.model_overrides[role] when set` (override wins), and `falls back to 'default' model when neither override nor TOML model exists` cover all three precedence branches. |
| 22 | MH-3D | agents-templates/{role}.toml is read from the shipped package (not the user's filesystem) — the templates are static fixtures, not configuration | PASS | Six TOML files moved from monorepo-root agents-templates/ to packages/methodology/templates/agents/. getBundledAgentTemplatesDir() resolves them via `new URL('../../../templates/agents/', import.meta.url)`. Reachable in monorepo and once published. |
| 23 | MH-3E | CodexAgentSpawner.installAgent is called for each role before the first spawn; removeAgent is called on session shutdown | PASS | LazyInstallSpawner is the install/cleanup gate. lazy-install-spawner.test.ts case `installs the role on first spawn and reuses the install on subsequent spawns` asserts installAgent fires exactly once per role on first spawn. CLI vibe.ts calls `spawner.cleanup()` in the dispatch finally; lazy-install-spawner.test.ts case `cleanup removes every installed role and never throws even when removeAgent rejects` confirms removal + best-effort behavior. |
| 24 | ART-3A | packages/methodology/src/vibe/orchestration/agent-spec-resolver.ts contains resolveAgentSpec | PASS | File exists at packages/methodology/src/vibe/orchestration/agent-spec-resolver.ts (DEV-3B documents the path correction). Contains `export async function resolveAgentSpec`. |
| 25 | ART-3B | packages/cli/src/commands/vibe.ts contains CodexAgentSpawner | PASS | vibe.ts line 3 imports CodexAgentSpawner from @swt-labs/codex-driver; line 92 constructs `new CodexAgentSpawner()`. |
| 26 | KL-3A | vibe.ts → codex-agent-spawner.ts via new CodexAgentSpawner() construction | PASS | vibe.ts:92 — `const baseSpawner = new CodexAgentSpawner();` |
| 27 | KL-3B | agent-spec-resolver.ts → orchestration/index.ts via barrel export | PASS | packages/methodology/src/vibe/orchestration/index.ts line 5: `export * from './agent-spec-resolver.js';` |
| 28 | DEV-1A | Plan 02-01 SUMMARY records pre-existing v1.0 strict-typecheck failures in packages/codex-driver/src/spawn/wrapper.ts (env type) and packages/codex-driver/src/toml/emit.ts:54 (TomlValue array branch). Plan 02-01's new code is typecheck-clean; these failures pre-existed and surface when the codex-driver package is typechecked. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — same DEV-1D class as Phase 01 carryforward) |
| 29 | DEV-1B | Plan 02-01 SUMMARY records 1 pre-existing test failure in packages/codex-driver/test/toml.test.ts (`emits a [features] table when flags are present`) caused by toml/emit.ts:54 type error. Plan 02-01 new test file is 5/5 passing; net new failures from Plan 02-01: 0. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — pre-existing v1.0 baseline) |
| 30 | DEV-2A | Plan 02-02 NDJSON fixture schema is hand-crafted, not captured from a live `codex exec --json` run. Schema matches AgentSpawner contract's SpawnResult.usage type and OpenAI Codex CLI documentation conventions for September 2025 release. Flagged for verification once a Codex CLI install is available locally. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — schema verification cannot run without local Codex CLI install; tracked as v1.5 follow-up) |
| 31 | DEV-2B | Plan 02-02 amended files_modified mid-execution: corrected paths from packages/codex-driver/test/spawn/{parser,wrapper}.test.ts (assumed nested layout) to packages/codex-driver/test/{parser,wrapper}.test.ts (actual flat layout). | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — files_modified amendment; same pattern as Phase 01 DEV-1A) |
| 32 | DEV-3A | Plan 02-03 claimed `config.model_overrides` and `config.mcp_overrides` 'already exist (v1.0 ship)' but ConfigSchema only had `agent_max_turns`. Extended ConfigSchema with `model_overrides` and `mcp_overrides` records (both `record(AgentRoleEnum, ...)` with empty defaults). | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — files_modified amended to include packages/core/src/config/Config.ts before edit) |
| 33 | DEV-3B | Plan 02-03 paths used `packages/methodology/src/orchestration/...` but the actual orchestration directory is `packages/methodology/src/vibe/orchestration/`. Same path issue Plan 02-02 hit with the codex-driver test layout. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — no code change needed beyond amending files_modified to the real paths) |
| 34 | DEV-3C | Plan 02-03 asked for `LazyInstallSpawner` implicitly via the architectural decision but listed no file for it. Added packages/methodology/src/vibe/orchestration/lazy-install-spawner.ts and matching test file. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — the lazy-install architectural decision was resolved via AskUserQuestion at execute time, requiring a new wrapper class file) |
| 35 | DEV-3D | Plan 02-03 referenced T4 test path packages/cli/test/commands/vibe.test.ts but the directory only contained update.test.ts. Created the new file (matches existing CLI command-test convention). | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — new test file creation was always intended by T4) |
| 36 | DEV-3E | Plan 02-03 added zod dep to packages/methodology/package.json — pre-existing v1.0 bug where scope.ts imports zod but the manifest never declared it. Same class as Phase 01's codex-driver fix. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — pre-existing v1.0 hygiene fix surfaced by new test path; not Plan 02-03 scope) |
| 37 | DEV-3F | Added packages/methodology/src/state/load-config.ts exporting loadSwtConfig so the CLI can share methodology's config-loading semantics (the in-place loadConfig inside phase-detect.ts is private). | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — extended files_modified to include the new state helper) |
| 38 | DEV-3G | Plan 02-03 T4 was originally specified to use vi.doMock('execa', ...) to fake Codex output. In trial runs, vi.doMock did not intercept the bare 'execa' specifier when methodology / codex-driver source files were imported through the workspace-linked CLI test (likely a pnpm strict-isolation interaction). Switched to a node-script `codex` stub on $PATH instead. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — vitest mock-resolution limitation in pnpm strict mode is environmental, not a Plan 02-03 implementation choice) |

## Summary

**Tier:** standard
**Result:** PARTIAL
**Passed:** 27/38
**Failed:** DEV-1A, DEV-1B, DEV-2A, DEV-2B, DEV-3A, DEV-3B, DEV-3C, DEV-3D, DEV-3E, DEV-3F, DEV-3G
