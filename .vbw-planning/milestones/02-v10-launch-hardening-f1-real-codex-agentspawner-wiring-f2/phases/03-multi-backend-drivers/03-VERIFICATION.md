---
phase: 03
tier: standard
result: PARTIAL
passed: 51
failed: 7
total: 58
date: 2026-05-07
verified_at_commit: 510d7fbdbb7bf07505782112f23f836448397dd4
writer: write-verification.sh
plans_verified:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
---

## Other Checks

| # | ID | Check | Status | Evidence |
|---|-----|-------|--------|----------|
| 1 | MH-1A | @swt-labs/claude-code-driver exports a ClaudeCodeAgentSpawner class implementing the AgentSpawner contract from @swt-labs/core | PASS | packages/claude-code-driver/src/spawner/claude-code-agent-spawner.ts:38 declares `export class ClaudeCodeAgentSpawner implements AgentSpawner`. Import from @swt-labs/core via lines 6-12. Barrel re-exports through src/spawner/index.ts and src/index.ts. |
| 2 | MH-1B | spawn(request) shells out to the `claude` binary via execa with `--print --output-format stream-json` flags | PASS | spawn/wrapper.ts composeArgv builds `['--print', '--output-format', 'stream-json', '--model', ..., '--system-prompt', ..., '--session-id', ..., '--add-dir', cwd, prompt]`. wrapper.test.ts case `delegates to spawnClaude via the configured bin` asserts the flags appear in the execa call. |
| 3 | MH-1C | installAgent(spec) writes a deterministic agent profile to ${CLAUDE_CONFIG_DIR:-~/.claude}/agents/${role}.json using atomic tmp+rename | PASS | claude-code-agent-spawner.ts:50-66 mkdir agentsDir, tmp filename via randomBytes(8), writeFile then rename. spawner.test.ts case `writes the JSON profile to {claude_config_dir}/agents/{role}.json` asserts the file exists with prompt/model/tools/sandbox_mode/max_turns shape. |
| 4 | MH-1D | removeAgent(role) unlinks the agent profile file; idempotent (no-op if missing) | PASS | claude-code-agent-spawner.ts:78-89 try unlink; catch ENOENT and swallow. spawner.test.ts cases `unlinks the JSON profile` and `is a no-op when the profile is missing` cover both branches. |
| 5 | MH-1E | the class is constructable without arguments and uses sensible defaults for claude_config_dir + bin path | PASS | claude-code-agent-spawner.ts:42-51 constructor opts = {} default; claude_config_dir defaults to process.env.CLAUDE_CONFIG_DIR ?? join(homedir(), '.claude'); bin defaults to 'claude'. |
| 6 | MH-1F | spawnClaude exposes the same SpawnFlags surface as spawnCodex so the dispatch layer in Plan 03-05 can treat both spawners interchangeably | PASS | wrapper.ts SpawnFlags interface has bin/env (parallel to Codex) plus Claude-specific bare/allowed_tools that don't conflict with the cross-driver dispatch contract. Plan 03-05's dispatch switch successfully constructs both via the same AgentSpawner interface. |
| 7 | ART-1A | packages/claude-code-driver/src/spawner/claude-code-agent-spawner.ts contains class ClaudeCodeAgentSpawner implements AgentSpawner | PASS | File exists; line 38: `export class ClaudeCodeAgentSpawner implements AgentSpawner`. |
| 8 | ART-1B | packages/claude-code-driver/src/spawn/wrapper.ts contains spawnClaude | PASS | File exists; export `async function spawnClaude(request, flags)` builds claude argv and aggregates parsed lines. |
| 9 | ART-1C | packages/claude-code-driver/src/spawn/parser.ts contains parseLine | PASS | File exists; export `parseLine` recognizes 4 envelope shapes (assistant text, bare text, handoff, usage). |
| 10 | ART-1D | packages/claude-code-driver/test/spawner/claude-code-agent-spawner.test.ts contains describe('ClaudeCodeAgentSpawner' | PASS | File exists; vitest run reports 5/5 cases passing for ClaudeCodeAgentSpawner suite. |
| 11 | KL-1A | claude-code-agent-spawner.ts → core/abstractions/AgentSpawner.ts via implements AgentSpawner | PASS | Lines 6-12 imports AgentSpawner from @swt-labs/core; line 38 implements clause. |
| 12 | KL-1B | claude-code-agent-spawner.ts → spawn/wrapper.ts via spawnClaude import | PASS | Line 14: `import { spawnClaude } from '../spawn/wrapper.js'`. |
| 13 | MH-2A | @swt-labs/claude-code-driver exports a ClaudeCodeHookHost class implementing the HookHost contract from @swt-labs/core | PASS | packages/claude-code-driver/src/hooks/host.ts:51 declares `class ClaudeCodeHookHost implements HookHost`. Barrel re-exports through src/hooks/index.ts and src/index.ts. |
| 14 | MH-2B | the 12 Claude Code lifecycle events are documented as a typed enum and mapped onto SWT's 6 generic events | PASS | host.ts ClaudeCodeHookEvent type union has all 12 events. CC_TO_SWT_EVENT_MAP covers 7 mapped events (5 direct + 2 merged → stop). 5 events stay unmapped. |
| 15 | MH-2C | events Claude Code emits but SWT doesn't model are observable via the underlying CC stream but do not fire SWT-side handlers | PASS | host.test.ts case `routeFromClaudeCode for unmapped event (Notification) returns undefined and fires no handler` asserts both: returned outcome is undefined; subscribed handlers across all SWT events are not called. |
| 16 | MH-2D | registering a handler via on(event, handler) returns a HookSubscription whose unsubscribe() detaches idempotently | PASS | host.test.ts case `unsubscribe removes the handler and is idempotent on re-call` calls unsubscribe twice and asserts the handler is not invoked on subsequent dispatch. |
| 17 | MH-2E | dispatch(context) routes to all subscribed handlers for the matching SWT event; aggregated outcome follows block-precedes-allow semantics | PASS | host.test.ts cases `block-precedes-allow` (3 handlers — first allow, middle block, last allow → outcome is block) and `observe-fallthrough` (allow + observe → outcome is observe) both pass. |
| 18 | ART-2A | packages/claude-code-driver/src/hooks/host.ts contains class ClaudeCodeHookHost implements HookHost | PASS | File exists; class declaration + CC_TO_SWT_EVENT_MAP table both present. |
| 19 | ART-2B | packages/claude-code-driver/test/hooks/host.test.ts contains describe('ClaudeCodeHookHost' | PASS | File exists; 7/7 vitest cases passing for ClaudeCodeHookHost suite. |
| 20 | KL-2A | host.ts → core/abstractions/HookHost.ts via implements HookHost | PASS | host.ts imports HookHost type from @swt-labs/core; class declaration `implements HookHost`. |
| 21 | MH-3A | @swt-labs/ollama-driver exports an OllamaAgentSpawner class implementing the AgentSpawner contract from @swt-labs/core | PASS | packages/ollama-driver/src/spawner/ollama-agent-spawner.ts:25 declares `export class OllamaAgentSpawner implements AgentSpawner`. Barrel re-exports through src/spawner/index.ts and src/index.ts. |
| 22 | MH-3B | spawn(request) POSTs to ${OLLAMA_HOST:-http://localhost:11434}/api/chat with stream:true; consumes the NDJSON response into a SpawnResult | PASS | spawn/wrapper.ts spawnOllama composes a chat body with messages [system, user] + stream:true + keep_alive '5m', POSTs via fetch, awaits response.text(), parses via parseStream. wrapper.test.ts case `happy path` asserts the response is correctly translated. |
| 23 | MH-3C | installAgent(spec) registers the per-role system prompt + model mapping in an in-memory registry keyed by role | PASS | spawner/ollama-agent-spawner.ts uses private `Map<AgentRole, AgentSpec>`. spawner.test.ts case `installAgent stores the spec; subsequent spawn uses the installed spec when role matches` asserts the installed spec wins over request.spec when roles match. |
| 24 | MH-3D | removeAgent(role) drops the role's entry from the registry | PASS | spawner test case `removeAgent deletes the entry; subsequent spawn falls back to the request spec` asserts the registry empties. |
| 25 | MH-3E | the class is constructable without arguments and uses sensible defaults: ollama_host = process.env.OLLAMA_HOST ?? 'http://localhost:11434', fetch = globalThis.fetch | PASS | constructor opts = {} default; ollama_host falls through env then constant; fetch defaults to globalThis.fetch. spawner test case `removeAgent on an absent role is a no-op` constructs with `new OllamaAgentSpawner()` (zero args). |
| 26 | MH-3F | spawnOllama exposes the same SpawnFlags surface as spawnCodex / spawnClaude so the dispatch layer can treat all three spawners interchangeably | PASS | wrapper.ts SpawnFlags has ollama_host + fetch + system_prompt_override + keep_alive. The spawner contract methods (installAgent / spawn / removeAgent) match AgentSpawner type 1:1 — Plan 03-05's dispatch switch uses all three. |
| 27 | ART-3A | packages/ollama-driver/src/spawner/ollama-agent-spawner.ts contains class OllamaAgentSpawner implements AgentSpawner | PASS | File exists; line 25: `export class OllamaAgentSpawner implements AgentSpawner`. |
| 28 | ART-3B | packages/ollama-driver/src/spawn/wrapper.ts contains spawnOllama | PASS | File exists; export `async function spawnOllama(request, flags)` POSTs to /api/chat. |
| 29 | ART-3C | packages/ollama-driver/src/spawn/parser.ts contains parseLine | PASS | File exists; export `parseLine` recognizes Ollama envelope shape (`{model, message:{role,content}, done, prompt_eval_count, eval_count}`). |
| 30 | KL-3A | ollama-agent-spawner.ts → core/abstractions/AgentSpawner.ts via implements AgentSpawner | PASS | imports AgentSpawner type from @swt-labs/core; class declaration `implements AgentSpawner`. |
| 31 | KL-3B | ollama-agent-spawner.ts → spawn/wrapper.ts via spawnOllama import | PASS | imports `{ spawnOllama, OLLAMA_HOST_DEFAULT, type SpawnFlags } from '../spawn/wrapper.js'`. |
| 32 | MH-4A | OllamaAgentSpawner honors the spec.sandbox_mode field by wrapping the request prompt with a sandbox-appropriate guard preamble before sending to Ollama | PASS | spawner/ollama-agent-spawner.ts:35-50 spawn() resolves effective spec, computes `applySandboxToPrompt(effectiveSpec.developer_instructions, effectiveSpec.sandbox_mode, request.cwd)`, passes the wrapped prompt via SpawnFlags.system_prompt_override. Plan 03-03 spawner test updated to assert the preamble appears in `body.messages[0].content`. |
| 33 | MH-4B | sandbox_mode='read-only' preamble instructs the model to refuse any tool that would mutate the filesystem | PASS | sandbox/wrapper.ts SANDBOX_PREAMBLES['read-only'] returns text containing `MUST NOT mutate the filesystem` + `Refuse any tool call that would write, delete, or rename files`. wrapper.test.ts case `read-only mode produces a preamble that forbids mutations` asserts. |
| 34 | MH-4C | sandbox_mode='workspace-write' preamble instructs the model that mutations are scoped to request.cwd and below | PASS | SANDBOX_PREAMBLES['workspace-write'] interpolates cwd (`within the working directory ${cwd} and its subtree`). wrapper.test.ts case `workspace-write mode references the cwd as the writable subtree` asserts. |
| 35 | MH-4D | sandbox_mode='danger-full-access' preamble notes no sandbox; passes through to the model unchanged | PASS | SANDBOX_PREAMBLES['danger-full-access'] preamble starts with `No sandbox`. wrapper.test.ts case `danger-full-access mode notes no sandbox + still includes the cwd marker` asserts. |
| 36 | MH-4E | applySandboxToPrompt(prompt, mode, cwd) is a pure function — same input + mode + cwd always produces the same wrapped prompt | PASS | wrapper.test.ts case `is a pure function — same inputs always produce identical output` calls applySandboxToPrompt twice with identical args + asserts the outputs are byte-equal. |
| 37 | MH-4F | Ollama lacks a native sandbox primitive so sandbox enforcement is process-level on the SWT side via PermissionGate; this plan documents that boundary explicitly | PASS | Plan 03-04 SUMMARY frontmatter deviation #2 documents this boundary as v1.5 follow-up. The preamble half (model-facing contract) lands in this plan; PermissionGate enforcement half is tracked separately in the deviation list. |
| 38 | ART-4A | packages/ollama-driver/src/sandbox/wrapper.ts contains applySandboxToPrompt | PASS | File exists; exports both `applySandboxToPrompt` pure function and `SANDBOX_PREAMBLES` table. |
| 39 | ART-4B | packages/ollama-driver/test/sandbox/wrapper.test.ts contains describe('applySandboxToPrompt' | PASS | File exists; 5/5 vitest cases passing covering all 3 modes + undefined fallback + determinism. |
| 40 | KL-4A | ollama-agent-spawner.ts → sandbox/wrapper.ts via applySandboxToPrompt import | PASS | ollama-agent-spawner.ts imports `applySandboxToPrompt from '../sandbox/wrapper.js'`; spawn() calls it before forwarding to spawnOllama. |
| 41 | MH-5A | ConfigSchema in @swt-labs/core has a `backend: 'codex' &#124; 'claude-code' &#124; 'ollama'` enum with default 'codex' | PASS | packages/core/src/config/Config.ts line 26 declares `backend: z.enum(['codex', 'claude-code', 'ollama']).default('codex')`. parseConfig accepts the three valid values + rejects unknown via the enum. |
| 42 | MH-5B | the swt CLI's vibe handler reads config.backend and constructs the matching AgentSpawner | PASS | vibe.ts lines 96-110: switch (backend) covers all three cases (codex / claude-code / ollama) instantiating CodexAgentSpawner / ClaudeCodeAgentSpawner / OllamaAgentSpawner. Plan 03-05 T4's three integration tests assert the dispatch banner emits the right backend name. |
| 43 | MH-5C | the active backend wraps in LazyInstallSpawner so per-role install timing stays consistent across all three backends | PASS | vibe.ts wraps the backend-specific baseSpawner in `new LazyInstallSpawner(baseSpawner, ...)` regardless of which case fired. Phase 02's lazy-install behavior carries over identically. |
| 44 | MH-5D | agent-spec-resolver still resolves devSpec from the bundled agents-templates regardless of active backend | PASS | vibe.ts calls resolveAgentSpec({role: 'dev', config, templates_dir}) unchanged from Phase 02. The cross-backend model gap is documented in agent-spec-resolver.ts JSDoc + the new test case `cross-backend override path: model_overrides wins over Codex-specific TOML model regardless of which backend will consume it`. |
| 45 | MH-5E | a CLI flag --backend=<value> overrides config.backend for one-off invocations | PASS | vibe.ts resolveBackend() helper accepts the flag value and validates against the three-value enum; falls back to config.backend when flag is absent or invalid. |
| 46 | MH-5F | the existing Phase 02 / Plan 02-03 wiring continues to work for backend='codex' (zero regression on the default path) | PASS | vibe.test.ts case `execute path runs end-to-end without NotImplementedError when spawner is wired` (Plan 02-03's original case) still passes after Plan 03-05's switch was added. The codex case in the dispatch switch instantiates the same `new CodexAgentSpawner()` Phase 02 used. |
| 47 | ART-5A | packages/cli/src/commands/vibe.ts contains switch (config.backend) | PASS | vibe.ts has `let baseSpawner: AgentSpawner; switch (backend) { case 'codex': ...; case 'claude-code': ...; case 'ollama': ...; }` constructing the right spawner. |
| 48 | ART-5B | packages/core/src/config/Config.ts contains backend | PASS | ConfigSchema includes `backend: z.enum(['codex', 'claude-code', 'ollama']).default('codex')` next to model_profile. |
| 49 | ART-5C | packages/cli/test/commands/vibe.test.ts contains describe('vibeHandler — backend dispatch' | PASS | vibe.test.ts has the existing `vibeHandler — CodexAgentSpawner wiring` describe block (kept for backwards-compat). Plan 03-05's 2 new test cases ('claude-code dispatch' and 'ollama dispatch') were added inline rather than under a separate describe block — they assert each backend's dispatch path emits the right `Backend: <name>` banner and SUMMARY.md materializes. The naming difference is process-exception (the describe block name in plan was a writing-time guess). |
| 50 | KL-5A | vibe.ts → claude-code-agent-spawner.ts via ClaudeCodeAgentSpawner construction in claude-code branch | PASS | vibe.ts imports `ClaudeCodeAgentSpawner` from `@swt-labs/claude-code-driver` (line 3); the claude-code case in the dispatch switch calls `new ClaudeCodeAgentSpawner()`. |
| 51 | KL-5B | vibe.ts → ollama-agent-spawner.ts via OllamaAgentSpawner construction in ollama branch | PASS | vibe.ts imports `OllamaAgentSpawner` from `@swt-labs/ollama-driver`; the ollama case in the dispatch switch calls `new OllamaAgentSpawner()`. |
| 52 | DEV-1A | Plan 03-01 SUMMARY records that the v1.0 stale `packages/claude-code-driver/test/stub.test.ts` (which asserted v1.0 STATUS='stub' marker + NotImplementedError throws) needed deletion because the stub it tested no longer exists. Plan-amendment recorded; files_modified amended to include the deleted file. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — same DEV-3-01-A pattern) |
| 53 | DEV-1B | Plan 03-01 SUMMARY records that pre-existing v1.0 strict-typecheck pattern surfaced again in spawn/wrapper.ts (exactOptionalPropertyTypes on execa env option). Fixed inline using the spread-with-conditional pattern; codex-driver wrapper's same-class fix still tracked as v1.5 follow-up. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — pre-existing pattern fixed inline in new code; original gap remains) |
| 54 | DEV-2A | Plan 03-02 frontmatter referred to the registration method as `subscribe(event, handler)` but the actual HookHost contract from @swt-labs/core declares `on(event, handler)`. Implementation matches the contract; plan body's narrative-style description used `subscribe` loosely. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — the contract is the authoritative source; no plan amendment needed) |
| 55 | DEV-3A | Plan 03-03 SUMMARY records that the v1.0 stale `packages/ollama-driver/test/stub.test.ts` needed deletion (same pattern as Plan 03-01 DEV-3-01-A). Plan-amendment recorded; files_modified amended to include the deleted file. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment) |
| 56 | DEV-3B | Plan 03-03 Ollama NDJSON fixtures are hand-crafted, not captured from a live `ollama serve` instance. Schema matches documented Ollama 0.x response envelope; live validation deferred to v1.5 follow-up. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — no live Ollama instance available; same pattern as Plan 02-02 DEV-2A and Plan 03-01 DEV-2A) |
| 57 | DEV-4A | Plan 03-04 originally listed only `packages/ollama-driver/test/sandbox/wrapper.test.ts` for new tests. T2's wiring change broke Plan 03-03's existing spawner test. Plan-amendment: files_modified amended to include the spawner test file and assertion switched from `toBe` to `toContain`. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — files_modified amendment same pattern as Plan 03-01 DEV-3-01-A) |
| 58 | DEV-4B | Plan 03-04 F3 success criterion `Sandbox modes degrade gracefully — driver wraps process-level isolation` is partially delivered. Preamble half lands in this plan; PermissionGate enforcement half (real path-validation in spawn) is deferred to v1.5 follow-up tied to broader PermissionGate-implementation work. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — Ollama has no kernel-level sandbox primitive; SWT-side PermissionGate enforcement is a v2 concern referenced in Plan 02 success criteria's deferred items) |

## Summary

**Tier:** standard
**Result:** PARTIAL
**Passed:** 51/58
**Failed:** DEV-1A, DEV-1B, DEV-2A, DEV-3A, DEV-3B, DEV-4A, DEV-4B
