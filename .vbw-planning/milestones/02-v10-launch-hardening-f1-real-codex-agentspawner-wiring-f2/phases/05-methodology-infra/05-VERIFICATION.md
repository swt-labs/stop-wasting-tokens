---
phase: 05
tier: standard
result: PARTIAL
passed: 32
failed: 4
total: 36
date: 2026-05-07
verified_at_commit: 23cec4bfa83bf8d0731af06b8fd91dd782dee50c
writer: write-verification.sh
plans_verified:
  - 05-01
  - 05-02
  - 05-03
---

## Other Checks

| # | ID | Check | Status | Evidence |
|---|-----|-------|--------|----------|
| 1 | MH-1A | running `pnpm docs:gen` (or `npx tsx scripts/docs-gen.ts`) regenerates `docs/reference/{cli,config,artifacts}.mdx` from source | PASS | package.json has `"docs:gen": "tsx scripts/docs-gen.ts"`. Running `pnpm docs:gen` exits 0 and produces zero diff against the committed MDX (verified via `git status --short docs/reference/`). |
| 2 | MH-1B | config.mdx is derived from `ConfigSchema` in `@swt-labs/core/src/config/Config.ts` — every top-level key surfaces with type, default, constraints | PASS | scripts/docs-gen.ts:121 `generateConfigMdx` walks `ConfigSchema.shape`, emits one section per key with `**Type:**` (via `describeZodType`) and `**Default:**` (via `defaultValueFor`). 15 PROSE markers in `_prose/config.mdx` map 1:1 to ConfigSchema's 15 top-level keys. |
| 3 | MH-1C | cli.mdx is derived from the CLI command registry in `@swt-labs/cli/src/main.ts` | PASS | scripts/docs-gen.ts:159 `generateCliMdx` imports `buildRegistry` from `@swt-labs/cli` and iterates `registry.list()`, emitting `### swt {name}` per command with `Usage:` + description + inlined override prose. |
| 4 | MH-1D | artifacts.mdx is derived from the published Zod schemas in `@swt-labs/artifacts` | PASS | scripts/docs-gen.ts:195 `generateArtifactsMdx` imports 7 schemas (ProjectFrontmatterSchema, RoadmapSchema, PlanFrontmatterSchema, SummaryFrontmatterSchema, ResearchFrontmatterSchema, RemediationPlanFrontmatterSchema, RemediationSummaryFrontmatterSchema) and emits a section per schema with field listing via `describeZodType`. |
| 5 | MH-1E | hand-authored prose lives in `docs/reference/_prose/{cli,config,artifacts}.mdx` and is inlined at marker comments during codegen | PASS | All three `_prose/*.mdx` files exist. `loadProseBlocks` regex `\{\/\*\s*PROSE:([\w-]+)\s*\*\/\}([\s\S]*?)\{\/\*\s*\/PROSE\s*\*\/\}` parses each block; `inlineProse` substitutes by ID. Marker counts: config.mdx=15, cli.mdx=7, artifacts.mdx=8. |
| 6 | MH-1F | AUTO-DERIVE-CANDIDATE annotations from v1.0 docs are removed from generated MDX once codegen is canonical (replaced by AUTO-GENERATED header) | PASS | Generated MDX top-line: `{/* AUTO-GENERATED by scripts/docs-gen.ts. Do not edit by hand. Edit prose in docs/reference/_prose/. */}`. `head -3` of all three reference files confirms the header replaces the v1.0 `<!-- AUTO-DERIVE-CANDIDATE -->` marker. |
| 7 | MH-1G | the codegen script is hand-rolled — no new runtime or dev dependencies introduced beyond what already ships with the workspace | PASS | package.json runtime `dependencies: {}` (empty). devDeps added: `tsx ^4.19.2` (script runner), `zod ^3.23.8` (already a transitive dep, hoisted to root for direct resolution), and three workspace packages (`@swt-labs/core`, `@swt-labs/cli`, `@swt-labs/artifacts`). No new production runtime deps shipped to end users. |
| 8 | ART-1A | scripts/docs-gen.ts contains `generateConfigMdx` | PASS | File exists at scripts/docs-gen.ts:121 — `export function generateConfigMdx(): string`. Also exports `generateCliMdx`, `generateArtifactsMdx`, and `main()`. |
| 9 | ART-1B | docs/reference/_prose/config.mdx contains `PROSE:` markers | PASS | File exists; `grep -c "{/\* PROSE:" docs/reference/_prose/config.mdx` returns 15 (one block per ConfigSchema top-level key). |
| 10 | ART-1C | docs/reference/config.mdx contains `AUTO-GENERATED` header | PASS | First line of docs/reference/config.mdx is the AUTO-GENERATED comment. Same for cli.mdx + artifacts.mdx. |
| 11 | KL-1A | scripts/docs-gen.ts → packages/core/src/config/Config.ts via imports `ConfigSchema` | PASS | scripts/docs-gen.ts:20 `import { ConfigSchema } from '@swt-labs/core';`. The import resolves through the workspace devDep declared in package.json. |
| 12 | KL-1B | scripts/docs-gen.ts → packages/cli/src/main.ts via imports `buildRegistry` | PASS | scripts/docs-gen.ts:21 `import { buildRegistry } from '@swt-labs/cli';`. Workspace devDep resolves the import to packages/cli's barrel export. |
| 13 | KL-1C | docs/reference/config.mdx → docs/reference/_prose/config.mdx via inlined PROSE markers during codegen | PASS | `inlineProse(prose, override-config-${key})` reads the loaded prose blocks and substitutes by ID into the generated MDX at the `**When to override:**` line. Verified by `pnpm docs:gen` producing identical MDX (no drift). |
| 14 | MH-2A | a vitest test fails when `scripts/docs-gen.ts`'s output differs from the committed `docs/reference/{cli,config,artifacts}.mdx` files | PASS | test/docs/drift.test.ts has 3 cases (one per file). Each calls the matching generator + compares against the on-disk MDX. If `actual !== expected`, throws `Error('docs/reference/${file} is out of sync...')` then asserts `expect(actual).toBe(expected)` for vitest's reporter. |
| 15 | MH-2B | the test imports `generateConfigMdx`, `generateCliMdx`, `generateArtifactsMdx` from the generator script and compares against the on-disk MDX byte-for-byte | PASS | test/docs/drift.test.ts:7-11 imports all three named generators from `'../../scripts/docs-gen.js'`. Each test reads on-disk file via `readFileSync(...,'utf8')` and compares the generator return string via `toBe` (byte-for-byte). |
| 16 | MH-2C | the failure message names which file drifted and shows a concise diff hint (`run pnpm docs:gen to regenerate`) | PASS | drift.test.ts:34-37 throws `docs/reference/${file} is out of sync with scripts/docs-gen.ts. Run 'pnpm docs:gen' to regenerate.` — file name + actionable hint. |
| 17 | MH-2D | the drift test runs as part of the normal `pnpm test` invocation — no separate command needed | PASS | vitest.config.ts include array now contains `'test/**/*.test.ts'` alongside `'packages/*/src/**/*.test.ts'` and `'packages/*/test/**/*.test.ts'`. `pnpm vitest run test/docs/drift.test.ts` runs successfully (3/3 pass). |
| 18 | MH-2E | if a Zod schema gains a new key without prose coverage, generated config.mdx includes the key with `_(no override guidance documented)_` and the drift test STILL passes | PASS | scripts/docs-gen.ts:62-64 `inlineProse` returns `_(no override guidance documented)_` when the prose ID is missing. The drift test compares committed === generator(); a missing prose key still produces deterministic generator output, so drift stays clean. (Validated structurally — `ConfigSchema.model_overrides` and `mcp_overrides` already exercise this path: they have no `_prose/config.mdx` entry, the rendered `config.mdx` shows the fallback text, and drift.test.ts still passes 3/3.) |
| 19 | MH-2F | Plan 05-02 explicitly does not enforce 'every Zod key must have prose coverage' — that's a stricter check the user can opt into via `--strict-prose` after v1.5 | PASS | drift.test.ts only asserts structural equality (committed === generator output). It does not iterate ConfigSchema keys checking for prose coverage. The strict-prose check is documented as v1.5 follow-up scope in the SUMMARY. |
| 20 | ART-2A | test/docs/drift.test.ts contains `describe('docs drift'` | PASS | File exists; line 28: `describe('docs drift', () => {...});`. 3 cases iterating the file/generator pairs. |
| 21 | KL-2A | test/docs/drift.test.ts → scripts/docs-gen.ts via imports generator functions | PASS | drift.test.ts:7-11 imports `generateCliMdx, generateConfigMdx, generateArtifactsMdx` from `'../../scripts/docs-gen.js'`. The `.js` suffix matches the tsx/Node ESM resolution convention. |
| 22 | MH-3A | the HookEvent union in @swt-labs/core is extended with: pre_archive, post_phase, pre_phase, post_uat_fail, pre_qa, post_qa | PASS | packages/core/src/abstractions/HookHost.ts:13-27 declares the 12-variant union. Lines 22-27 are the six new SDLC events; lines 15-20 are the v1.0 generic events. Section comments mark each tier. |
| 23 | MH-3B | ConfigSchema gains a `hooks` block (optional) with one optional sub-block per event: `hooks.{event_name}: { script_path: string }` | PASS | packages/core/src/config/Config.ts:17-22 declares `HookSubBlockSchema = z.object({script_path: z.string().min(1)}).optional()`. Lines 62-77 add the `hooks` block to ConfigSchema with all 12 event keys. Validated: `parseConfig({hooks: {pre_archive: {script_path: '/tmp/x.sh'}}})` succeeds; `parseConfig({hooks: {pre_archive: {script_path: ''}}})` throws (proven by 2/2 vitest cases). |
| 24 | MH-3C | narrowing helpers ship in HookHost.ts: 12 helpers (one per event) for TypeScript type narrowing | PASS | HookHost.ts exports 12 type-guard functions: `isSessionStartEvent`, `isUserPromptSubmitEvent`, `isPreToolUseEvent`, `isPostToolUseEvent`, `isPermissionRequestEvent`, `isStopEvent`, `isPreArchiveEvent`, `isPostPhaseEvent`, `isPrePhaseEvent`, `isPostUatFailEvent`, `isPreQaEvent`, `isPostQaEvent`. Each: `ctx is HookContext & { event: <literal> }`. `grep -c "^export function is" HookHost.ts` returns 12. |
| 25 | MH-3D | each new event has a sample shell script under `templates/hooks/{event}.sample.sh` (executable + documented + harmless on dry-run) | PASS | `ls -la templates/hooks/` shows 6 files, all `-rwxr-xr-x` (mode 0755): pre-archive, post-phase, pre-phase, post-uat-fail, pre-qa, post-qa. Each first line is `#!/usr/bin/env bash`. Each documents env vars + use case + exit-code semantics. Bodies are `printf` only — no destructive operations. |
| 26 | MH-3E | existing post_archive semantics unchanged — the v1.0 hook event behavior is preserved | PASS | The v1.0 6-event taxonomy (session_start, user_prompt_submit, pre_tool_use, post_tool_use, permission_request, stop) is preserved verbatim in HookEvent union (HookHost.ts:15-20). No v1.0 event was renamed, removed, or repurposed. The 6 new SDLC events are purely additive. NB: `post_archive` itself was never a v1.0 SWT HookEvent (the v1.0 taxonomy is the 6 generic events; the SUMMARY 05-03 ac#5 evidence statement referenced `scripts/post-archive-hook.sh`, which is a VBW-plugin script that does not exist in the SWT product code — but the underlying must_have intent (v1.0 hook event behavior preserved) is structurally met). |
| 27 | MH-3F | the v1.0 6-event taxonomy is preserved alongside the 6 new events for a total of 12 events | PASS | `ALL_HOOK_EVENTS` const has length 12, asserted by hook-host-narrowing.test.ts case 6: `expect(ALL_HOOK_EVENTS).toHaveLength(12)` + helper-correspondence map iterating all 12 events. 6/6 narrowing tests pass. |
| 28 | ART-3A | packages/core/src/abstractions/HookHost.ts contains `isPreArchiveEvent` | PASS | HookHost.ts:126-130 exports `isPreArchiveEvent(ctx)` returning `ctx is HookContext & { event: 'pre_archive' }`. |
| 29 | ART-3B | packages/core/src/config/Config.ts contains `hooks` block | PASS | Config.ts:62-77 declares the `hooks` z.object with 12 sub-block keys (all 12 HookEvents). |
| 30 | ART-3C | packages/core/test/abstractions/hook-host-narrowing.test.ts contains `describe('HookHost narrowing'` | PASS | Test file exists; line 30: `describe('HookHost narrowing helpers', () => {...});`. 6 vitest cases covering positive/negative narrowing for individual helpers + ALL_HOOK_EVENTS length-12 + helper-correspondence map. |
| 31 | ART-3D | templates/hooks/pre-archive.sample.sh contains `#!/usr/bin/env bash` | PASS | First line of pre-archive.sample.sh is `#!/usr/bin/env bash`. Same for the other 5 sample scripts. File mode `-rwxr-xr-x`. |
| 32 | KL-3A | packages/core/src/abstractions/HookHost.ts → packages/core/src/config/Config.ts via HookEvent type keying the hooks config block | PASS | Config.ts uses 12 keys in the `hooks` z.object that match the 12 variants of HookEvent declared in HookHost.ts. The structural correspondence is asserted at compile-time (TypeScript will reject any divergence between Config's hooks keys and the HookEvent union). |
| 33 | DEV-1A | Plan 05-01 SUMMARY records that files_modified was amended at execution time to include `package.json` (added `tsx`, `zod`, and three `@swt-labs/*` workspace devDeps so the codegen script could resolve its imports from the root). Plan-amendment recorded. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — same class as Plans 02-03 / 03-01 / 04-02 missing-zod / workspace-devDep amendments) |
| 34 | DEV-1B | Plan 05-01 SUMMARY records that files_modified was amended to include `packages/cli/src/commands/stubs.ts` (removed the pre-existing v1.0 duplicate `update` stub entry that surfaced as a runtime error when scripts/docs-gen.ts called `buildRegistry()`). Plan-amendment recorded. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — same audit-trail pattern as Plans 03-01 / 04-01 stale-file deletions) |
| 35 | DEV-2A | Plan 05-02 T2 was specified as 'manually verify the drift detection actually fires' (mutate-then-revert on disk). Skipped the manual mutation in favor of treating the structural assertion (file content === generator output) as sufficient evidence the test fires when content drifts. Process-exception recorded. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely process-exception — vitest's `toBe` always fires on byte-distinct strings; mutate-then-revert in CI is fragile) |
| 36 | DEV-3A | Plan 05-03 originally listed source + test + sample-script files but did not list `docs/reference/config.mdx`. Adding the `hooks` block to ConfigSchema in T2 changed the codegen output for config.mdx, so the file needed regenerating + committing to keep the Plan 05-02 drift check green. Plan-amendment recorded. | FAIL | deviation type pending classification in QA Remediation Round 01 (likely plan-amendment — required for the drift check to stay green; same files_modified amendment class as DEV-1A / DEV-1B) |

## Summary

**Tier:** standard
**Result:** PARTIAL
**Passed:** 32/36
**Failed:** DEV-1A, DEV-1B, DEV-2A, DEV-3A
