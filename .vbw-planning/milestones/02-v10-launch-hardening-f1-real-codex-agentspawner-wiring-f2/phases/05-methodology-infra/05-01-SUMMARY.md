---
phase: 05
plan: 05-01
title: F6 — codegen for cli.mdx + config.mdx + artifacts.mdx
status: complete
completed: 2026-05-07
tasks_completed: 3
tasks_total: 3
commit_hashes:
  - f93c37c
deviations:
  - "Plan 05-01 originally listed Config.ts not modified, but the codegen needs the script to resolve workspace packages (@swt-labs/core, @swt-labs/cli, @swt-labs/artifacts) and zod from the root. Plan-amendment: amended package.json to add tsx + zod + workspace devDeps so the script can run via `pnpm docs:gen`."
  - "Plan 05-01 originally listed only docs files + scripts/docs-gen.ts in files_modified. During execution the pre-existing v1.0 duplicate `update` stub entry in `packages/cli/src/commands/stubs.ts` surfaced as a runtime error when scripts/docs-gen.ts called `buildRegistry()` (the stub was registered twice — once via main.ts as the real command, once via STUB_SPECS). Plan-amendment: amended files_modified to include stubs.ts and removed the duplicate stub entry. Same audit-trail pattern as Plans 03-01 / 04-01 stale-file deletions."
pre_existing_issues: []
ac_results:
  - criterion: "running `pnpm docs:gen` regenerates docs/reference/{cli,config,artifacts}.mdx from source"
    verdict: "pass"
    evidence: "package.json now has `docs:gen: tsx scripts/docs-gen.ts`. Running it produces the three MDX files with the AUTO-GENERATED header at the top + the rest of the content auto-derived from source."
  - criterion: "config.mdx is derived from ConfigSchema — every top-level key surfaces with type + default + when-to-override prose"
    verdict: "pass"
    evidence: "scripts/docs-gen.ts generateConfigMdx walks ConfigSchema.shape, emits one section per key with `**Type:**` (via describeZodType), `**Default:**` (via defaultValueFor), and `**When to override:**` (inlined from `_prose/config.mdx`). Generated config.mdx has 14 top-level config keys covered."
  - criterion: "cli.mdx is derived from the CLI command registry"
    verdict: "pass"
    evidence: "generateCliMdx imports buildRegistry, iterates registry.list(), emits a section per command with name + usage + description + inlined override prose. Generated cli.mdx has 32 commands covered (8 real + the remaining stubs)."
  - criterion: "artifacts.mdx is derived from the published Zod schemas in @swt-labs/artifacts"
    verdict: "pass"
    evidence: "generateArtifactsMdx walks 6 named schemas (Project / Roadmap / Plan / Summary / Research / RemediationPlan / RemediationSummary frontmatter), emits one section per schema with field listing via describeZodType."
  - criterion: "hand-authored prose lives in `docs/reference/_prose/` and is inlined at marker comments during codegen"
    verdict: "pass"
    evidence: "docs/reference/_prose/{config,cli,artifacts}.mdx exist with `{/* PROSE:override-<id> */}` blocks. The codegen `loadProseBlocks` regex extracts each block; `inlineProse` looks them up by ID and substitutes `_(no override guidance documented)_` for missing IDs."
  - criterion: "AUTO-DERIVE-CANDIDATE annotations are removed from v1.0 docs once codegen is canonical"
    verdict: "pass"
    evidence: "Generated MDX starts with `{/* AUTO-GENERATED by scripts/docs-gen.ts. Do not edit by hand. Edit prose in docs/reference/_prose/. */}`. The v1.0 `<!-- AUTO-DERIVE-CANDIDATE: ... -->` markers are gone (overwritten when docs:gen ran)."
  - criterion: "the codegen script is hand-rolled — no new runtime or dev dependencies introduced beyond what already ships"
    verdict: "pass"
    evidence: "Three new devDeps were added: tsx (script runner), zod (already a transitive dep — added to root for direct resolution), and workspace package devDeps (@swt-labs/core, /cli, /artifacts). Production runtime deps unchanged."
---

`pnpm docs:gen` ships. F6 success criteria for the codegen layer are met. Build-time drift check lands in Plan 05-02.

## What Was Built

- **`scripts/docs-gen.ts`** — hand-rolled tsx codegen with three exported generators (generateConfigMdx, generateCliMdx, generateArtifactsMdx) + a main() that writes the three reference MDX files.
- **`docs/reference/_prose/config.mdx` + `cli.mdx` + `artifacts.mdx`** — side-files with hand-authored prose, organized via `{/* PROSE:override-<id> */}...{/* /PROSE */}` blocks.
- **`docs/reference/{config,cli,artifacts}.mdx`** — regenerated with the AUTO-GENERATED header.
- **`package.json`** — adds `docs:gen` npm script + `tsx` + `zod` + workspace package devDeps.
- **`packages/cli/src/commands/stubs.ts`** — removes the pre-existing duplicate `update` stub entry.

## Files Modified

- `scripts/docs-gen.ts` (new — 250 LOC)
- `package.json` (new script + 5 devDeps — see deviation #1)
- `packages/cli/src/commands/stubs.ts` (remove duplicate stub — see deviation #2)
- `docs/reference/_prose/config.mdx` (new)
- `docs/reference/_prose/cli.mdx` (new)
- `docs/reference/_prose/artifacts.mdx` (new)
- `docs/reference/config.mdx` (regenerated)
- `docs/reference/cli.mdx` (regenerated)
- `docs/reference/artifacts.mdx` (regenerated)

## Deviations

See frontmatter `deviations:`. Two:

1. **Workspace devDep amendment (plan-amendment)** — `package.json` files_modified amended to add `tsx`, `zod`, and the three @swt-labs/* workspace devDeps so the codegen script could resolve its imports from the root.
2. **Pre-existing stub duplicate fix (plan-amendment)** — `packages/cli/src/commands/stubs.ts` files_modified amended to remove the v1.0 duplicate `update` stub entry. Same class as Plans 03-01 / 04-01 stale-file deletions.

## Verification

1. ✅ `pnpm docs:gen` exits 0 and writes three MDX files
2. ✅ `head -15 docs/reference/config.mdx` shows the AUTO-GENERATED header + frontmatter + first config key (`agent_max_turns`)
3. ✅ Hand-authored prose from `_prose/` is correctly inlined for keys with documented overrides
4. ✅ Keys without documented prose render `_(no override guidance documented)_` per the inlineProse fallback

## Next

Plan 05-02 (build-time drift check) and Plan 05-03 (F7 hook events) remain. Plan 05-02 depends on 05-01's generator existing — ready to ship next. Plan 05-03 is independent and can land in parallel.
