node_modules
dist
build
coverage
.changeset
pnpm-lock.yaml
.vbw-planning/research

# Auto-generated by `pnpm docs:gen` (scripts/docs-gen.ts). Prettier and the
# generator disagree on frontmatter blank-line placement; keeping these
# under the generator's authority lets `test/docs/drift.test.ts` stay green
# without fighting `pnpm format:check`.
docs/reference/artifacts.mdx
docs/reference/cli.mdx
docs/reference/config.mdx

# Vendored upstream templates — body MUST stay byte-exact with upstream so
# the recorded sha256 (in YAML frontmatter + audit-script baseline +
# CodexViaOverlayPack.upstreamSources contentHash) verifies. Letting
# Prettier reformat the body would silently break the drift-audit invariant.
references/codex/
