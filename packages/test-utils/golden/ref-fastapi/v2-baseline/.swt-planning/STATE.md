---
phase: 1
phase_name: foundation
status: planning
last_updated: 2026-01-15T10:00:00Z
reasoning_effort: medium
---

# Project State

Synthetic v2.3.5-era state file. The `reasoning_effort` frontmatter
exercises the markdown frontmatter rewrite path at
`packages/cli/src/commands/migrate.ts:219`.

## Current Phase

`01-foundation` — planning.

## Provenance

This tree is the Phase 6 plan 06-04 T1 synthesised fixture (option b
per research §5.4), NOT a recorded v2.3.5 baseline. The full v2.3.5
binary recording (option a) remains DEVN-03 placeholder work, deferred
to Phase G public-benchmark recordings. The agent-parity regression
tests at `test/regression/agent-parity/*.test.ts` gate behind the
"DEVN-03 placeholder" marker in this file (`isPlaceholderBaseline()`
check) and stay skipped until the real recording lands.
