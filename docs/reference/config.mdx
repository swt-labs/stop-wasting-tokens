{/* AUTO-GENERATED by scripts/docs-gen.ts. Do not edit by hand. Edit prose in docs/reference/_prose/. */}

---
title: Configuration reference
description: Every key in `.swt-planning/config.json` — type, default, when to override.
---

# Configuration reference

`config.json` lives at `.swt-planning/config.json` and is read on every `swt` invocation. All keys are optional — defaults from `@swt-labs/core/config/defaults.json` apply where missing.

### `agent_max_turns`

- **Type:** `Record<string, number>`
- **Default:** `{"scout":15,"qa":25,"architect":30,"debugger":80,"lead":50,"dev":75}`
- **When to override:** Bump `qa` if your QA agent regularly hits the cap on dense phases. The shipped defaults (15/25/30/80/50/75 for scout/qa/architect/debugger/lead/dev) reflect the v1.0 ship's empirical floor.

### `auto_push`

- **Type:** `"never" | "after_phase" | "always"`
- **Default:** `"never"`
- **When to override:** `"after_phase"` for solo projects with a remote; `"always"` only for ephemeral branches.

### `auto_uat`

- **Type:** `boolean`
- **Default:** `false`
- **When to override:** `false` to require explicit `swt vibe --verify` invocations. The default `true` is right for solo / pure-vibe workflows; teams may prefer explicit verification gates.

### `autonomy`

- **Type:** `"cautious" | "standard" | "confident" | "pure-vibe"`
- **Default:** `"standard"`
- **When to override:** `pure-vibe` for solo mature projects, `cautious` for regulated work. See [autonomy levels](/concepts/autonomy-levels).

### `backend`

- **Type:** `"pi"`
- **Default:** `"pi"`
- **When to override:** `"codex"` is the default and most-tested path. Switch to `"claude-code"` when running against the Claude Code CLI; `"ollama"` for local-only inference. Cross-backend model resolution requires `model_overrides[role]` to be set to a backend-appropriate value (see Phase 03 / Plan 03-05's cross-backend-model-gap notes).

### `effort`

- **Type:** `"thorough" | "balanced" | "fast" | "turbo"`
- **Default:** `"balanced"`
- **When to override:** `turbo` for prototypes, `thorough` for production. See [effort levels](/concepts/effort-levels).

### `hooks`

- **Type:** `object (optional)`
- **When to override:** Wire shell scripts to SDLC lifecycle events. Each event accepts an optional `{ script_path: "..." }` block. The script runs synchronously when the event fires; non-zero exit aborts the lifecycle action (e.g., `pre_archive` exiting 1 aborts the archive). See `templates/hooks/*.sample.sh` for shell script templates.

### `marketplace`

- **Type:** `object (optional)`
- **When to override:** The Codex Plugin Marketplace API surface is reserved for when the marketplace publishes their endpoint. When set, `swt update` queries both npm and the marketplace, surfacing any version divergence. Default is undefined → marketplace lookup stays dormant.

### `mcp_overrides`

- **Type:** `Record<string, string[]>`
- **Default:** `{}`
- **When to override:** Override the MCP server allow-list for a specific role. The default is whatever the agents-templates `{role}.toml` declares.

### `model_overrides`

- **Type:** `Record<string, string>`
- **Default:** `{}`
- **When to override:** Pin a specific model for a role: `{ "qa": "gpt-5-codex", "dev": "gpt-5-codex-thinking" }`. Required for non-Codex backends because the bundled agents-templates declare Codex-specific model identifiers.

### `model_profile`

- **Type:** `"quality" | "balanced" | "cost"`
- **Default:** `"quality"`
- **When to override:** `"fast"` to save tokens during iteration; `"quality"` is the default for shipping work.

### `planning_tracking`

- **Type:** `"manual" | "ignore" | "commit"`
- **Default:** `"manual"`
- **When to override:** `"commit"` when planning history should ship with code commits; `"ignore"` for personal exploration repos where `.swt-planning/` shouldn't appear in git.

### `prefer_teams`

- **Type:** `"auto" | "always" | "never"`
- **Default:** `"auto"`
- **When to override:** `"never"` if your environment cannot reliably create real team-scoped teammates (e.g., pure-CLI Codex without team support).

### `telemetry`

- **Type:** `object`
- **Default:** `{"enabled":false}`
- **When to override:** Telemetry is opt-in, anonymous (UUIDv4 per machine), and sanitized at the client. Set `enabled: true` + `endpoint: "https://your-collector.example.com/ingest"` to wire the production HttpSender. The privacy contract is preserved: no PII, no machine identifiers, no auth headers.

### `verification_tier`

- **Type:** `"quick" | "standard" | "deep"`
- **Default:** `"standard"`
- **When to override:** `"thorough"` for security-sensitive code or before a customer-facing release.

### `worktree_isolation`

- **Type:** `"off" | "on" | "auto"`
- **Default:** `"off"`
- **When to override:** _(no override guidance documented)_
